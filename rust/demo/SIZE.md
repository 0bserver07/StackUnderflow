# stax-wasm — measured size

Regenerated by `rust/demo/build.sh`; do not hand-edit. Every number below is
`stat -c%s` on the artifact named beside it.

    cargo build -p stax-wasm --target wasm32-unknown-unknown --release
    wasm-bindgen --target web --out-dir rust/demo/pkg \
        rust/target/wasm32-unknown-unknown/release/stax_wasm.wasm

| artifact | bytes | KiB |
|---|---:|---:|
| `target/wasm32-unknown-unknown/release/stax_wasm.wasm` (cargo) | 2715741 | 2652 |
| `demo/pkg/stax_wasm_bg.wasm` (after wasm-bindgen) | 2164469 | 2113 |
| `demo/pkg/stax_wasm_bg.wasm`, gzip -9 (what a server sends) | 834490 | 814 |
| `demo/pkg/stax_wasm.js` (glue) | 11893 | 11 |
| `demo/pkg/stax_wasm_inline.js` (the same wasm as base64, so the page fetches nothing) | 2886107 | 2818 |

That payload contains a whole SQLite (FTS5, math functions, rtree, the
dbstat/bytecode vtabs — `sqlite-wasm-rs`'s `FULL_FEATURED` set), rusqlite,
`stax-core`'s query layer and `stax-memory`'s envelope writer. For scale: the
React dashboard this project already ships is an order of magnitude larger.

Toolchain: rustc 1.97.1, wasm-bindgen 0.2.126,
wasi-sdk-33.0-x86_64-linux for the C half. Profile: workspace `[profile.release]`
— `lto = true`, `codegen-units = 1`.

`wasm-opt` is NOT installed on this box, so nothing here is post-processed — these are raw LLVM+lto outputs. A `wasm-opt -Oz` pass typically takes another 10-20%; the number is not claimed because it was not run.
