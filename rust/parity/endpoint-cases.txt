# The endpoint differ's case matrix — `rust/endpoint-parity.sh` reads this file.
#
# Format:  <id> | <METHOD> | <path-and-query> | <json body or -> | <headers or ->
#
#   id      stable; names the dumped bodies under .parity-state/endpoint-diffs/
#           A LEADING `!` marks a KNOWN-OPEN case: the differ reports it in full
#           under a loud banner and does NOT fail the gate. Unported endpoints
#           belong here, in the file, not missing from it — "we know" beats
#           "nobody looked".
#   METHOD  GET / POST / PUT / DELETE / HEAD / OPTIONS
#   target  sent verbatim, query string included; it is not re-encoded
#   body    JSON, sent as application/json; `-` or empty means no body
#   headers OPTIONAL fifth field: `Name: value` lines separated by `;;`.
#           `;;` and not `;`, because a header VALUE contains `;`
#           (`text/html; charset=utf-8`) and `,` (`X-Foo, Content-Type`).
#           Added by the rulings pass for DIV-050: `CORSMiddleware` returns
#           immediately when there is no `Origin`, so 763 rows of evidence said
#           nothing whatever about it and it could not be ported honestly until
#           a row could reach it.
#
# ORDER IS SIGNIFICANT. `deps.current_log_path` is server state that
# `POST /api/project-by-dir` writes and every project-scoped GET reads, so the
# rows run top to bottom against both servers and the state carries. Do not sort
# this file.
#
# Provenance of the row groups:
#   S-*   the four SPA page routes + the /static mount (RS-5-117..121) — the
#         wave's oracle: these serve the UNMODIFIED checked-in React bundle
#   P-*   routes/projects.py (RS-5-095..101)
#   D-*   routes/data.py (RS-5-066..070)
#   V-*   validation / error paths — the shapes a happy-path matrix never sees

# ── the React oracle ─────────────────────────────────────────────────────────
S-root              | GET | /                                  | -
S-settings          | GET | /settings                          | -
S-live              | GET | /live                              | -
S-project-1         | GET | /project/-home-u-repo              | -
S-project-nested    | GET | /project/a/b/c                     | -
S-static-index      | GET | /static/react/index.html           | -
S-static-missing    | GET | /static/react/nope.js              | -

# ── projects: the no-project state, before anything is selected ──────────────
P-current-none      | GET | /api/project                       | -
P-recent            | GET | /api/recent-projects               | -
P-providers         | GET | /api/providers                     | -

# ── /api/projects — the flagship. include_stats is the July wedge (§6b) ──────
P-list-default      | GET | /api/projects                      | -
P-list-limit20      | GET | /api/projects?limit=20             | -
P-list-page2        | GET | /api/projects?limit=20&offset=20   | -
P-list-past-end     | GET | /api/projects?limit=20&offset=99999 | -
P-list-stats        | GET | /api/projects?include_stats=true&limit=20 | -
P-list-stats-page2  | GET | /api/projects?include_stats=true&limit=20&offset=20 | -
P-list-stats-1      | GET | /api/projects?include_stats=1&limit=5 | -
P-sort-first-seen   | GET | /api/projects?sort_by=first_seen&limit=20 | -
P-sort-size         | GET | /api/projects?sort_by=size&limit=20 | -
P-sort-name         | GET | /api/projects?sort_by=name&limit=20 | -
P-sort-unknown      | GET | /api/projects?sort_by=nonsense&limit=20 | -
P-provider-claude   | GET | /api/projects?provider=claude&limit=20 | -
P-provider-multi    | GET | /api/projects?provider=claude&provider=codex&limit=20 | -
P-provider-case     | GET | /api/projects?provider=CLAUDE&limit=20 | -
P-provider-none     | GET | /api/projects?provider=nosuchprovider | -
P-worktrees-raw     | GET | /api/projects?include_worktrees=true&limit=50 | -
P-worktrees-stats   | GET | /api/projects?include_worktrees=1&include_stats=1&limit=50 | -
P-limit-clamp-hi    | GET | /api/projects?limit=999999         | -
P-limit-clamp-lo    | GET | /api/projects?limit=0              | -
P-offset-negative   | GET | /api/projects?offset=-5&limit=5    | -
P-offset-last-wins  | GET | /api/projects?limit=5&offset=1&offset=2 | -

# ── project selection: the state mutation every project-scoped GET needs ─────
P-by-dir-missing    | POST | /api/project-by-dir               | {}
P-by-dir-empty      | POST | /api/project-by-dir               | {"dir_name": ""}
P-by-dir-unknown    | POST | /api/project-by-dir               | {"dir_name": "-no-such-project-anywhere"}
P-by-dir-traversal  | POST | /api/project-by-dir               | {"dir_name": "../../etc"}
P-by-dir-known      | POST | /api/project-by-dir               | {"dir_name": "-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow"}
P-current-after     | GET  | /api/project                      | -

# ── /api/stats — needs the full collector pipeline (RS-3-062 / RS-5-066) ─────
# Ported: YES, once RS-3-062 (aggregator.summarise) landed. Both timezone signs
# are here on purpose — §6b divergence 5 says the port must inherit the React
# callers' sign-inverted offset faithfully, so a case that only sent the correct
# sign would never have proved it.
D-stats-default     | GET | /api/stats                        | -
D-stats-tz          | GET | /api/stats?timezone_offset=-480   | -
D-stats-tz-inverted | GET | /api/stats?timezone_offset=480    | -
D-stats-days        | GET | /api/stats?days=7                 | -
D-stats-days-zero   | GET | /api/stats?days=0                 | -
D-stats-details     | GET | /api/stats?details=true           | -
D-stats-include     | GET | /api/stats?include=overview       | -
D-stats-include-2   | GET | /api/stats?include=overview&include=models | -
# The 422 shape. Added by batch C after `DD-bad-int` / `MSG-tz-bad` caught the
# identical bug in the sibling handlers: no D-stats row had ever sent an
# uncoercible value, so `/api/stats` shipped `{"detail":"days"}` where FastAPI
# ships pydantic's structured list. Fixed and covered in the same pass.
D-stats-bad-int     | GET | /api/stats?days=abc               | -
D-stats-bad-bool    | GET | /api/stats?details=maybe          | -

# ── the ensure_ascii proof ───────────────────────────────────────────────────
# The `cursor` project's display_name is
#   "cursor (legacy — reingest to split by workspace)"
# and that em-dash is the whole wave-5 JSON-writer decision in one row: the CLI
# writer (`json.dumps` default, ensure_ascii=True) would ship `—`, while
# starlette's JSONResponse ships the three raw UTF-8 bytes. If this case is
# byte-identical, the port is using the right one of the two writers.
P-nonascii          | GET | /api/projects?provider=cursor&limit=5 | -
P-nonascii-stats    | GET | /api/projects?provider=cursor&limit=5&include_stats=1 | -

# ── validation + error shapes ────────────────────────────────────────────────
V-bad-bool          | GET | /api/projects?include_stats=maybe  | -
V-bad-int           | GET | /api/projects?limit=abc            | -
V-unknown-path      | GET | /api/definitely-not-a-route        | -
V-method-not-allowed | POST | /api/projects                    | {}
V-spa-method        | POST | /settings                         | {}

# ═══ BATCH A — cost / usage / dashboard family ═══════════════════════════════
#
# These rows run AFTER `P-by-dir-known`, so a project is selected and every
# `log_path`-defaulting route below resolves to it. Do not move them above it.
#
#   M-*   routes/misc.py       (RS-5-079..084)
#   PR-*  routes/pricing.py    (RS-5-094)
#   C-*   routes/commands.py   (RS-5-056..058)
#   B-*   routes/budgets.py    (RS-5-047..049)  — the only WRITING rows in the file

# ── misc: health, favicon, assets ────────────────────────────────────────────
M-health            | GET | /api/health                        | -
M-favicon           | GET | /favicon.ico                       | -
M-assets-real       | GET | /assets/index-CQL5TeFe.css         | -
M-assets-js         | GET | /assets/CostTab-CTkdsIc3.js        | -
M-assets-missing    | GET | /assets/definitely-not-a-chunk.js  | -
M-assets-nested     | GET | /assets/a/b/c.js                   | -
# `/assets/` with an empty rest: FastAPI's `{full_path:path}` matches it and the
# containment check rejects the directory itself, so this is a 400, not a 404.
M-assets-bare       | GET | /assets/                           | -
#
# `/api/pricing` and `/api/pricing/refresh` (RS-5-079/080, DIV-065) are NOT rows
# here, and that is a deliberate exception to "unported belongs in the file".
# They were added as `!` rows first and the run proved why they cannot stay: the
# Python side fetched LiteLLM and WROTE $STACKUNDERFLOW_HOME/cache/pricing.json,
# which turned every `PR-doctor*` row below from a deterministic "no overlay on
# disk" payload into a live `age_days` elapsed-time float — 5 clean cases became
# 5 divergences on one field, in one run, with no code change. A `!` row still
# issues the request; the differ only softens the VERDICT. An endpoint whose
# side effect is another endpoint's input cannot share a home with it.
# Tracked in the ledger (DIV-065) and in `routes/misc.rs`'s module docs instead.

# ── pricing doctor: read-only, three sweeps + the no-network freshness probe ─
PR-doctor           | GET | /api/pricing/doctor                | -
PR-doctor-limit     | GET | /api/pricing/doctor?limit=5        | -
PR-doctor-limit0    | GET | /api/pricing/doctor?limit=0        | -
PR-doctor-stale     | GET | /api/pricing/doctor?stale_days=90  | -
PR-doctor-bad-int   | GET | /api/pricing/doctor?limit=abc      | -

# ── commands: the enriched-dataset rebuild + the command_day_mart series ─────
C-daily             | GET | /api/commands/daily                | -
C-list-default      | GET | /api/commands                      | -
C-list-page2        | GET | /api/commands?offset=50&limit=25    | -
C-list-asc          | GET | /api/commands?order=asc&limit=10    | -
C-sort-tokens       | GET | /api/commands?sort=tokens&limit=10  | -
C-sort-tools        | GET | /api/commands?sort=tools&limit=10   | -
C-sort-steps        | GET | /api/commands?sort=steps&limit=10   | -
C-sort-time         | GET | /api/commands?sort=time&limit=10    | -
C-sort-unknown      | GET | /api/commands?sort=nonsense&limit=10 | -
C-limit-clamp-lo    | GET | /api/commands?limit=0              | -
C-limit-clamp-hi    | GET | /api/commands?limit=99999&offset=-5 | -
C-tooldist          | GET | /api/tool-distribution             | -
C-tooldist-tz       | GET | /api/tool-distribution?timezone_offset=-480 | -
C-tooldist-prov     | GET | /api/tool-distribution?provider=claude | -
C-tooldist-prov-none | GET | /api/tool-distribution?provider=nosuchprovider | -
C-tooldist-model    | GET | /api/tool-distribution?model=claude-opus-4-5-20251101 | -
C-tooldist-model-none | GET | /api/tool-distribution?model=nosuchmodel | -

# ── budgets: the ONLY rows in this file that write. Sequenced to self-clean ──
#
# `PUT` / `DELETE` persist through `settings._save`, i.e. `json.dumps(indent=2)`
# over the shared home's `config.json`. The order below is load-bearing: it ends
# with a DELETE that must restore the file byte-for-byte, and the final GET
# proves the round-trip. Never reorder, and never leave a PUT as the last row.
B-get-unset         | GET    | /api/budgets                    | -
B-put-monthly       | PUT    | /api/budgets                    | {"monthly_usd": 150.0}
B-get-monthly       | GET    | /api/budgets                    | -
B-put-daily         | PUT    | /api/budgets                    | {"daily_usd": 10.0}
B-get-both          | GET    | /api/budgets                    | -
B-get-tz            | GET    | /api/budgets?timezone_offset=-480 | -
B-put-clear-monthly | PUT    | /api/budgets                    | {"monthly_usd": null}
B-get-daily-only    | GET    | /api/budgets                    | -
B-put-negative      | PUT    | /api/budgets                    | {"daily_usd": -5}
B-put-empty-body    | PUT    | /api/budgets                    | {}
B-delete            | DELETE | /api/budgets                    | -
B-get-after-delete  | GET    | /api/budgets                    | -

# ── cost: the mart-overlay flagship + the two rollups + one interaction ──────
#
# `?period=week` is `now - timedelta(days=7)`, a rolling INSTANT with
# microseconds, so the two servers compute bounds a few ms apart. It stays a
# real row (a message inside that gap is a genuine finding, not noise) — but if
# it ever flakes, that is the reason, and the mart fast-path refuses `week` for
# the same lossiness.
CD-cost-data        | GET | /api/cost-data                     | -
CD-cost-data-tz     | GET | /api/cost-data?timezone_offset=-480 | -
CD-range-7d         | GET | /api/cost-data?range=7d            | -
CD-range-30d        | GET | /api/cost-data?range=30d           | -
CD-range-all        | GET | /api/cost-data?range=all           | -
CD-range-bad        | GET | /api/cost-data?range=nonsense      | -
CD-model-filter     | GET | /api/cost-data?model=claude-opus-4-5-20251101 | -
CD-model-none       | GET | /api/cost-data?model=nosuchmodel   | -
CD-model-and-range  | GET | /api/cost-data?model=claude-opus-4-5-20251101&range=7d | -
CD-prov-default     | GET | /api/cost-data/by-provider         | -
CD-prov-today       | GET | /api/cost-data/by-provider?period=today | -
CD-prov-all         | GET | /api/cost-data/by-provider?period=all   | -
CD-prov-week        | GET | /api/cost-data/by-provider?period=week  | -
CD-prov-bad         | GET | /api/cost-data/by-provider?period=nope  | -
CD-prov-filter      | GET | /api/cost-data/by-provider?period=all&provider=claude | -
CD-prov-filter-none | GET | /api/cost-data/by-provider?period=all&provider=nosuchprovider | -
CD-model-default    | GET | /api/cost-data/by-model            | -
CD-model-today      | GET | /api/cost-data/by-model?period=today | -
CD-model-all        | GET | /api/cost-data/by-model?period=all   | -
CD-model-week       | GET | /api/cost-data/by-model?period=week  | -
CD-model-bad        | GET | /api/cost-data/by-model?period=nope  | -
CD-interaction-miss | GET | /api/interaction/no-such-interaction | -
# A REAL interaction id, taken from `C-list-default`'s own response. The 404 row
# above proves the miss path; this proves the hit path — the full `Record`
# serialisation for the command, every response and every tool_result, including
# the `{"name","id","input"}` tool blocks and the `Counter`-shaped `tokens`.
CD-interaction-hit  | GET | /api/interaction/8f2bd929023b5151 | -

# ── batch B (sessions/search/qa/tags/bookmarks) — merged from endpoint-cases-b.txt at integration ──
# BATCH B's endpoint case matrix — the sessions / search side.
#
# Same row format as `endpoint-cases.txt`, which stays batch A's this round:
#
#   <id> | <METHOD> | <path-and-query> | <json body or ->
#
# A LEADING `!` marks a KNOWN-OPEN case: reported in full under a loud banner,
# never fatal. Unported endpoints belong HERE, in the file — "we know" beats
# "nobody looked".
#
# Run it with the batch-B ports (A owns the defaults):
#
#   STAX_ENDPOINT_CASES=rust/parity/endpoint-cases-b.txt \
#   STAX_ENDPOINT_RS_PORT=8098 STAX_ENDPOINT_PY_PORT=8099 \
#   rust/endpoint-parity.sh
#
# :8095 is the maintainer's live server and :8096/:8097 are batch A's. Never
# bind any of the three from here.
#
# ORDER IS SIGNIFICANT and this file leans on it harder than A's does:
#   * `deps.current_log_path` is server state; the `J-select` row writes it and
#     the two `*-default` rows after it read it.
#   * the `T-*` block MUTATES `tags.json`, which both servers share. The differ
#     runs Python then Rust for each case in turn, so `T-add` writes the file
#     twice with the same content (the service is idempotent) and every row
#     after it observes the same file on both sides. The block cleans up after
#     itself with `T-remove`.
#   * the three `!B-*write*` rows are LAST in their block on purpose: they stamp
#     a `uuid4` and a wall clock, so the two servers write genuinely different
#     rows and nothing may read `bookmarks.json` after them.
#
# ── DESTRUCTIVE ROWS: why the three `reindex` endpoints are NOT in this file ──
#
# `!` marks a case reported-but-not-failed. It does NOT stop the request being
# sent. The first run of this matrix learned that the hard way: `!X-reindex`
# reached Python's real `SearchService.reindex_all`, which rebuilt
# `.parity-state/fresh/search_index.db` from 0 to 383,580 messages (53 KB ->
# 520 MB, 192 seconds) and starved the differ's remaining 47 cases into
# connection errors. That state is SHARED — gate 4 diffs the CLI against a
# `fresh` home precisely because its FTS index is empty, so the LIKE fallback
# is the path under test. The index was restored to its empty schema
# immediately.
#
# So the rule for this file: a `!` row must still be SAFE to execute. The three
# reindex endpoints are writers that rebuild shared corpora and stamp a wall
# clock into their own bodies; they are recorded as open in their modules'
# doc tables and in the divergence ledger, and they are not exercised here.
#
# The two `!B-*` writer rows below ARE safe — they append one row to a
# `bookmarks.json` nothing else reads, and the block is ordered so nothing
# after them observes the file. `bookmarks.json` and `tags.json` are removed
# from the home after a run; both are absent in the pristine state.
#
# Fixtures this file names, all measured on `.parity-state/fresh`:
#   -media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow   claude, 2 sessions
#   04a86c63-4cfd-4929-91a3-3edfe80e3e2f                       3 messages
#   -Users-yadkonrad-dev-dev-year26-jun26-worldcup             claude, 19-msg session
#   927a29a6-9409-411d-ac6b-f2e7b44abdb5                       2 inline base64 images
#
# Group provenance:
#   J-*  routes/sessions.py  (RS-5-104..106)
#   X-*  routes/search.py    (RS-6-016..018)
#   Q-*  routes/qa.py        (RS-6-012..015)
#   T-*  routes/tags.py      (RS-6-019..024)
#   B-*  routes/bookmarks.py (RS-6-006..011)
#   A-*  routes/agent_teams.py (RS-5-042..044) — all known-open
# ── sessions: the no-project state, before anything is selected ──────────────
J-files-noproject   | GET | /api/jsonl-files                    | -
J-content-noproject | GET | /api/jsonl-content?file=x.jsonl     | -
# ── /api/jsonl-files, explicitly scoped ──────────────────────────────────────
J-files-project     | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
J-files-unknown     | GET | /api/jsonl-files?project=-no-such-project-anywhere | -
J-files-empty-proj  | GET | /api/jsonl-files?project=            | -
J-files-worldcup    | GET | /api/jsonl-files?project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -
# The provider filter has three distinct empty shapes; all three are here.
J-files-provider    | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider=claude | -
J-files-prov-case   | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider=CLAUDE | -
J-files-prov-multi  | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider=claude&provider=codex | -
J-files-prov-none   | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider=nosuchprovider | -
J-files-prov-blank  | GET | /api/jsonl-files?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider= | -
# ── /api/jsonl-content ───────────────────────────────────────────────────────
J-content-small     | GET | /api/jsonl-content?file=04a86c63-4cfd-4929-91a3-3edfe80e3e2f.jsonl&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
J-content-no-ext    | GET | /api/jsonl-content?file=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
J-content-bad-stem  | GET | /api/jsonl-content?file=&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
J-content-no-proj   | GET | /api/jsonl-content?file=x.jsonl&project=-no-such-project-anywhere | -
J-content-no-sess   | GET | /api/jsonl-content?file=00000000-0000-0000-0000-000000000000.jsonl&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
# The base64 elision — the reason this endpoint is affordable. The `raw_media`
# row is two orders of magnitude larger and proves the stub is the ONLY thing
# that differs between them.
J-content-media     | GET | /api/jsonl-content?file=927a29a6-9409-411d-ac6b-f2e7b44abdb5.jsonl&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -
J-content-raw-media | GET | /api/jsonl-content?file=927a29a6-9409-411d-ac6b-f2e7b44abdb5.jsonl&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup&raw_media=1 | -
# ── the state mutation, then the two default-project reads ───────────────────
J-select            | POST | /api/project-by-dir                | {"dir_name": "-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow"}
J-files-default     | GET  | /api/jsonl-files                   | -
J-content-default   | GET  | /api/jsonl-content?file=04a86c63-4cfd-4929-91a3-3edfe80e3e2f.jsonl | -
# ── sessions: validation + the unported endpoint ─────────────────────────────
# DIV-053 says a 422 `detail` list is only APPROXIMATED. These three rows are
# the counter-evidence: `missing`, `bool_parsing` and (below) `int_parsing` all
# come out byte-identical against the reference, so the caveat is narrower than
# it was written. What remains unpinned is the wording pydantic may change
# between releases — see `ci_dep_drift_fastapi_routes`.
J-content-no-file   | GET | /api/jsonl-content?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
J-content-bad-bool  | GET | /api/jsonl-content?file=04a86c63-4cfd-4929-91a3-3edfe80e3e2f.jsonl&raw_media=maybe | -
# ── search ───────────────────────────────────────────────────────────────────
X-stats             | GET | /api/search/stats                   | -
X-empty             | GET | /api/search?q=                      | -
X-empty-echo        | GET | /api/search?q=&page=-3&per_page=5    | -
X-punct-only        | GET | /api/search?q=%21%21%21             | -
X-basic             | GET | /api/search?q=sqlite                 | -
X-basic-small       | GET | /api/search?q=sqlite&per_page=5      | -
X-page2             | GET | /api/search?q=sqlite&page=2&per_page=5 | -
X-page-clamp-hi     | GET | /api/search?q=sqlite&page=99999&per_page=5 | -
X-page-clamp-lo     | GET | /api/search?q=sqlite&page=-4&per_page=5 | -
X-perpage-clamp     | GET | /api/search?q=sqlite&per_page=9999   | -
X-filter-project    | GET | /api/search?q=sqlite&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
X-filter-role       | GET | /api/search?q=sqlite&role=user&per_page=5 | -
X-filter-model      | GET | /api/search?q=sqlite&model=claude-opus-4-20250514&per_page=5 | -
X-filter-dates      | GET | /api/search?q=sqlite&date_from=2026-01-01&date_to=2026-06-30&per_page=5 | -
X-filter-nohit      | GET | /api/search?q=sqlite&project=-no-such-project-anywhere | -
# The sanitiser proof: `NOT` must arrive as a literal term, not an operator.
X-operators         | GET | /api/search?q=use+NOT+null           | -
X-quote             | GET | /api/search?q=%22unbalanced          | -
X-multi-word        | GET | /api/search?q=session+fk+index&per_page=5 | -
X-bad-int          | GET | /api/search?page=abc                 | -
# DIV-079 — `per_page` is a DIVISOR and was clamped from above only, so
# `?per_page=0` reached `(total + per_page - 1) // per_page` and came back a
# **500** (ZeroDivisionError through the handler's blanket `except`), while a
# negative reached SQLite as an unbounded `LIMIT`. Fixed Python-first under
# ruling 8: `per_page: int = Query(20, ge=1)`, so both legs are pydantic's
# `greater_than_equal` 422 — a DIFFERENT body from `X-bad-int`'s
# `int_parsing`, because a bound failure carries a `ctx`. Both legs are pinned
# on both endpoints; the ceiling stays a silent clamp (`X-perpage-clamp`,
# `Q-list-clamp`), which is the asymmetry these rows exist to hold still.
X-perpage-zero      | GET | /api/search?q=sqlite&per_page=0      | -
X-perpage-negative  | GET | /api/search?q=sqlite&per_page=-5     | -
# The floor is checked on the EMPTY-query path too — the early return echoes
# `per_page`, so a guard placed after it would have shipped a 200 here.
X-perpage-zero-empty | GET | /api/search?q=&per_page=0           | -
# DIV-471 — the floor made DIV-107's bignum leg REACHABLE on this param, and
# ruling 4's saturating `opt_pyint` is what makes it come out right: pydantic's
# ints are arbitrary-precision, so a huge positive COERCES, clears `ge=1`, and
# `min(per_page, 100)` clamps it to 100 (a 200, not a 422) — while a huge
# negative clears coercion and fails the bound, echoing the raw string. Two
# rulings meet on these rows; neither alone answers both legs.
X-perpage-bignum     | GET | /api/search?q=sqlite&per_page=99999999999999999999999 | -
X-perpage-bignum-neg | GET | /api/search?q=sqlite&per_page=-99999999999999999999999 | -
# `5.5` is NOT an integer to pydantic even though the float is representable —
# `int_parsing`, no `ctx`. The third of the three shapes this param can answer.
X-perpage-float      | GET | /api/search?q=sqlite&per_page=5.5    | -
# DIV-078 / `POST /api/search/reindex` is DELIBERATELY ABSENT — see the
# "destructive rows" note in this file's header. It rebuilt the shared
# `.parity-state/fresh` search index (0 -> 383,580 rows, 520 MB, 192 s) on the
# Python side of the very first run, which is state gate 4 depends on being
# EMPTY. `!` suppresses the failure, not the request.
# ── qa ───────────────────────────────────────────────────────────────────────
Q-list              | GET | /api/qa                              | -
Q-list-page         | GET | /api/qa?page=2&per_page=5            | -
Q-list-clamp        | GET | /api/qa?page=-1&per_page=9999        | -
Q-search            | GET | /api/qa?search=index                 | -
Q-search-operator   | GET | /api/qa?search=index+AND+scan        | -
Q-status            | GET | /api/qa?resolution_status=looped     | -
Q-status-open       | GET | /api/qa?resolution_status=open       | -
Q-project           | GET | /api/qa?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
Q-dates             | GET | /api/qa?date_from=2026-01-01&date_to=2026-06-30 | -
Q-stats             | GET | /api/qa/stats                        | -
Q-byid-missing      | GET | /api/qa/no-such-qa-pair              | -
Q-bad-int          | GET | /api/qa?page=abc                     | -
# DIV-079's other half — same divisor, same declared floor, same bytes but
# for the `loc`. See the note above `X-perpage-zero`.
Q-perpage-zero      | GET | /api/qa?per_page=0                   | -
Q-perpage-negative  | GET | /api/qa?per_page=-5                  | -
# DIV-471's other half — see the note above `X-perpage-bignum`.
Q-perpage-bignum     | GET | /api/qa?per_page=99999999999999999999999 | -
Q-perpage-bignum-neg | GET | /api/qa?per_page=-99999999999999999999999 | -
# DIV-080 / `POST /api/qa/reindex` is DELIBERATELY ABSENT, same reason.
# ── tags — this block mutates tags.json and cleans up after itself ───────────
T-cloud             | GET | /api/tags                            | -
T-session           | GET | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
T-session-unknown   | GET | /api/tags/session/no-such-session    | -
T-browse-empty      | GET | /api/tags/browse/parity-b            | -
T-add-blank         | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {"tag": "   "}
# Deterministic on purpose: `add_manual_tag` has no clock and no random id, and
# it is idempotent, so Python writing then Rust writing yields one identical
# body from each. The case-insensitivity is the point of the mixed case.
T-add               | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {"tag": "Parity-B"}
T-add-again         | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {"tag": "parity-b"}
T-session-after     | GET | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
T-cloud-after       | GET | /api/tags                            | -
T-browse-after      | GET | /api/tags/browse/parity-b            | -
T-browse-case       | GET | /api/tags/browse/PARITY-B            | -
T-remove            | DELETE | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/parity-b | -
T-remove-again      | DELETE | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/parity-b | -
T-session-clean     | GET | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
T-cloud-clean       | GET | /api/tags                            | -
# `dict[str, str]` handed an int: pydantic's `string_type` entry, byte-exact.
T-add-non-string    | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {"tag": 3}
# DIV-075 / `POST /api/tags/reindex` is DELIBERATELY ABSENT, same reason.
# ── bookmarks — reads and error legs first, the stamped writers LAST ─────────
B-list              | GET | /api/bookmarks                       | -
B-list-sort-title   | GET | /api/bookmarks?sort_by=title         | -
B-list-sort-unknown | GET | /api/bookmarks?sort_by=nonsense      | -
B-list-tag          | GET | /api/bookmarks?tag=perf              | -
B-session           | GET | /api/bookmarks/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
B-add-no-session    | POST | /api/bookmarks                      | {}
B-add-empty-session | POST | /api/bookmarks                      | {"session_id": ""}
B-toggle-no-session | POST | /api/bookmarks/toggle               | {}
B-delete-missing    | DELETE | /api/bookmarks/no-such-bookmark   | -
B-put-missing       | PUT | /api/bookmarks/no-such-bookmark      | {"notes": "x"}
# DIV-073: `uuid4()` + `datetime.now(UTC)`. Two servers, two ids, two clocks —
# a byte differ can only ever prove the shape, so these report and never fail.
# Nothing below them reads `bookmarks.json`.
!B-add              | POST | /api/bookmarks                      | {"session_id": "04a86c63-4cfd-4929-91a3-3edfe80e3e2f", "title": "parity b", "notes": "n", "tags": ["parity"]}
!B-toggle           | POST | /api/bookmarks/toggle               | {"session_id": "04a86c63-4cfd-4929-91a3-3edfe80e3e2f", "title": "parity b"}

# ═══ BATCH C — the service-layer remainder ═══════════════════════════════════
#
# Every row below runs AFTER `P-by-dir-known`, so a project is selected and the
# `log_path`-defaulting routes resolve to it. Each member's sidecar file
# (`parity/endpoint-cases-c-<name>.txt`) opens with its own copy of that
# selection row so it can be run standalone with `--only`; the copies are
# stripped here, where the shared file already selects earlier.
#
#   X-*             routes/export.py          (RS-5-073)
#   OPT-/PRE-/CMD-  routes/optimize.py        (RS-5-074..076)
#   PL-*            routes/plan.py            (RS-5-090)
#   CMP-*           routes/compare.py         (RS-5-059)
#   Y-*             routes/yield_route.py     (RS-5-116)
#   CB-*            routes/context_budget.py  (RS-5-060)
#   CR-*            routes/context_replay.py  (RS-5-061)
#   DD-/MSG-/MS-    routes/data.py            (RS-5-067..069)
#
# `POST /api/refresh` (RS-5-070) is ported and has NO ROW, here or anywhere. It
# re-runs ingest — it writes the store every row above reads — and a `!` row
# still ISSUES the request (DIV-059). Its procedure is `rust/REFRESH-DIFFER.md`,
# on a throwaway state copy with its own ports.

# ── from parity/endpoint-cases-c-export.txt ─────────────────────────
# BATCH C / export — the case matrix for `GET /api/export` (RS-5-073).
#
# Same row format as `parity/endpoint-cases.txt`, whose header documents it:
#
#   <id> | <METHOD> | <path-and-query> | <json body or ->
#
# A leading `!` marks a KNOWN-OPEN case: reported in full under a loud banner,
# never fatal. Every row below is a read-only GET; nothing here writes.
#
# Run it with batch C's own ports (A owns :8096/:8097 defaults):
#
#   STAX_ENDPOINT_CASES=rust/parity/endpoint-cases-c-export.txt \
#   rust/endpoint-parity.sh
#
# :8095 is the maintainer's live server. Never bind it.
#
# NOTE on the id prefix: batch B's `routes/search.py` rows also use `X-`. The
# two sets were checked and do not collide — B's are `X-stats`, `X-basic`,
# `X-page*`, `X-filter-*`, `X-operators`, `X-quote`, `X-multi-word`,
# `X-bad-int`; these are all `X-csv-*`, `X-json-*` or a validation name none of
# those uses. The only row shared with the merged matrix is `P-by-dir-known`,
# which the integrator strips.
#
# ── ORDER ────────────────────────────────────────────────────────────────────
#
# `deps.current_log_path` is server state, so the file opens with the selection
# row every merged matrix needs. `/api/export` itself does NOT read it — every
# scope it uses comes from the query string — but the row is here so this file
# runs standalone in the same state the shared matrix establishes. The
# integrator strips it when merging (it already runs earlier there).
#
# ── SIDE EFFECTS: none, and that was checked rather than assumed ─────────────
#
# `routes/export.py` opens its own connection and calls `schema.apply(conn)` on
# every request — a MIGRATION runner. On `.parity-state/fresh` it is a no-op:
# `PRAGMA user_version` is 30 and the highest migration in both trees is
# `v030_live_indexes.sql`, so `_discover()` yields nothing and nothing is
# written. Measured on the harness store before these rows were written. If the
# home is ever repointed at a store behind on migrations, the FIRST request
# from the Python side migrates it and the Rust side then reads a store the
# reference just changed — see DIV-132 before moving this file to a new home.
#
# ── WHAT CAN BE GREEN, AND WHAT CANNOT ───────────────────────────────────────
#
# Only two of the four period names are stable within a calendar day:
#
#   period=today  -> scope spec `today`   midnight..23:59:59, no microseconds  STABLE
#   period=all    -> scope spec `all`     unbounded                            STABLE
#   period=week   -> scope spec `7days`   now - 7d, CARRIES THE MICROSECOND    rolling
#   period=month  -> scope spec `30days`  now - 30d, CARRIES THE MICROSECOND   rolling
#   (no period)   -> the rollup: today + 7days + 30days, plus a `generated`
#                    wall-clock stamp IN THE BODY                             rolling
#
# `period=month` does NOT reach `scope.parse_period("month")` (the calendar
# month) — `EXPORT_PERIOD_MAP` maps it to `30days`. So this endpoint has no
# calendar-month window at all, and `month` is as rolling as `week` is.
#
# The rolling bounds land in the JSON payload as `since` / `until`, so a
# `format=json` row on `week` / `month` / the rollup can NEVER be byte-equal:
# two servers, two microsecond samples. Those rows carry `!`. The CSV renderer
# does not print the bounds, so the same windows diff cleanly there — with the
# same residual risk `CD-prov-week` carries in the shared file: a message
# landing inside the few-millisecond gap between the two clock reads is a real
# (if rare) divergence, not noise.
#
# ── COST, which is why the shapes below are scoped the way they are ──────────
#
# `deep` is `fmt == "json"`, and the deep pass runs the WHOLE aggregator
# pipeline once per in-scope project. `format=json` with no project filter over
# `period=all` would run it for ~300 projects — the same class of accident
# `!X-reindex` was (DIV-078). Every JSON row below is therefore bounded, either
# by `period=today` (one project has messages today on this snapshot) or by an
# explicit `project=`. The CSV rows are pure aggregate SQL and are safe
# unbounded; `X-csv-all` sweeps 383,580 messages four times and takes seconds,
# which is inside the differ's 300 s timeout with room to spare.
#
# Fixtures, measured on `.parity-state/fresh`:
#   -media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow   claude, 1,279 messages
#   -Users-yadkonrad-dev-dev-year26-jun26-worldcup             claude, 32 messages
#   providers present: claude, codex, cursor, gemini, grok, antigravity, …


# ── CSV, the cheap window: every query-parameter branch ──────────────────────
X-csv-today         | GET | /api/export?format=csv&period=today | -
X-csv-today-prov    | GET | /api/export?format=csv&period=today&provider=claude | -
X-csv-prov-none     | GET | /api/export?format=csv&period=today&provider=nosuchprovider | -
# `if provider:` — the EMPTY string is falsy and must filter nothing, rather
# than becoming `WHERE projects.provider = ''` and emptying the export.
X-csv-prov-blank    | GET | /api/export?format=csv&period=today&provider= | -
X-csv-project       | GET | /api/export?format=csv&period=today&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
X-csv-project-multi | GET | /api/export?format=csv&period=today&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -
X-csv-project-none  | GET | /api/export?format=csv&period=today&project=-no-such-project-anywhere | -
X-csv-exclude       | GET | /api/export?format=csv&period=today&exclude=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
# include AND exclude naming the same slug: exclude is applied second and wins.
X-csv-both-filters  | GET | /api/export?format=csv&period=today&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&exclude=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -

# ── CSV, the full sweep and the rolling windows ──────────────────────────────
# The flagship: four unbounded passes over the whole messages table, every
# project, every model, priced through the primed price book (DIV-056).
X-csv-all           | GET | /api/export?format=csv&period=all | -
# DIV-131's provider pooling, on a slug that really does exist under three
# providers here (claude / codex / antigravity). The `daily` rows keep them
# apart on `(day, provider, slug)`; the per-project roll keys on the SLUG alone,
# so the single `projects` entry reports the first-seen provider with the counts
# summed across all three. If the port had keyed that map on `(provider, slug)`
# — the obvious "fix" — this row is where it would show.
X-csv-multiprov     | GET | /api/export?format=csv&period=all&project=-Users-yadkonrad-dev-dev-year26-feb26-chimera | -
# Rolling instants. The CSV body does not render `since`/`until`, so these are
# expected GREEN — a red here means a message landed in the clock gap.
# DIV-379: the three rows below are CLOCK-BEARING — the rolling window's row set sweeps with `now` even though no timestamp prints. A SOLITARY divergence here must be re-run before it is believed (measured drifting live 2026-08-02); a reproducing divergence is real. Pinning is impossible (the endpoint has no absolute-window params); the principled fix is a retry-once row marker, filed as a harness improvement in TASKS-RS.
X-csv-week          | GET | /api/export?format=csv&period=week | -
X-csv-month         | GET | /api/export?format=csv&period=month | -
# No `period` at all: the today + 7d + 30d rollup. `generated` is in the JSON
# payload but NOT in the CSV rendering, so this one can be green too.
X-csv-rollup        | GET | /api/export?format=csv | -

# ── JSON: the pretty, ensure_ascii=True body and the deep breakdowns ─────────
# This is the writer proof for this endpoint. `render_export_json` is
# `json.dumps(payload, indent=2)` — the CLI writer — so a non-ASCII project
# name ships ESCAPED here while `/api/projects` ships it raw. If this row is
# byte-identical the port picked the right one of the two writers.
X-json-today        | GET | /api/export?format=json&period=today | -
X-json-project      | GET | /api/export?format=json&period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
X-json-small-proj   | GET | /api/export?format=json&period=all&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -
# Zero candidates: the deep pass produces four empty lists and the daily /
# projects / models sections are empty too. The `totals` block is the literal's
# int/float mix (`cost_usd: 0.0`, everything else `0`) — DIV-057's split.
X-json-miss         | GET | /api/export?format=json&period=all&project=-no-such-project-anywhere | -
# `?format=csv&format=json` — starlette's scalar rule takes the LAST value, so
# this is a JSON export. The intuitive "first wins" would fail here.
X-json-last-wins    | GET | /api/export?format=csv&format=json&period=today | -

# ── JSON on a rolling window: reported every run, never green ────────────────
# `since` / `until` carry the microsecond of each server's own clock read, so
# these two are structurally undiffable. Safe to execute (read-only, and both
# are project-scoped so the deep pass runs once per window, not 300 times).
!X-json-week        | GET | /api/export?format=json&period=week&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -
# The rollup additionally embeds `generated`, a full ISO instant.
!X-json-rollup      | GET | /api/export?format=json&project=-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -

# ── validation: the 422 and the two 400 shapes ───────────────────────────────
# `format` is `Query(...)`, i.e. REQUIRED — an absent one is pydantic's
# `missing` entry, not a 400.
X-no-format         | GET | /api/export | -
X-no-format-period  | GET | /api/export?period=today | -
# `?format=` is PRESENT, so it passes the required check and then fails the
# allow-list: a 400 with a `detail` STRING, not a 422 with a detail LIST.
X-empty-format      | GET | /api/export?format= | -
X-bad-format        | GET | /api/export?format=xml | -
# The detail embeds `sorted({...})` through repr: `['csv', 'json']`, single
# quotes, space after the comma.
X-bad-format-case   | GET | /api/export?format=CSV | -
# `if period is not None and period not in _VALID_PERIODS` — `?period=` is not
# None, so the empty string reaches the allow-list and 400s.
X-empty-period      | GET | /api/export?format=csv&period= | -
X-bad-period        | GET | /api/export?format=csv&period=fortnight | -
# The scope vocabulary is NOT the export vocabulary: `7days` is a valid
# `parse_period` spec and an invalid `?period=`.
X-scope-period      | GET | /api/export?format=csv&period=7days | -
X-bad-period-case   | GET | /api/export?format=csv&period=Today | -
# Both invalid: `format` is checked FIRST, so the format message wins.
X-both-invalid      | GET | /api/export?format=xml&period=fortnight | -

# ── method ──────────────────────────────────────────────────────────────────
X-method            | POST | /api/export?format=csv | {}

# ── from parity/endpoint-cases-c-optimize.txt ─────────────────────────
# Batch C / `optimize` — endpoint differ cases for `routes/optimize.py`.
#
# Format is `rust/parity/endpoint-cases.txt`'s, read that file's header first.
# The integrator merges these into the shared file and strips the leading
# `P-by-dir-known` row, which already runs earlier there.
#
# ── ORDER IS SIGNIFICANT, and here it is significant TWICE ───────────────────
#
# The usual reason applies (`deps.current_log_path` is server state). The second
# reason is specific to this module: `/api/optimize` returns `"cache": "hit"` or
# `"cache": "miss"` **in the response body**, so the in-process memo is an
# answer-changer and the rows below are a scripted hit/miss sequence, not a set.
# Both servers start cold and read the same file top to bottom, so they agree —
# but reordering, deleting or re-running a single row changes the expected
# bytes of every later row with the same cache key. See DIV-111.
#
# Cache key = (period, sorted(project), sorted(exclude)) + store.db's mtime_ns.
#
# ── the rolling-window caveat ────────────────────────────────────────────────
#
# `7days` / `30days` bound on `now`, so the two servers compute bounds a few
# milliseconds apart and a message inside that gap is a real (if rare)
# divergence — the same property `CD-prov-week` already carries. It bites at
# most ONCE per cache key here, because every later request on that key is
# served from the memo. The `force` row is deliberately on `period=all`, whose
# bounds are `None` on both sides, so the one row that recomputes on purpose is
# the one row with nothing to drift.
#
# ── LAW 7 ────────────────────────────────────────────────────────────────────
#
# `/api/optimize` writes nothing: the detectors only stat/read under
# `~/.claude`, the pricing engine only reads `models.toml`, and the memo is
# process memory. Verified against the sources, not the docstring (DIV-118).
#
# `/api/optimize/prescriptions` reads the same `~/.claude` CLAUDE.md files the
# bloat detector already scanned and writes nothing;
# `/api/optimize/claudemd-preview` is a pure function of the request body — no
# filesystem read at all. Both are case-row-safe; both have rows below.


# ── validation, first: these never reach the cache and never write to it ─────
OPT-bad-period      | GET | /api/optimize?period=week           | -
OPT-empty-period    | GET | /api/optimize?period=               | -
OPT-period-case     | GET | /api/optimize?period=30Days         | -
OPT-bad-force       | GET | /api/optimize?force=maybe           | -

# ── the default key (30days, (), ()) — miss, then two hits ──────────────────
# `period=30days` is the DECLARED default, so the third row is the same cache
# key as the first two and must come back "hit" on both servers.
OPT-default         | GET | /api/optimize                       | -
OPT-default-again   | GET | /api/optimize                       | -
OPT-period-30days   | GET | /api/optimize?period=30days         | -
# starlette takes the LAST occurrence of a scalar, so this is still 30days.
OPT-period-last     | GET | /api/optimize?period=today&period=30days | -

# ── the calendar-stable periods, one fresh key each ─────────────────────────
OPT-today           | GET | /api/optimize?period=today          | -
OPT-7days           | GET | /api/optimize?period=7days          | -
OPT-month           | GET | /api/optimize?period=month          | -

# ── period=all: miss, hit, then the force bypass and its consequence ────────
# `force=true` skips the READ and still writes back, so OPT-all-after-force is
# a hit on the FORCED payload. The force row sits at the end of its key group
# precisely so it cannot poison a row that expects the pre-force value.
OPT-all             | GET | /api/optimize?period=all            | -
OPT-all-again       | GET | /api/optimize?period=all            | -
OPT-all-force       | GET | /api/optimize?period=all&force=true | -
OPT-all-after-force | GET | /api/optimize?period=all            | -
# `force=false` is the default and takes the cached value.
OPT-all-force-false | GET | /api/optimize?period=all&force=false | -

# ── project / exclude scoping, all on the stable `all` window ───────────────
OPT-project-self    | GET | /api/optimize?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
OPT-project-unknown | GET | /api/optimize?period=all&project=-no-such-project-anywhere | -
OPT-exclude-self    | GET | /api/optimize?period=all&exclude=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
OPT-both-filters    | GET | /api/optimize?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&exclude=-no-such-project-anywhere | -

# ── the sorted-key collision: `?project=b&project=a` == `?project=a&project=b`
# `tuple(sorted(project))` means the SECOND of these is a cache HIT on the
# first, even though the query strings differ. That is the single most
# surprising thing about this endpoint's cache and it gets its own pair.
OPT-multi-ba        | GET | /api/optimize?period=all&project=zzz-b&project=aaa-a | -
OPT-multi-ab        | GET | /api/optimize?period=all&project=aaa-a&project=zzz-b | -

# ── the empty-result end of the range ───────────────────────────────────────
# Every filter matches nothing: empty `waste`, empty mart-backed detectors, and
# `total_waste_usd` renders as whatever the FILESYSTEM detectors leave behind
# (they ignore the store entirely — DIV-116).
OPT-scope-empty     | GET | /api/optimize?period=today&project=-no-such-project-anywhere | -


# ── GET /api/optimize/prescriptions ─────────────────────────────────────────
# No cache here — every call recomputes, so these rows are order-independent
# among themselves. They DO depend on the project selection above: with no
# `?project=`, the slug falls back to `deps.current_log_path`'s basename, which
# `P-by-dir-known` set. `?project=` with an unknown slug resolves to an empty
# project-id list, which is "matched nothing", NOT "the whole store".
PRE-bad-period      | GET | /api/optimize/prescriptions?period=week | -
PRE-default         | GET | /api/optimize/prescriptions          | -
PRE-all             | GET | /api/optimize/prescriptions?period=all | -
PRE-today           | GET | /api/optimize/prescriptions?period=today | -
PRE-month           | GET | /api/optimize/prescriptions?period=month | -
PRE-project-self    | GET | /api/optimize/prescriptions?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
PRE-project-unknown | GET | /api/optimize/prescriptions?period=all&project=-no-such-project-anywhere | -
# An EMPTY `?project=` is falsy in Python and falls back to the active project,
# exactly as if the parameter were absent.
PRE-project-empty   | GET | /api/optimize/prescriptions?period=all&project= | -

# ── POST /api/optimize/claudemd-preview ─────────────────────────────────────
# A pure function of the body: no filesystem, no store write, no cache. Every
# row here is independent of every other row in the file.
CMD-clean           | POST | /api/optimize/claudemd-preview | {"text": "# Title\n\nA short paragraph that is here once.\n"}
CMD-comments        | POST | /api/optimize/claudemd-preview | {"text": "# Title\n\n<!-- author note\nspanning lines -->\n\nBody text goes here and is long enough to matter for the dedupe rule check.\n\n\n\n\nTail.\n"}
CMD-dupes           | POST | /api/optimize/claudemd-preview | {"text": "# T\n\nA duplicated paragraph long enough to pass the sixty character floor easily.\n\nA duplicated paragraph long enough to pass the sixty character floor easily.\n\nEnd.\n"}
CMD-empty-text      | POST | /api/optimize/claudemd-preview | {"text": ""}
CMD-label           | POST | /api/optimize/claudemd-preview | {"text": "# T\n\n<!-- x -->\n", "file_label": "repo/CLAUDE.md"}
CMD-sessions        | POST | /api/optimize/claudemd-preview | {"text": "# T\n\n<!-- x -->\n", "sessions_per_month": 250}
# Lax coercion: a numeric string, a whole float and `true` are all valid ints.
CMD-sessions-str    | POST | /api/optimize/claudemd-preview | {"text": "# T\n\n<!-- x -->\n", "sessions_per_month": " 5 "}
CMD-sessions-float  | POST | /api/optimize/claudemd-preview | {"text": "# T\n\n<!-- x -->\n", "sessions_per_month": 5.0}
CMD-sessions-true   | POST | /api/optimize/claudemd-preview | {"text": "# T\n\n<!-- x -->\n", "sessions_per_month": true}
# An unknown key is ignored, not rejected.
CMD-extra-key       | POST | /api/optimize/claudemd-preview | {"text": "# T\n", "nope": 1}

# ── the 422 shapes, each measured against the reference before it was written
CMD-missing-text    | POST | /api/optimize/claudemd-preview | {}
CMD-text-int        | POST | /api/optimize/claudemd-preview | {"text": 3}
CMD-text-null       | POST | /api/optimize/claudemd-preview | {"text": null}
CMD-label-int       | POST | /api/optimize/claudemd-preview | {"text": "x", "file_label": 3}
CMD-sessions-zero   | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": 0}
CMD-sessions-false  | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": false}
CMD-sessions-huge   | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": 100001}
CMD-sessions-abc    | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": "abc"}
CMD-sessions-frac   | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": 5.5}
CMD-sessions-list   | POST | /api/optimize/claudemd-preview | {"text": "x", "sessions_per_month": []}
CMD-all-bad         | POST | /api/optimize/claudemd-preview | {"sessions_per_month": "z", "file_label": 2, "text": 1}
CMD-not-an-object   | POST | /api/optimize/claudemd-preview | []

# ── deliberately ABSENT rows, with the reason ───────────────────────────────
#
# * **malformed JSON.** pydantic reports
#   `{"type":"json_invalid","loc":["body",<byte offset>],"ctx":{"error":<parser
#   message>}}`, and both the offset and the message come from pydantic-core's
#   own parser. The port emits a fixed `offset 0 / "Expecting value"` shape,
#   which is only verified for leading garbage. Recorded as DIV-119 rather
#   than asserted here — no row claims a shape that was not measured.
# * **the 413 branch.** It needs a body over 2 MB, which does not belong in a
#   line-oriented case file. Covered by a unit test on the rendered error
#   instead (`the_oversize_guard_is_the_413_and_it_counts_bytes`).
# * **an empty request body.** The differ's `-` means "send no body at all",
#   which also drops the `content-type`; FastAPI's answer then depends on the
#   client's framing rather than on the port. Covered by a unit test
#   (`an_empty_body_and_a_non_object_body_fail_differently`).

# ── from parity/endpoint-cases-c-plan.txt ─────────────────────────
# Batch C / member `plan` — case rows for `GET /api/plan` (RS-5-090).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file and strips the leading
# `P-by-dir-known` selection row, which already runs earlier there.
#
# `/api/plan` takes NO query parameters — it is a pure function of
# `config.json`, `store.db` and the clock — so the matrix is thin by nature.
# What it can still prove is the null-plan payload byte for byte, the two
# router fallbacks, and that the endpoint is genuinely side-effect free.
#
# Side effects: NONE. Every row below is a GET or an unclaimed method on
# `/api/plan`. The handler opens the store read-write (that is what
# `store/db.py::connect` does on both sides) but issues only SELECTs; the
# Python side additionally runs `schema.apply`, which is idempotent DDL the
# harness store already satisfies. Nothing under `$STACKUNDERFLOW_HOME` is
# written. Law 7 is satisfied.
#
# COVERAGE GAP, stated up front (see DIV-c-plan.md): the harness home's
# `config.json` carries no `plan_name` / `plan_monthly_usd`, so `PL-plan`
# exercises the NULL branch only. The spend rollup, the billing window, the
# burn projector and the currency conversion are covered by unit tests, not by
# this matrix. Setting a plan is a home mutation, not a case row.

# ── the state row this file needs first (integrator strips it) ───────────────

# ── the endpoint ─────────────────────────────────────────────────────────────
# The happy path. On the harness home this is the no-plan payload:
#   {"plan":null,"usage":null,"projection":null,"currency":{...}}
# `currency` is stamped even in the null branch, and `rate_from_usd` must render
# `1.0` and not `1` — a one-byte difference the pyjson writer is the only thing
# guarding.
PL-plan             | GET | /api/plan                          | -

# Repeat immediately. Python memoises the spend rollup (`_SPEND_CACHE`) and the
# port does not (DIV-091), so this row is the cheapest available proof that the
# memo cannot change the answer within one store revision. It is also the row
# that would catch a first-call-only side effect.
PL-plan-repeat      | GET | /api/plan                          | -

# `/api/plan` is not project-scoped — it has no `_require_project()` guard — so
# it must answer identically whether or not a project has been selected. This
# row runs AFTER the selection above; a divergence here would mean one of the
# two servers is leaking `deps.current_log_path` into a global rollup.
PL-plan-any-project | GET | /api/plan                          | -

# ── error / fallback shapes ──────────────────────────────────────────────────
# The route is GET-only, so FastAPI's router raises HTTPException(405) and the
# installed handler renders `{"detail":"Method Not Allowed"}` as JSON. axum's
# native 405 is an empty body with no content-type; `crate::json` restores
# starlette's. Body is `-` because a 405 is decided before the body is read.
PL-plan-post        | POST | /api/plan                         | -
PL-plan-put         | PUT  | /api/plan                         | -
PL-plan-delete      | DELETE | /api/plan                       | -

# Neighbouring paths nothing claims — FastAPI's `{"detail":"Not Found"}`, not
# starlette's plain-text default. `/api/plans` (plural) is the typo a client
# actually makes; the trailing slash is the one a proxy adds.
PL-plan-plural      | GET | /api/plans                         | -
# ── DIV-133 — CLOSED by the rulings pass. `redirect_slashes`, all of it ─────
#
# `/api/plan/` was the one row in the matrix that sent a trailing slash, and it
# was `!` because the answer is a `307` on EVERY one of the 93 endpoints and
# `NormalizePathLayer` (the obvious fix) answers `200`. `crate::path_semantics`
# reproduces the redirect instead: a fallback that re-tests the toggled path and
# emits starlette's own response. Measured before it was written —
#
#   307, `content-length: 0`, NO `content-type`, and an ABSOLUTE
#   `location` built from the `Host` header and put through
#   `quote(url, safe=":/%#?=@[]!$&'()*+,;")`.
#
# The `location` is now compared (`parity/src/endpoints.rs`), which it never was
# — a `307` has an empty body and no content-type, so status alone had been the
# entire test and a redirect to the wrong URL would have scored identical.
PL-plan-slash       | GET | /api/plan/                         | -
PL-plan-subpath     | GET | /api/plan/status                   | -
# The query string rides along, RAW: uvicorn never decodes it and `quote`'s safe
# set keeps `?`, `=`, `&` and `%` intact.
SL-projects-query   | GET | /api/projects/?limit=2             | -
SL-plan-query-pct   | GET | /api/plan/?a=b&c=d%20e             | -
# `rstrip("/")` takes EVERY trailing slash, not one.
SL-plan-double      | GET | /api/plan//                        | -
# The redirect is decided before the method is: the router never gets far enough
# to raise its 405, so a method the path does not accept still redirects.
SL-plan-put         | PUT  | /api/plan/                        | {}
SL-plan-head        | HEAD | /api/plan/                        | -
SL-plan-post        | POST | /api/plan/                        | {}
# The SPA page routes are starlette routes too.
SL-settings         | GET | /settings/                         | -
SL-live             | GET | /live/                             | -
# The OTHER direction — `route_path + "/"`. `/project/{full_path:path}` matches
# the empty rest on the reference (DIV-462), so `/project/` is a 200 and
# `/project` redirects to it. Both were wrong here before this pass: axum's
# `{*full_path}` needs at least one character, so `/project/` 404'd and
# `/project` had nothing to redirect TO.
SL-project-append   | GET | /project                           | -
SL-project-empty    | GET | /project/                          | -
# Where the redirect must NOT fire, which is the half a normalising layer gets
# wrong. `//` rstrips to the empty string and matches nothing; `/nope/` trims to
# a path no route claims; `/` is excluded by name (`route_path != "/"`).
SL-root-double      | GET | //                                 | -
SL-unknown-slash    | GET | /nope/                             | -
# A POST that redirects: the reference answers before the handler runs, so this
# row cannot select a project as a side effect — which is what makes it safe to
# put in an ORDER-DEPENDENT file at all (law: case rows are side-effect-free).
SL-by-dir-slash     | POST | /api/project-by-dir/              | {"dir_name": "-no-such-project-anywhere"}
# The two rows other batches recorded as deliberately ABSENT because DIV-133 was
# open — `endpoint-cases.txt` says so at both sites. They are here now.
SL-compare-slash    | GET | /api/sessions/compare/             | -
SL-ollama-bare      | GET | /ollama-api                        | -
# Decode-then-redirect, in one row: `%61` is `a`, so uvicorn hands starlette
# `/api/plan/`, which redirects to `/api/plan` — and the `location` carries the
# DECODED spelling, not the escape. DIV-168 and DIV-133 in the same request.
SL-encoded-slash    | GET | /api/pl%61n/                       | -

# A query string on a parameter-less endpoint is ignored, not rejected: neither
# FastAPI nor axum validates unknown query keys, so this must be byte-identical
# to `PL-plan`. It is here because "and then someone adds a parameter" is how a
# 422 quietly appears on a surface that never had one.
PL-plan-junk-query  | GET | /api/plan?nonsense=1&days=7        | -

# ── from parity/endpoint-cases-c-compare.txt ─────────────────────────
# BATCH C / compare — `GET /api/compare` (RS-5-059).
#
# Format and conventions: see the header of `endpoint-cases.txt`. This is a
# SIDECAR file; the integrator merges it into the shared matrix and strips the
# leading `P-by-dir-known` row, which already runs earlier there.
#
# ── READ THIS BEFORE WONDERING WHY THE 200s ARE ALL `!` ──────────────────────
#
# The payload is `{"period": …, "models": [...], "generated": time.time()}`.
# `generated` is a wall-clock float, read *after* the query runs, and the differ
# compares BODY BYTES with no field masking. So every 200 from this endpoint is
# a guaranteed divergence on one field, in every run, forever — not a port
# defect and not something a port can fix. Every 200 row below is therefore a
# `!` (known-open): reported under the banner, never fatal.
#
# That would leave the endpoint with no evidence at all, so the evidence was
# produced out of band instead. With `generated` pinned to the same value on
# both sides, `build_compare_payload` was run against
# `.parity-state/fresh/store.db` (the differ's own corpus, opened read-only) for
# fourteen argument combinations — both codepaths, both filters, the case-fold,
# the misses — and every one rendered BYTE-IDENTICAL to the Python. Method,
# script and the one seam that broke it are in `DIV-c-compare.md` (DIV-085).
#
# The two things that WOULD have to be true for these rows to go green:
#   * the differ learns to mask a named field per case, or
#   * `pyserver.py` pins `time.time()` — which its own docstring forbids
#     ("NOT patched, on purpose: anything that changes a RESPONSE").
# Neither is this batch's file to edit. Flagged, not decided.
#
# The 4xx/405 rows below carry no `generated` at all and ARE deterministic, so
# they are plain rows and they do gate.
#
# Side effects: none. Every codepath here is `SELECT`-only. Python additionally
# runs `schema.apply(conn)` per request, which is a `PRAGMA user_version` read
# and an immediate return on a current store (DIV-087) — the differ boots the
# Python server first, which has already applied it. No row writes. DIV-059 is
# satisfied.

# ── project selection (stripped on merge; here so the file stands alone) ─────

# ── the four periods ─────────────────────────────────────────────────────────
# `month` is the default and is stable within a calendar month; `today` is
# stable within a calendar day; `all` is unbounded. `week` is the odd one —
# `PERIOD_MAP` maps it to `7days`, a ROLLING instant with microseconds, so the
# two servers compute bounds a few milliseconds apart and a message inside that
# gap is a second, real divergence on top of `generated`. Same property
# `CD-prov-week` already carries in the shared file, same reason.
!CMP-default        | GET | /api/compare                       | -
!CMP-month          | GET | /api/compare?period=month          | -
!CMP-today          | GET | /api/compare?period=today          | -
!CMP-all            | GET | /api/compare?period=all            | -
!CMP-week           | GET | /api/compare?period=week           | -

# ── the period allow-list: 400s, and they are byte-deterministic ─────────────
# `_VALID_PERIODS` is a TUPLE and the detail is `', '.join(_VALID_PERIODS)`, so
# the message reads `today, week, month, all` — declaration order, NOT sorted.
# `services/compare.py::_resolve_scope` raises the same sentence for the same
# four aliases with `sorted(PERIOD_MAP)` instead (`all, month, today, week`);
# the route's 400 is what makes that second spelling unreachable over HTTP.
# `7days` is the sharpest case: it is a perfectly valid *scope* spec and still a
# 400 here, because the route validates the ALIAS layer.
CMP-bad-period      | GET | /api/compare?period=nonsense       | -
CMP-bad-7days       | GET | /api/compare?period=7days          | -
CMP-bad-30days      | GET | /api/compare?period=30days         | -
CMP-bad-empty       | GET | /api/compare?period=               | -
CMP-bad-case        | GET | /api/compare?period=Month          | -
CMP-bad-space       | GET | /api/compare?period=%20month       | -
# Scalar query parameters take the LAST occurrence (starlette builds
# `QueryParams._dict` by comprehension), so this 400s on `nonsense` and not on
# the valid `today` that precedes it.
CMP-bad-last-wins   | GET | /api/compare?period=today&period=nonsense | -

# ── provider: a scalar filter, and it does not behave like the project one ───
# `LOWER(provider) = ?` against a lowered parameter on the mart path, so
# `CLAUDE` matches — while the fallback path compares `projects.provider = ?`
# raw and the same spelling matches nothing there. Both spellings are here.
!CMP-prov-claude    | GET | /api/compare?provider=claude       | -
!CMP-prov-case      | GET | /api/compare?provider=CLAUDE       | -
!CMP-prov-codex     | GET | /api/compare?period=all&provider=codex | -
!CMP-prov-miss      | GET | /api/compare?provider=nosuchprovider | -
# `?provider=` is the empty string, NOT an absent parameter, and the mart path
# tests it two different ways in two adjacent functions: `if provider_filter:`
# (falsy — filters no session rows) and `if provider_filter is not None:`
# (true — prunes the model list to models that HAVE a session row). DIV-086.
!CMP-prov-empty     | GET | /api/compare?provider=             | -
!CMP-prov-last-wins | GET | /api/compare?provider=codex&provider=claude | -

# ── project: repeatable, and it switches the whole implementation ────────────
# `project_filter is None` is the mart fast-path's gate. One `project=` — even a
# slug that matches nothing — drops the request onto the raw `messages` join and
# re-prices every assistant message through the cost engine, because
# `model_day_mart` carries no `project_id`. These rows are the only ones in the
# file that exercise the pricing seam (DIV-056), which is why the miss case is
# here too: it proves the fallback returns `[]` rather than falling back again.
!CMP-proj-1         | GET | /api/compare?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
!CMP-proj-all       | GET | /api/compare?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
!CMP-proj-today     | GET | /api/compare?period=today&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
!CMP-proj-multi     | GET | /api/compare?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&project=-home-u-nope | -
!CMP-proj-miss      | GET | /api/compare?project=-no-such-project-anywhere | -
!CMP-proj-prov      | GET | /api/compare?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider=claude | -
!CMP-proj-prov-empty | GET | /api/compare?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&provider= | -

# ── method + unknown-parameter shapes ────────────────────────────────────────
# Deterministic: no body from the handler at all.
CMP-method          | POST | /api/compare                      | {}
CMP-method-put      | PUT  | /api/compare                      | {}
# An unknown query parameter is ignored, not rejected — FastAPI does not reject
# extras on a query signature. This one 200s, so it is `!` like its siblings.
!CMP-unknown-param  | GET | /api/compare?nonsense=1            | -

# ── from parity/endpoint-cases-c-yield.txt ─────────────────────────
# Batch C / member `yield` — cases for GET /api/yield (RS-5-116).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. Ids are
# prefixed `Y-`. ORDER IS SIGNIFICANT; do not sort this file.
#
# ── LAW 7 clearance ──────────────────────────────────────────────────────────
#
# `/api/yield` shells out to git. Every invocation is READ-ONLY —
# `rev-parse --git-dir`, `log`, `rev-list` — and the endpoint writes no file, no
# store row and no cache. The full audit is DIV-095 in `parity/DIV-c-yield.md`.
# That is why these rows exist at all: an endpoint that wrote anything would get
# NO row, `!` marker or not (DIV-059, DIV-078).
#
# ── What can legitimately differ, and is not a port defect ───────────────────
#
# The payload is a function of the MACHINE's git working trees, not only of the
# store. Read DIV-095 before filing a divergence here. In short:
#
#  * a commit landing in any indexed `cwd` between the reference request and the
#    port request moves `follow_commit_sha` / `_msg` / `_age_hours` and can flip
#    `classification`. So can a rebase (reachability from HEAD). This worktree
#    is itself one of the indexed repos.
#  * `week` / `7days` / `30days` are ROLLING instants — `parse_period` reads the
#    clock, so the two servers compute bounds milliseconds apart and a session
#    inside that gap is in one payload and not the other. Same property
#    `CD-prov-week` already carries. `today`, `month` and `all` are stable
#    within a calendar day.
#  * `Y-all` is the expensive row: 1,310 sessions / 186 distinct cwds (22 of
#    which exist on this host and reach git) on the harness store, measured at
#    ~8 s for the bulk cwd query alone. It fits inside the differ's 300 s
#    per-case timeout with room, but if it ever does not, dropping THIS row
#    loses nothing the other period rows do not already cover.

# Select a project so `deps.current_log_path` is set. The integrator strips this
# row when merging — it already runs earlier in the shared file. `/api/yield`
# itself is global and does not read the current project, but the shared file's
# ordering contract is the contract.

# ── the happy path and every period branch ───────────────────────────────────
# `Y-today` is the empty case on the harness store (0 sessions started today),
# which is the one that proves the summary ships `"total_cost":0.0` — a FLOAT.
# There is no `sum()` in `yield_tracker.py`; the cost fields are seeded with the
# literal `0.0` and stepped with `+=`, so an int `0` here would be a divergence
# in the OTHER direction. DIV-096.
Y-today             | GET | /api/yield?period=today            | -
Y-default           | GET | /api/yield                         | -
Y-month             | GET | /api/yield?period=month            | -
Y-week              | GET | /api/yield?period=week             | -
Y-7days             | GET | /api/yield?period=7days            | -
Y-30days            | GET | /api/yield?period=30days           | -
Y-all               | GET | /api/yield?period=all              | -

# `week` and `7days` normalise to the SAME scope inside the tracker, but the
# body echoes the raw `period` string — so these two must differ in exactly one
# field and agree in every other. Both rows are here for that reason.

# ── the project filter (a repeatable query parameter) ────────────────────────
Y-project           | GET | /api/yield?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
Y-project-multi     | GET | /api/yield?period=all&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&project=-Users-yadkonrad-dev-dev-year26-jan26-StackUnderflow | -
# A slug nothing matches: the empty-entries shape again, this time reached
# through the SQL `IN (…)` rather than through an empty window.
Y-project-miss      | GET | /api/yield?period=month&project=-no-such-project-anywhere | -
# The filter narrows a period that WOULD have had rows — `jan26` has no July
# sessions, so this must come back empty while `Y-month` does not.
Y-project-narrows   | GET | /api/yield?period=month&project=-Users-yadkonrad-dev-dev-year26-jan26-StackUnderflow | -

# ── validation: every 400 shape ──────────────────────────────────────────────
# The detail joins `_VALID_PERIODS` in TUPLE order, so `week` and `all` sit in
# the middle: "Valid: today, week, month, all, 7days, 30days". `routes/cost.py`'s
# sibling message IS sorted; these are not copy-pasteable.
Y-bad-period        | GET | /api/yield?period=nonsense         | -
# `?period=` is the EMPTY string, not an absent parameter — it fails the
# allow-list rather than falling back to `month`.
Y-empty-period      | GET | /api/yield?period=                 | -
# `parse_period`'s own vocabulary is not this route's: `yesterday` is rejected
# here even though a reader might expect the scope parser to be the gate.
Y-unknown-period    | GET | /api/yield?period=yesterday        | -
# Case sensitivity: the allow-list is exact, so `Month` is a 400.
Y-period-case       | GET | /api/yield?period=Month            | -
# starlette resolves a repeated SCALAR to the LAST occurrence, so this is
# `month` and a 200 — not `all`, and not a 422.
Y-period-last-wins  | GET | /api/yield?period=all&period=month | -
# An unknown query parameter is ignored by FastAPI, not rejected.
Y-unknown-param     | GET | /api/yield?period=today&nope=1     | -

# ── from parity/endpoint-cases-c-ctxbudget.txt ─────────────────────────
# Batch C / ctxbudget — `GET /api/context-budget` (RS-5-060).
#
# Format and conventions: see the header of `endpoint-cases.txt`. The integrator
# folds these rows into that file and strips the leading `P-by-dir-known`, which
# already runs earlier there.
#
# ── What these rows prove ────────────────────────────────────────────────────
#
# The endpoint has THREE answer shapes and the case list covers all three:
#
#   * no `project`                     → the global budget
#   * a slug the store does not know   → 404 `Unknown project slug: <slug>`
#   * a slug it DOES know whose on-disk path is NULL/empty/gone
#                                      → 200, the GLOBAL shape, not a 404
#
# The fourth shape — a known slug with a live directory, which is the only one
# that emits `memory:project_CLAUDE.md` and the project agent/MCP slices —
# CANNOT be exercised here: every one of the 335 `projects` rows in
# `.parity-state/fresh/store.db` has `path IS NULL`, and populating one would be
# a write to the shared store. It is covered by unit tests in
# `routes/context_budget.rs` and `services/context_budget.rs` instead. Recorded
# as a coverage gap, not papered over.
#
# ── Side effects: none from the port, one from the reference ─────────────────
#
# LAW 7 audit. The port does no writes at all. The reference calls
# `schema.apply(conn)` per request, which is the migration ladder — a no-op on
# any store a server has already booted against (and the harness boots Python
# first for exactly this reason). No file, cache or store row is written by any
# row below. `schema.apply` is not ported: DIV-102.
#
# ── The one hazard the integrator must know about: DIV-103 ──────────────────
#
# This endpoint reads the maintainer's REAL `~` — `~/.claude/CLAUDE.md`,
# `~/.claude.json`, `~/.claude/skills/*/SKILL.md`, `~/.claude/agents/*.md`. Both
# servers inherit the same `$HOME` from `endpoint-parity.sh`, so they read the
# same files; but `~/.claude.json` is rewritten by any Claude Code session
# running on the machine, and the two servers read it a few milliseconds apart.
# A rewrite landing inside that window changes the `mcp:*` slices — or, if the
# reader catches a truncated file, removes them entirely — and the differ will
# report a divergence that is not one. Rows are left UNMARKED (a `!` would hide
# a real regression forever), so if `CB-*` fails on the `mcp:` slices alone,
# re-run before believing it.


# ── the global branch: no `project` at all ───────────────────────────────────
CB-global           | GET | /api/context-budget | -

# ── a known slug — 200 with the GLOBAL shape, because `path IS NULL` ─────────
CB-known-slug       | GET | /api/context-budget?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -

# DIV-101's row: this slug names FOUR provider rows (claude, codex, antigravity,
# grok) and `get_project` is a `fetchone` with no ORDER BY, so exactly one of
# them decides the answer. Both sides must pick the same one.
CB-multi-provider   | GET | /api/context-budget?project=-Users-yadkonrad-dev-dev-year26-jan26-StackUnderflow | -

# A non-claude provider's slug, to prove the branch is provider-blind.
CB-gemini-slug      | GET | /api/context-budget?project=stackunderflow | -

# ── the 404 branch, and the shapes that reach it ─────────────────────────────
CB-slug-unknown     | GET | /api/context-budget?project=no-such-project-anywhere | -
# `?project=` is the EMPTY STRING, not `None` — it is a slug lookup that misses,
# so the detail string ends in a space. A port that treated it as "absent" would
# answer 200 here.
CB-slug-empty       | GET | /api/context-budget?project= | -
# The slug is never joined to a path; traversal is just a miss.
CB-slug-traversal   | GET | /api/context-budget?project=../../etc/passwd | -
# Percent-decoding happens before the lookup, so this is the SAME query as
# CB-known-slug and must answer identically.
CB-slug-encoded     | GET | /api/context-budget?project=%2Dmedia%2Dtmos%2Dbumblebe%2Ddev%2Ddev%2Dyear26%2Djul26%2DStackUnderflow | -
# starlette scalars take the LAST occurrence, so this 404s on `nope` even though
# a valid slug came first. First-wins would answer 200.
CB-slug-last-wins   | GET | /api/context-budget?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&project=nope | -

# ── the shapes a happy-path matrix never sees ────────────────────────────────
# An undeclared query parameter is ignored, not a 422.
CB-extra-param      | GET | /api/context-budget?project=nope&details=1&limit=abc | -
# GET-only path: FastAPI's 405, `{"detail":"Method Not Allowed"}`.
CB-method-post      | POST | /api/context-budget | -

# ── from parity/endpoint-cases-c-ctxreplay.txt ─────────────────────────
# Batch C / ctxreplay — `routes/context_replay.py` (RS-5-061).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file and strips the leading
# `P-by-dir-known` row, which already runs earlier there.
#
# ORDER IS SIGNIFICANT. `deps.current_log_path` is server state, and this
# endpoint's fence reads it whenever no `?project=` / `?log_path=` is given, so
# the project must be selected before the first CR-* row runs.
#
# SIDE EFFECTS: none. Every row is a GET that only reads. Two things that look
# like writes are not:
#   * Python's handler calls `schema.apply(conn)` per request — a MIGRATION.
#     On `.parity-state/fresh` (`PRAGMA user_version` = 30, the newest
#     migration is v030) it iterates zero migrations and writes nothing. The
#     Rust port does not call it at all (DIV-106).
#   * Python memoises the built timeline in a process-local dict
#     (`_CONTEXT_CACHE`). No other endpoint reads it and it never touches disk,
#     so it cannot leak into another case the way DIV-059's pricing cache did.
#     Not ported — DIV-105.
#
# Session ids below are real rows in `.parity-state/fresh`:
#   04a86c63-… project 314 (-media-…-StackUnderflow), 3 messages, seq 2369/3036/3576
#   44b8f238-… project 314, 1276 messages, seq 1285 … 5832062
#   3cf184ca-… project  16 (-Users-…-chimera--worktrees-all-issues), 68 messages
# `seq` is NOT per-session-sequential — it is a global-ish counter — which is
# why the cutoffs are the values they are and not 1/2/3.


# ── the happy path, and every `at` branch ────────────────────────────────────
CR-full           | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
CR-at-mid         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3036 | -
CR-at-first       | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=2369 | -
# Below every seq: zero events, and `total_tokens` must be the int `0`.
CR-at-below       | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=2368 | -
CR-at-zero        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=0 | -
CR-at-negative    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=-1 | -
CR-at-past-end    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=99999999 | -
# starlette keeps the LAST occurrence of a scalar query key, not the first.
CR-at-repeated    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3576&at=3036 | -
# `%2B` is a literal `+`; pydantic and `i64::from_str` both accept it.
CR-at-plus        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%2B3036 | -
# A bare `+` decodes to a space; pydantic strips before parsing.
CR-at-spaced      | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=+3036+ | -

# ── the heavy session: 1276 messages, every raw_json parsed ──────────────────
# The real payload the scrubber drags across, and the only row that exercises
# the preview truncation, the tool-label table and the `\uXXXX` token
# accounting at volume.
CR-big-full       | GET | /api/context-replay/44b8f238-3609-4445-97a9-37a5a99f1c9a | -
CR-big-at         | GET | /api/context-replay/44b8f238-3609-4445-97a9-37a5a99f1c9a?at=3153599 | -

# ── the miss: advisory, never a 404 ─────────────────────────────────────────
CR-unknown        | GET | /api/context-replay/no-such-session-at-all | -
CR-unknown-at     | GET | /api/context-replay/no-such-session-at-all?at=5 | -
# A path parameter that needs percent-decoding, on a session that cannot exist.
CR-unknown-utf8   | GET | /api/context-replay/caf%C3%A9 | -
# Starlette's `str` converter stops at `/`, so this matches no route at all and
# is FastAPI's `{"detail":"Not Found"}` — the port must 404 the same way rather
# than matching an empty `session_id`.
CR-no-segment     | GET | /api/context-replay/ | -

# ── the cross-project fence ─────────────────────────────────────────────────
# No `?project=` / `?log_path=`, so the scope is the current project (set by
# the row at the top). A session in ANOTHER project is fenced to the empty
# shape plus the warning — never that project's transcript.
CR-fence-current  | GET | /api/context-replay/3cf184ca-83e0-4c6c-8f8a-9b49bcfc89e5 | -
# An explicit `?project=` overrides the current project, and now the SAME
# session is in scope and serves its full timeline.
CR-scope-explicit | GET | /api/context-replay/3cf184ca-83e0-4c6c-8f8a-9b49bcfc89e5?project=-Users-yadkonrad-dev-dev-year26-feb26-chimera--worktrees-all-issues | -
# …which fences the StackUnderflow session in the same breath.
CR-fence-project  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?project=-Users-yadkonrad-dev-dev-year26-feb26-chimera--worktrees-all-issues | -
# A slug naming no project is the EMPTY scope `[]`, which fences everything —
# distinct from "no scope", which fences nothing. Getting these two confused is
# the whole reason the resolver returns `list | None`.
CR-scope-nomatch  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?project=-no-such-project-anywhere | -
# A blank / whitespace `?project=` is FALSY and falls through to the next leg
# (here: the current project), so this must equal CR-full.
CR-scope-blank    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?project= | -
CR-scope-space    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?project=%20 | -
# `?log_path=`'s BASENAME is the slug; the directory part is ignored.
CR-log-path       | GET | /api/context-replay/3cf184ca-83e0-4c6c-8f8a-9b49bcfc89e5?log_path=/anywhere/-Users-yadkonrad-dev-dev-year26-feb26-chimera--worktrees-all-issues | -
CR-log-path-fence | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?log_path=/anywhere/-Users-yadkonrad-dev-dev-year26-feb26-chimera--worktrees-all-issues | -
# Precedence: an explicit `project` beats `log_path` beats the current project.
CR-scope-both     | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow&log_path=/anywhere/-Users-yadkonrad-dev-dev-year26-feb26-chimera--worktrees-all-issues | -
# A trailing slash on `log_path`: `PurePath(...).name` ignores it.
CR-log-path-slash | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?log_path=/anywhere/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow/ | -

# ── validation: the only non-200 this endpoint can produce ──────────────────
# `at: int | None` — FastAPI's own 422, raised before the handler body runs.
CR-bad-at         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=abc | -
CR-empty-at       | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at= | -
CR-at-fractional  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=5.5 | -
CR-at-bool        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=true | -

# ── DIV-107 — the pydantic int grammar, enumerated ──────────────────────────
#
# `crate::qs::opt_int` was `raw.trim().parse::<i64>()`, which is three whole
# classes stricter than pydantic's lax string→int. All of these were measured on
# the running reference (`fastapi 0.141.1` / `pydantic 2.13.4`) before the parser
# was rewritten; the block deliberately carries the ACCEPTED and the REFUSED
# side of every rule, because a fix that only widens is as wrong as one that
# only narrows and only the refusals can catch it.
#
# The `''`-vs-`'0'` truthiness class is here on purpose: it is thrice-proven
# elsewhere (`--project ''`, `backup verify --name ''`, `--history-source ''`)
# and this is its query-parameter face. `?at=` is a 422, `?at=0` is a 200 with
# `at_seq: 0`, and an ABSENT `at` is a third thing again (`at_seq: null`) —
# `CR-full` already covers the third.
CR-at-zero        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=0 | -
# Rule 1, the integer form: sign, leading zeros, and CPython's underscores.
CR-at-plus        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%2B5 | -
CR-at-neg         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=-5 | -
CR-at-neg-zero    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=-0 | -
CR-at-leading-zero| GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=03 | -
CR-at-underscore  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3_0_3_6 | -
CR-at-under-lead  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=_5 | -
CR-at-under-trail | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=5_ | -
CR-at-under-double| GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=1__0 | -
# Rule 1's whitespace: `str::trim` is Unicode `White_Space`, so NBSP strips and
# ZERO WIDTH SPACE does not. Sent as escapes because a raw one in the request
# line is a uvicorn 400 before any of this is reached.
CR-at-spaces      | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%20%203036%20 | -
CR-at-tab         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%093036%09 | -
CR-at-nbsp        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%C2%A05 | -
CR-at-zwsp        | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=%E2%80%8B5 | -
# Rule 2, the integral-decimal form — DIV-107's headline. `?at=3036.0` was a 200
# there and a 422 here; the `!` on this row is struck.
CR-at-float       | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3036.0 | -
CR-at-float-zero  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=-0.0000 | -
CR-at-float-long  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3036.000000000000000000000 | -
# The underscore is legal in the WHOLE part and not in the fraction — the single
# fact that pins the implementation to "split, then parse the whole part with
# rule 1" rather than to any looser sponge.
CR-at-float-under | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3_0.0 | -
CR-at-frac-under  | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3.0_0 | -
# The fraction test is on DIGITS, not on a float. `1.0000000000000000001` IS
# `1.0` in an f64 and the reference answers 422 — a `parse::<f64>()` port would
# be green on every other row here and wrong on this one.
CR-at-precise     | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=1.0000000000000000001 | -
# Both sides of the dot must have digits; there is no exponent and no radix.
CR-at-dot-trail   | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=10. | -
CR-at-dot-lead    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=.0 | -
CR-at-two-dots    | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=1.0.0 | -
CR-at-exp         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=1e3 | -
CR-at-exp-float   | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=3.0e3 | -
CR-at-hex         | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=0x10 | -
CR-at-comma       | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=1,0 | -
# Rule 3, precision. `i64::MAX` itself is exact on both sides.
CR-at-i64-max     | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=9223372036854775807 | -

# ── KNOWN OPEN — DIV-107, the two `int` coercions `qs::opt_int` does not do ──
# Both rows are safe to execute (plain GETs, no writes); the `!` only softens
# the verdict. Python answers 200 for each; the port answers 422.
#   at=3036.0  pydantic accepts an INTEGRAL float string for an `int` field.
#   at=1e21-ish  Python ints are arbitrary precision; `i64` is not, and the
#                echoed `at_seq` could not be rendered even if it parsed.
# Both live in the SHARED `crate::qs` helper, not in this module, so the fix is
# the architect's to place. Measured on fastapi 0.141.1 / pydantic 2.13.4.
# `!CR-at-float` moved into the DIV-107 block above and lost its `!`.
#
# `!CR-at-bignum` keeps its `!`, and the reason CHANGED: it used to be a
# `422`-vs-`200`, i.e. the port refusing an input pydantic accepts. It is a
# `200`-vs-`200` now, and what differs is one echoed NUMBER — the port accepts
# the value and prints `9223372036854775807` where CPython prints
# `10000000000000000000000`. Closing that last inch means an
# arbitrary-precision `serde_json::Value` (the workspace `arbitrary_precision`
# feature), which changes how every number in the port is parsed and rendered.
# Same class as DIV-453 and the maintainer-accepted `--limit` clamp; recorded in
# `qs::PyInt`, which carries the exact digits so the day that ruling changes
# nothing has to be re-derived.
!CR-at-bignum     | GET | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f?at=10000000000000000000000 | -

# ── method coverage ─────────────────────────────────────────────────────────
# A known path with an unclaimed method is FastAPI's 405 JSON, not starlette's
# plain text. `crate::json::method_not_allowed` already handles this; the row
# exists because a router that mounts only `get` is one `any()` away from a
# silent 200 on a POST.
CR-post           | POST | /api/context-replay/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {}

# ── from parity/endpoint-cases-c-data.txt ─────────────────────────
# BATCH C — routes/data.py's remaining endpoints (RS-5-067..069).
#
# Merged into `endpoint-cases.txt` by the integrator; the leading
# `P-by-dir-known` row below is stripped there, because the shared file already
# selects a project earlier and the ordering is load-bearing.
#
#   DD-*  /api/dashboard-data      (RS-5-067)
#   MSG-* /api/messages            (RS-5-068)
#   MS-*  /api/messages/summary    (RS-5-069)
#
# `POST /api/refresh` (RS-5-070) has NO ROW HERE AND NEVER WILL. It re-runs the
# ingest pass — it writes to the store every other row in this file reads — and
# a `!` known-open row would still ISSUE the request (DIV-059). Its own
# procedure is `rust/REFRESH-DIFFER.md`, on a throwaway state copy.


# ── /api/dashboard-data ──────────────────────────────────────────────────────
# Both timezone signs, for the same §6b divergence-5 reason the D-stats rows
# carry them: the React callers send minutes WEST where the backend wants EAST,
# and the port must inherit that faithfully. Note this endpoint does NOT clamp
# the offset the way /api/stats does (DIV-124) — DD-tz-absurd is the row that
# proves the missing clamp is inherited rather than quietly added.
DD-default          | GET | /api/dashboard-data                | -
DD-tz               | GET | /api/dashboard-data?timezone_offset=-480 | -
DD-tz-inverted      | GET | /api/dashboard-data?timezone_offset=480 | -
DD-tz-absurd        | GET | /api/dashboard-data?timezone_offset=99999 | -
DD-provider-claude  | GET | /api/dashboard-data?provider=claude | -
DD-provider-multi   | GET | /api/dashboard-data?provider=claude&provider=codex | -
DD-provider-case    | GET | /api/dashboard-data?provider=CLAUDE | -
# The provider-excluded early return: a DIFFERENT body shape (four-key
# messages_page, empty statistics, a `filtered` flag), not an empty normal one.
DD-provider-none    | GET | /api/dashboard-data?provider=nosuchprovider | -
# An all-blank filter list leaves the filter UNSET, which is not the same as an
# empty one — this must behave like DD-default, not like DD-provider-none.
DD-provider-blank   | GET | /api/dashboard-data?provider=%20   | -
DD-model            | GET | /api/dashboard-data?model=claude-opus-4-5-20251101 | -
DD-model-none       | GET | /api/dashboard-data?model=nosuchmodel | -
DD-model-case       | GET | /api/dashboard-data?model=CLAUDE-OPUS-4-5-20251101 | -
DD-bad-int          | GET | /api/dashboard-data?timezone_offset=abc | -

# ── /api/messages ────────────────────────────────────────────────────────────
MSG-default         | GET | /api/messages                      | -
MSG-page2           | GET | /api/messages?page=2&per_page=5    | -
MSG-per-page        | GET | /api/messages?per_page=5           | -
MSG-per-page-hi     | GET | /api/messages?per_page=99999       | -
MSG-per-page-lo     | GET | /api/messages?per_page=0           | -
MSG-page-negative   | GET | /api/messages?page=-3&per_page=5   | -
MSG-page-past-end   | GET | /api/messages?page=99999&per_page=5 | -
# The legacy `?limit=` alias, and the bug in its guard: the test is against the
# DEFAULT per_page value, so an EXPLICIT `per_page=100` is indistinguishable
# from no per_page at all and `limit` wins. Both rows are here so the pair
# pins the behaviour rather than one row pinning half of it.
MSG-limit-alias     | GET | /api/messages?limit=7              | -
MSG-limit-vs-100    | GET | /api/messages?limit=7&per_page=100 | -
MSG-limit-vs-5      | GET | /api/messages?limit=7&per_page=5   | -
MSG-provider-none   | GET | /api/messages?provider=nosuchprovider | -
MSG-provider-claude | GET | /api/messages?provider=claude&per_page=5 | -
MSG-model           | GET | /api/messages?model=claude-opus-4-5-20251101&per_page=5 | -
MSG-model-none      | GET | /api/messages?model=nosuchmodel    | -
# `timezone_offset` is declared, coerced, and then never read — so a bad value
# is a 422 even though the parameter changes nothing.
MSG-tz-bad          | GET | /api/messages?timezone_offset=abc  | -

# ── /api/messages/summary ────────────────────────────────────────────────────
# No parameters at all. The mart gate decides the body SHAPE (the mart path
# adds `total_sessions`; the legacy path omits it), so this one row exercises
# whichever path the harness store is in — and the two servers must agree on
# which that is, which is itself the assertion.
MS-summary          | GET | /api/messages/summary              | -

# ── batch D (cfg/webhooks/sync/whatif/static_analysis/meta_agent) — merged from endpoint-cases-d.txt at integration ──
# BATCH D's endpoint case matrix — the unclaimed remainder.
#
# Same row format as `endpoint-cases.txt`, which is batch C's this round:
#
#   <id> | <METHOD> | <path-and-query> | <json body or ->
#
# A LEADING `!` marks a KNOWN-OPEN case: reported in full under a loud banner,
# never fatal. Unported endpoints belong HERE, in the file — "we know" beats
# "nobody looked".
#
# Run it with batch D's ports (C owns :8096/:8097; :8095 is the maintainer's
# live server and is never bound from anywhere):
#
#   STAX_ENDPOINT_CASES=rust/parity/endpoint-cases-d.txt \
#   STAX_ENDPOINT_RS_PORT=8098 STAX_ENDPOINT_PY_PORT=8099 \
#   STAX_ENDPOINT_DIFFS=rust/.parity-state/endpoint-diffs-d \
#   STAX_ENDPOINT_LOGS=rust/.parity-state/endpoint-logs-d \
#   rust/endpoint-parity.sh
#
# ── THE SIDE-EFFECT RULE, AND THE ONE THIS FILE ADDS ─────────────────────────
#
# `!` softens the VERDICT, never the REQUEST (DIV-059/078). Every row below is
# safe to execute on the shared home. Batch D found a second requirement:
#
#   A `!` row must also be able to TERMINATE.
#
# `Verdict::Error` — what the differ books when a socket read times out — is
# NOT downgraded by `!`, and `Tally::exit_code` ranks it ABOVE a divergence, at
# exit 2. An SSE body never ends, so a row for `GET /api/live/stream` would
# fail the whole run as a harness error and take every other row's verdict with
# it. It is therefore absent, and what WAS verified about it is written down in
# `rust/parity/SSE-PROBE-d.md` (DIV-136) rather than guessed at.
#
# Three more endpoints are deliberately absent for the older rule:
#   POST /api/etl/backfill          rebuilds the marts on the shared home
#   POST /api/patterns/dismiss      writes a governance file OUTSIDE the home
#   POST /api/worktrees/attribute   writes the store
# All three are recorded in their modules' doc tables and in the ledger.
#
# ── ORDER IS SIGNIFICANT ─────────────────────────────────────────────────────
#
# The `G-*` writer block MUTATES `config.json`, which both servers share. The
# differ runs Python then Rust for each case in turn, and `settings._save` is
# idempotent, so each row writes the same bytes twice. The block is ordered to
# self-clean and is LAST in the file, after every row that reads a setting or a
# price — an alias is global and `compute_cost` consults it, so an alias left
# set would silently reprice `WI-whatif-*`.
#
# `POST /api/cfg/currency`'s SUCCESS leg is not a row at any position: it
# persists a `currency` key nothing in the API can remove, and any value but
# USD walks into DIV-052. Its two 400 legs are here.
#
# Fixtures this file names, all measured on `.parity-state/fresh`:
#   37c6275a-ff92-4da8-916d-9097b6da1cc9   the ONE static_analysis_findings row
#   -media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow   claude, 2 sessions
#
# ID PREFIXES: `P-`, `Q-` and `Y-` are ALREADY TAKEN in `endpoint-cases.txt`
# (projects, qa, yield_route). An id names the dumped body under
# `.parity-state/endpoint-diffs*/`, so a collision after the architect merges
# these rows would silently overwrite one batch's evidence with another's.
# D's groups are therefore SY- / WI- / QL- / PT-, none of which appear there.
#
# Group provenance:
#   K-*  routes/cfg.py             (RS-5-050..055)
#   SY-* routes/sync.py            (RS-5-108/109)
#   WI-* routes/whatif.py         (RS-5-113)
#   SA-* routes/static_analysis.py (RS-5-107)
#   MA-* routes/meta_agent.py      (RS-5-077/078)
#   H-*  routes/webhooks.py        (RS-5-110..112)
#   E-*  routes/etl.py             (RS-5-071)       — all known-open
#   PB-* routes/playback.py        (RS-5-091..093)  — all known-open
#   LV-* routes/live.py            (RS-5-075)       — known-open
#   QL-* routes/quality.py         (RS-5-102/103)   — all known-open
#   F-*  routes/forks.py           (RS-5-074)       — all known-open
#   BM-* routes/benchmark.py       (RS-5-045/046)   — all known-open
#   PT-* routes/patterns.py        (RS-5-088)       — all known-open
#   W-*  routes/worktrees.py       (RS-5-114)       — known-open
# ── cfg: the read surface ────────────────────────────────────────────────────
# `GET /api/cfg` is 22 settings in `class Settings`'s declaration order, each
# resolved env -> file -> default. The shared home's `config.json` carries
# `auto_browser: false`, so this row also proves the FILE leg, not just defaults.
K-cfg               | GET | /api/cfg                          | -
K-currencies        | GET | /api/cfg/currencies               | -
K-aliases           | GET | /api/cfg/model-aliases            | -
# ── cfg: the validation legs (no writes reach disk on any of these) ──────────
K-cur-empty-body    | POST | /api/cfg/currency                | {}
K-cur-null          | POST | /api/cfg/currency                | {"code": null}
K-cur-blank         | POST | /api/cfg/currency                | {"code": "   "}
K-cur-not-string    | POST | /api/cfg/currency                | {"code": 3}
K-cur-bad-shape     | POST | /api/cfg/currency                | {"code": "EURO"}
K-cur-bad-short     | POST | /api/cfg/currency                | {"code": "e"}
# `code` is falsy, so `or` falls through to `currency` — and THAT is validated.
K-cur-fallthrough   | POST | /api/cfg/currency                | {"code": "", "currency": "NOPE"}
K-alias-no-from     | POST | /api/cfg/model-aliases           | {"to": "claude-opus-4-8"}
K-alias-blank-from  | POST | /api/cfg/model-aliases           | {"from": "  ", "to": "x"}
K-alias-no-to       | POST | /api/cfg/model-aliases           | {"from": "proxy/x"}
K-alias-blank-to    | POST | /api/cfg/model-aliases           | {"from": "proxy/x", "to": ""}
K-alias-del-none    | DELETE | /api/cfg/model-aliases         | -
K-alias-del-empty   | DELETE | /api/cfg/model-aliases?from=   | -
# 404 with Python's `repr()` of the key — the quoting rule is the byte contract.
K-alias-del-missing | DELETE | /api/cfg/model-aliases?from=no-such-alias | -
# ── static analysis: the ported read ─────────────────────────────────────────
# The store holds exactly ONE findings row, and it is the interesting shape: a
# `file_created_in_session` lint_count finding with a NULL `pre_value` and a
# NULL `delta`. So `avg_delta` is null, `_classify_delta` returns "unknown", and
# the metric reports `files: 1` with improved/regressed/neutral all zero — the
# case a summary written from the happy path would get wrong.
SA-session-real     | GET | /api/static-analysis/session/37c6275a-ff92-4da8-916d-9097b6da1cc9 | -
# An unanalyzed session is 200 with empty findings, NOT a 404.
SA-session-unknown  | GET | /api/static-analysis/session/no-such-session-anywhere | -
SA-session-empty    | GET | /api/static-analysis/session/    | -
# ── meta-agent: the constant ─────────────────────────────────────────────────
# 15 tool schemas, ~15 KB, byte-for-byte. This row IS the tripwire for the
# embedded catalogue drifting from `services/meta_agent.py`.
MA-tools            | GET | /api/meta-agent/tools             | -
# DIV-138: the chat endpoint is an NDJSON stream against a live LLM. Rust 404s;
# Python rejects on validation BEFORE it opens a socket, so both rows are safe.
!MA-chat-empty      | POST | /api/meta-agent/chat             | {}
!MA-chat-no-model   | POST | /api/meta-agent/chat             | {"messages": [{"role": "user", "content": "hi"}]}
# ── webhooks: the opt-in gate ────────────────────────────────────────────────
# No secret is exported into either server, so all three return 503 before the
# body is read, before it is parsed, and before anything touches the store.
# That is the whole point of the row: it proves the gate fires first.
H-github-503        | POST | /api/webhooks/github             | {"zen": "x"}
H-gitlab-503        | POST | /api/webhooks/gitlab             | {"object_kind": "ping"}
H-ci-503            | POST | /api/webhooks/ci                 | {"id": 1}
# ── sync: the default leg is deterministic; the merged one carries a clock ───
SY-overview-default  | GET | /api/sync/overview                | -
SY-overview-scope    | GET | /api/sync/overview?scope=this-device | -
# Anything that is not exactly `all-devices` takes the stub path.
SY-overview-nonsense | GET | /api/sync/overview?scope=nonsense | -
SY-overview-blank    | GET | /api/sync/overview?scope=         | -
# `?scope=` twice: starlette's QueryParams takes the LAST occurrence, so this
# is the stub leg, not the merged one.
SY-overview-last-wins | GET | /api/sync/overview?scope=all-devices&scope=this-device | -
# DIV-358 CLOSED (wave 6). `/api/sync/status` rebuilds every shard through
# `serialize.py` (227 lines) — batch D deferred it for exactly that reason, and
# `stax-sync` now owns that port with a differ of its own. What remains is the
# `scanned_at = datetime.now(UTC)` stamp, so this stays a `!` row whose diff can
# only ever be the timestamp: 537 bytes vs 537 bytes, first difference at byte
# 524. Read-only, so it is safe to execute.
!SY-status           | GET | /api/sync/status                  | -
# The merged roll-up. Every field agrees; `generated_at` cannot, and the diff
# offset in the report is the evidence that everything before it did.
!SY-overview-all     | GET | /api/sync/overview?scope=all-devices | -
# ── whatif: the pricing-seam flagship ────────────────────────────────────────
# Whole-store scope (no project selected yet in this file), so these reprice the
# entire token volume against 10 candidates. A 2% rate difference between the
# manifest and the primed `price_book` shows up here first (DIV-056).
WI-whatif-all        | GET | /api/whatif                       | -
WI-whatif-project    | GET | /api/whatif?log_path=/x/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
WI-whatif-unknown    | GET | /api/whatif?log_path=/x/-no-such-project-anywhere | -
# An empty `log_path` is falsy and falls back to the (unset) current project.
WI-whatif-blank      | GET | /api/whatif?log_path=             | -
# ── batch E: the deferred remainder, ported ──────────────────────────────────
#
# Wave 5 batch E closed the eight large deferred services plus quality, the
# ollama proxy and sessions/compare. Each member's rows are folded in below with
# its own commentary; the per-member sources are `endpoint-cases-e-*.txt`.
#
# The `P-by-dir-known` selection row each member led with is STRIPPED here: it
# already ran near the top of this file and the selection still stands.
#
# A remaining `!` in this block is an HONEST RECORDED IMPOSSIBILITY, not an
# unported endpoint — each one names the field that makes it so (a wall clock,
# a `scanned_at`, a hash-randomised set order). The endpoint behind it IS
# ported and is proven by unit tests and by the member's own off-matrix probe.

# ══ member `etl` ═════════════════════════════════════════════════════════════
# BATCH E — routes/etl.py (RS-5-071, RS-5-072). DIV-139 cleared.
#
# Merged into `endpoint-cases.txt` by the integrator; the leading
# `P-by-dir-known` row below is stripped there, because the shared file already
# selects a project earlier and the ordering is load-bearing. (Neither endpoint
# in this file reads `deps.current_log_path`; the row is here so the sidecar can
# be run standalone with the same server state the shared file establishes.)
#
#   E-etl-*   GET  /api/etl/status    (RS-5-071)
#   E-405-*   the unclaimed methods on both paths, and the bare prefix
#
# ── `POST /api/etl/backfill` HAS NO ROW HERE AND NEVER WILL ──────────────────
#
# It is a writer. `force=true` is `DELETE FROM usage_events` + `DELETE FROM
# mart_watermark` + `rebuild_from_scratch` on all eight marts + a full
# `messages → usage_events` normalize pass; `force=false` still re-streams every
# message and re-stamps every watermark. A `!` known-open row would still ISSUE
# the request (DIV-059) and the write would land on the SHARED harness home
# while it is being diffed — DIV-078, at mart scale. Its `409` leg additionally
# needs a job already in flight on BOTH servers at once, which a linear case
# file cannot express.
#
# Its verification is `rust/ETL-BACKFILL-DIFFER.md`: two separate scratch state
# copies, ports :8098/:8099, and its own seeds. RUN 2026-07-31 — every leg green
# (five byte-identical 422s, identical stores after both the incremental and the
# force run, 202→409 on both sides). Log:
# `rust/.parity-state/etl-backfill/REPORT.txt`.
#
# The 422 legs are not here either, for the same reason the refresh differ keeps
# its own: they are safe to issue (FastAPI validates before the handler), but a
# port bug that failed to reject `[]` would turn the "safe" row into a live mart
# rebuild. The isolated procedure is where a validation probe for a writer
# belongs.


# ── GET /api/etl/status — RS-5-071 ───────────────────────────────────────────
#
# STAYS KNOWN-OPEN, and the reason is NOT the port. Measured 2026-07-31 by
# `ETL-BACKFILL-DIFFER.md` §4: the two bodies differ in exactly one leaf,
# `watcher.enabled` — `false` on Python, `true` on Rust — and in nothing else.
#
# `_watcher_env_disabled()` reads `STACKUNDERFLOW_DISABLE_WATCHER` from the
# process environment on every request. `parity/pyserver.py` sets it with
# `os.environ.setdefault(...)` at import, which puts it in the PYTHON
# interpreter only; `endpoint-parity.sh` exports just `STACKUNDERFLOW_HOME` into
# the Rust subshell, so the Rust server never sees the variable and correctly
# reports the watcher as enabled. Both sides are faithful; the harness is
# asymmetric.
#
# Re-measured with `STACKUNDERFLOW_DISABLE_WATCHER=1` in the Rust environment
# too: the `watcher` block is byte-identical. The fix is one word in
# `endpoint-parity.sh`'s rust subshell —
#     export STACKUNDERFLOW_HOME="$HOME_DIR" STACKUNDERFLOW_DISABLE_WATCHER=1
# — and it belongs to the harness owner, not to this batch (the claim forbids
# editing it). Flip this row to `E-etl-status` in the same commit as that
# export; nothing else about the payload blocks it.
#
# Everything else in the body is deterministic against a shared store:
# `marts[*].last_refresh_ts` is READ from `mart_watermark` (a stored string, not
# a reading), `lag_seconds` is an event COUNT despite the name, `coverage` is an
# anti-join, and `current_job` / `last_job` are `null` unless a backfill ran in
# that process inside the last 30 s — which, with no backfill row in this file,
# never happens.
#
# The endpoint is a pure read on a current store. Python's handler calls
# `schema.apply(conn)`, which on an up-to-date store is a single
# `PRAGMA user_version` and writes nothing; the Rust port does not run
# migrations at all (RS-0-025).
#
# GREEN as of the batch-E fold-in. This row was `!` for exactly one reason —
# `watcher.enabled` was python `false` / rust `true` because `parity/pyserver.py`
# set `STACKUNDERFLOW_DISABLE_WATCHER` inside the Python interpreter only
# (DIV-153). `endpoint-parity.sh` now exports it into the Rust subshell too, the
# `watcher` block is byte-identical, and the marker is gone so that a future
# break here FAILS the gate instead of being reported and forgiven.
E-etl-status        | GET | /api/etl/status                    | -

# A second, back-to-back read. The payload must not move between two calls a
# few milliseconds apart — this is the row that would catch a `scanned_at`, a
# "seconds since", or a per-request clock reading sneaking into a STATUS
# surface (the DIV-073 / DIV-085 class). It carried the same `watcher.enabled`
# divergence and no other; DIV-153 closed it, so this row is green and load-
# bearing — it is the one that proves a STATUS payload holds still.
E-etl-status-again  | GET | /api/etl/status                    | -

# ── the unclaimed methods, and the bare prefix ───────────────────────────────
#
# FastAPI answers a known path with an unclaimed method as `405
# {"detail":"Method Not Allowed"}` (starlette's router raises, FastAPI's handler
# renders it as JSON); an unknown path is `404 {"detail":"Not Found"}`. axum's
# own defaults are an EMPTY body with no content-type, so these rows are what
# proves `lib.rs`'s two fallbacks still cover the two paths this module adds.
# None of them reaches a handler, so none of them writes.
E-405-status-post   | POST   | /api/etl/status                 | {}
E-405-status-del    | DELETE | /api/etl/status                 | -
E-405-backfill-get  | GET    | /api/etl/backfill               | -
E-405-backfill-put  | PUT    | /api/etl/backfill               | {}
E-404-etl-prefix    | GET    | /api/etl                        | -
E-404-etl-slash     | GET    | /api/etl/                       | -
E-404-etl-unknown   | GET    | /api/etl/nosuchthing            | -

# ══ member `playback` ═════════════════════════════════════════════════════════════
# Batch E / member `playback` — case rows for `routes/playback.py`
# (RS-5-091 `/api/playback/{session_id}/fs`, RS-5-092 `/api/playback/{session_id}`,
#  RS-5-093 `/api/playback/project/{project_slug}`).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file, replacing the five `!PB-*` rows
# that live there today, and strips the leading `P-by-dir-known` selection row.
#
# ── SAFETY (law 4) ───────────────────────────────────────────────────────────
#
# Every row is a GET or an unclaimed method on a GET-only path. All three
# endpoints are pure read-side over `messages.raw_json`, `sessions`, `projects`
# and (when present) `captured_events`; the only write in the Python handlers is
# `schema.apply(conn)`, idempotent DDL the harness store already satisfies, and
# the port does not run it at all (DIV-106). Nothing under
# `$STACKUNDERFLOW_HOME` is written by any row below.
#
# ── TERMINATION (law 4, second half) ─────────────────────────────────────────
#
# The `/fs` endpoint decorates each reconstructed file with a risk overlay, and
# that overlay is O(files × four `LIKE` sweeps of `messages`). Measured on the
# harness store (3.9 GB, `messages` a partitioned VIEW): ~0.6 s per sweep, so
# ~2.5 s per file. The fixtures below are chosen so no row reconstructs more
# than two files — see `PB-fs-replay`'s comment. A row against a
# hundred-file session would be minutes and does not belong in a gate.
#
# ── NO ROW DEPENDS ON SERVER STATE ───────────────────────────────────────────
#
# Unlike most of the matrix, these three endpoints never read
# `deps.current_log_path`: the session id and the project slug both arrive in
# the path. The selection row is carried anyway, per batch C's convention, so
# these rows behave identically whether they are run standalone or spliced into
# the middle of the merged file.
#
# ── THE FIXTURES ─────────────────────────────────────────────────────────────
#
#   04a86c63-4cfd-4929-91a3-3edfe80e3e2f  sessions.id 3488, project 314 (this
#       repo). THREE messages, all role=user, so the event stream and the fs
#       snapshot are both EMPTY at every cutoff — which is what makes it the
#       cheap, always-terminating fixture for the required rows.
#   4f6cb3a7-a786-4fe2-8c40-66c5ec02d933  sessions.id 20, 22 messages, 12 of
#       them assistant rows carrying `tool_use` blocks, TWO of which name a
#       `file_path`. Ingested 2026-03-13 from a project nothing in this
#       campaign writes to, so it is stable. This is the fixture that actually
#       exercises the extractor, the excerpt builder and the fs replay.
#   -media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow  projects.id 314,
#       two sessions, 1,279 messages. One provider, so `get_project`'s
#       first-row-wins narrowing is not observable here (see DIV finding 8).

# ── the state row this file needs first (integrator strips it) ───────────────

# ── RS-5-092  GET /api/playback/{session_id} ─────────────────────────────────
# The two rows the claim named. `PB-session` is the 404 leg; `PB-session-real`
# is a session that EXISTS and has no tool calls — 200 with an empty list, the
# distinction the whole `page is None` return type exists to make.
PB-session          | GET | /api/playback/no-such-session | -
PB-session-real     | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -

# The real extractor: tool-call pairing, the summary table, `byte_count` in
# BYTES, `duration_ms` from the result message's timestamp, and the 200-CODE-
# POINT `payload_excerpt` (which defaults ON for this endpoint).
PB-session-tools    | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933 | -
# `include_payload=0` must blank the excerpt and change nothing else.
PB-session-nopayload | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?include_payload=0 | -
# A filter subsets the list while each event KEEPS its `seq` from the full
# stream — the gap-preserving property a deep link depends on.
PB-session-filter   | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?tool_filter=Read,Edit | -
# A filter naming a tool the session never called: empty, not 404.
PB-session-filter-miss | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?tool_filter=Frobnicate | -
# `_parse_tool_filter` drops blank parts and then `cleaned or None` — so a
# filter of nothing but whitespace is NO filter, and every event survives.
PB-session-filter-blank | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?tool_filter=%20,%20, | -
# `truncated: true`, and the cap applies to the RETURNED list, not the scan.
PB-session-limit1   | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=1 | -
# Percent-decoding of the path parameter plus `ensure_ascii=False` on the way
# out: the 404 detail must ship the raw UTF-8 bytes of `café`.
PB-session-utf8     | GET | /api/playback/caf%C3%A9 | -
# An unclaimed method on a claimed path — FastAPI's `{"detail":"Method Not
# Allowed"}`, and no handler runs.
PB-session-post     | POST | /api/playback/no-such-session | -

# ── RS-5-092 validation legs (law 5: one row per branch) ─────────────────────
# `limit: int = Query(1000, ge=1, le=10_000)`. These three are the first rows in
# the whole matrix to issue `greater_than_equal` / `less_than_equal`, which
# carry a `ctx` object the type errors do not.
PB-session-limit-ge | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=0 | -
PB-session-limit-le | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=10001 | -
PB-session-limit-bad | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=abc | -
# pydantic's lax `str`→`int` accepts an integral float form and CPython's digit
# underscores; a big integer parses EXACTLY and then fails the bound, so this
# must be `less_than_equal` and not `int_parsing`. The `qs::opt_int` DIV-107
# defect is why this route coerces locally — finding 1.
PB-session-limit-float | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=100.0 | -
PB-session-limit-under | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=1_000 | -
PB-session-limit-big | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?limit=99999999999999999999 | -
# `include_payload: bool` — pydantic's boolean vocabulary, and what is outside it.
PB-session-bad-bool | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?include_payload=maybe | -
# TWO bad parameters: both are reported, in DECLARATION order (`limit` then
# `include_payload`), not in query-string order.
PB-session-two-errors | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933?include_payload=maybe&limit=0 | -

# ── RS-5-093  GET /api/playback/project/{project_slug} ───────────────────────
PB-project          | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
PB-project-unknown  | GET | /api/playback/project/-no-such-project-anywhere | -
# `include_payload` defaults OFF here — this row turns it on over a small window
# so the excerpt builder is measured on the cross-session path too.
PB-project-payload  | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?include_payload=1&limit=5 | -
PB-project-filter   | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?tool_filter=Bash&limit=50 | -
# `since` — an ISO literal above every stamp, so the stream is empty.
PB-project-since-iso | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?since=2027-01-01 | -
# The RELATIVE grammar (`^\s*(\d+)\s*([dhm])\s*$`). 9,999 days lands the bound
# in 1999, so the millisecond gap between the two servers' `datetime.now(UTC)`
# calls cannot move a row. A `?since=7d` row would be a coin flip on any
# message written in that gap and is deliberately NOT here — finding 2.
PB-project-since-rel | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?since=9999d | -
# The regex allows internal whitespace and is case-insensitive.
PB-project-since-rel2 | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?since=%20999999%20H%20 | -
# Unrecognised → passed through as a LITERAL, and `'7 days' >= '2026-…'`
# lexically, so the SQL matches nothing. Harmless by design, and measurable.
PB-project-since-junk | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?since=7%20days | -
# Blank `since` is `None`, NOT an empty lower bound — no clause is added at all.
PB-project-since-blank | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?since= | -
# The project endpoint's bounds are a DIFFERENT pair (`ge=1, le=20_000`) and the
# `le` message carries `20000`, not `10000`.
PB-project-limit-ge | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?limit=0 | -
PB-project-limit-le | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?limit=20001 | -
PB-project-limit1   | GET | /api/playback/project/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow?limit=1 | -

# ── RS-5-091  GET /api/playback/{session_id}/fs ──────────────────────────────
# `at: str = Query(...)` — absent is FastAPI's `missing`, with a null `input`.
PB-fs-no-at         | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs | -
# A session with no assistant rows: 200, `files: {}`, `warnings: []`, and the
# `snapshot_ts` echo. No file means no risk sweep, so this row is instant.
PB-fs-empty         | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?at=2026-07-30T00:00:00Z | -
# The full replay: Read seeds through the `cat -n` stripper, Edit substitutes,
# `operations_applied` counts per tool per file, and the risk overlay runs. Two
# files (~5 s of `LIKE` sweeps on both servers) — the ceiling this file accepts.
PB-fs-replay        | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2026-03-14T00:00:00Z | -
# The CUTOFF, and it is placed BETWEEN the session's two file-touching calls
# (14:38:26.965 and 14:38:49.636), so this row returns ONE file where
# `PB-fs-replay` returns two. A cutoff that happens to fall after both — the
# obvious mid-session guess — measures nothing, and did on the first attempt.
PB-fs-cutoff        | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2026-03-13T14:38:30Z | -
# `include_content=0` drops only the bodies; `byte_count` still measures the
# body that was NOT shipped.
PB-fs-no-content    | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2026-03-14T00:00:00Z&include_content=0 | -
# A cutoff before the session began: empty, not an error.
PB-fs-early         | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2020-01-01T00:00:00Z | -
# `paths` restricts the REPLAY, not just the output; a path the session never
# touched yields `files: {}` (and therefore no risk sweep).
PB-fs-paths-miss    | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2026-03-14T00:00:00Z&paths=/nope,%20, | -
# A naive cutoff (no offset) against aware message stamps — the third row of
# `_ts_le`'s four-case table, where CPython compares WALL clocks and discards
# the offset.
PB-fs-naive-at      | GET | /api/playback/4f6cb3a7-a786-4fe2-8c40-66c5ec02d933/fs?at=2026-03-14T00:00:00 | -
# 404 — session not in store. Note the cutoff is parsed BEFORE the session is
# resolved, so this row needs a VALID `at` to reach the 404 at all.
PB-fs-404           | GET | /api/playback/no-such-session/fs?at=2026-01-01T00:00:00Z | -
# 422 — `at` present and unparseable. This is `HTTPException(422, detail=str(e))`,
# a one-key object with a STRING detail, and the message embeds Python's
# `repr()` of the raw value. NOT the pydantic list the rows above produce.
PB-fs-bad-at        | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?at=nope | -
# An EMPTY `at` is present-but-unparseable, so it takes the string 422 and not
# the `missing` one — the distinction is `Query(...)` seeing a value at all.
PB-fs-blank-at      | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?at= | -
# `include_content: bool` — the third field in the signature.
PB-fs-bad-bool      | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?at=2026-07-30T00:00:00Z&include_content=maybe | -
# `missing` AND `bool_parsing` together, `at` first: declaration order again,
# and proof the dependency solver runs before the handler.
PB-fs-two-errors    | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?include_content=maybe | -
# A repeated scalar takes the LAST occurrence (starlette's `QueryParams._dict`).
PB-fs-repeated-at   | GET | /api/playback/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/fs?at=nope&at=2026-07-30T00:00:00Z | -

# ── router precedence: the three paths overlap, and the overlap is measurable ─
# `/api/playback/project/fs` is matched by BOTH `/api/playback/{session_id}/fs`
# and `/api/playback/project/{project_slug}`. starlette takes the first
# REGISTERED (the fs route, `session_id="project"`); axum's trie prefers the
# static `project` segment and would take the other. `routes/playback.rs` carries
# an explicit shadow check to reconcile them — finding 3.
PB-project-named-fs | GET | /api/playback/project/fs | -
PB-project-named-fs-at | GET | /api/playback/project/fs?at=2026-01-01T00:00:00Z | -
# `/api/playback/project` with no slug is the SESSION route, id `"project"`.
PB-project-bare     | GET | /api/playback/project | -
# Four segments match no route on either side — FastAPI's JSON 404, not
# starlette's plain text.
PB-fs-four-segments | GET | /api/playback/project/x/fs?at=2026-01-01T00:00:00Z | -

# ══ member `live` ═════════════════════════════════════════════════════════════
# Batch E / member `live` — case rows for `routes/live.py` (RS-5-075/076).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file, REPLACING the two existing
# `!LV-*` rows, and strips the leading `P-by-dir-known` selection row, which
# already runs earlier there.
#
# ── the honest headline ─────────────────────────────────────────────────────
#
# `/api/live/stats` IS PORTED AND MOUNTED, AND ITS ROWS STAY `!`. The payload
# stamps `burn.ts = datetime.now(UTC).isoformat()` at microsecond resolution,
# so no two responses byte-match — not python-vs-rust, and not python-vs-python
# on ONE server two seconds apart. Measured both ways before a line was ported;
# `DIV-e-live.md` carries the diffs. `!` here means "clock-stamped, permanently
# open by construction", the DIV-085 disposition, not "unported".
#
# What the `!` rows still buy is real: the differ executes them, so a 500, a
# panic, a schema drift or a shape change on either side surfaces in the dump
# under `.parity-state/endpoint-diffs/` even though the verdict is suppressed.
# The *content* proof is in `DIV-e-live.md`: five offsets, python and rust
# rendered from the same read-only store, byte-identical on every field except
# `.burn.ts`.
#
# `/api/live/stream` HAS NO ROW, EVER — DIV-136, restated in `SSE-PROBE-d.md`.
# `!` suppresses `Verdict::Divergent`, never `Verdict::Error`, and a body that
# never terminates books two harness errors and exits 2. Its verification is
# the recorded probe, now with both halves.
#
# Side effects: NONE. Every row is a GET or an unclaimed method on
# `/api/live/*`. `_live_stats_sync` opens the store read-write (that is what
# `store/db.py::connect` does) and Python additionally runs `schema.apply` —
# idempotent DDL the harness store already satisfies — but the handler issues
# only SELECTs. Nothing under `$STACKUNDERFLOW_HOME` is written.

# ── the state row this file needs first (integrator strips it) ───────────────

# ── the happy path and the timezone legs ────────────────────────────────────
# DIV-141 / finding 1: `burn.ts` is a clock stamp, so these can never go green.
# Everything else in the body is deterministic on a frozen store — six tools at
# 61/15/9/5/4/3 samples, both watermarks, and every burn figure but `ts`.
!LV-stats            | GET | /api/live/stats                        | -
# `-480` is minutes EAST of UTC and §6b divergence 5 (the React sign inversion)
# is INHERITED FAITHFULLY, exactly as `D-stats-tz` / `D-stats-tz-inverted` do
# for `/api/stats`. Both signs are ported, neither is "fixed": `-480` puts the
# local wall clock eight hours BEHIND UTC and `+480` eight hours ahead, which
# on the harness home moves `month_to_date` from 3857.83 to 0.0. The sign is
# load-bearing and both legs are measured.
!LV-stats-tz         | GET | /api/live/stats?timezone_offset=-480   | -
!LV-stats-tz-inverted| GET | /api/live/stats?timezone_offset=480    | -
# Zero is the default, spelled explicitly: it must take the same leg as an
# ABSENT parameter, not a different one.
!LV-stats-tz-zero    | GET | /api/live/stats?timezone_offset=0      | -

# ── the clamp that is NOT there (finding 2) ─────────────────────────────────
# `/api/stats` clamps `timezone_offset` to [-720, 840] inside
# `_project_stats_cached`. `routes/live.py` hands the raw int straight to
# `live_svc.snapshot`, so these two answer 200 with genuinely different buckets
# — probed: `-100000` gives month_to_date 1736.63 / projection 2340.67, and
# `+100000` gives 550.45 / 1895.99, neither of which is the `[-720, 840]`
# answer. Inheriting the missing clamp is the DIV-124 call for
# `/api/dashboard-data`, made again here. These rows exist so that if anyone
# ever "fixes" one side, the bodies stop matching in a way the dump shows.
!LV-stats-tz-under   | GET | /api/live/stats?timezone_offset=-100000 | -
!LV-stats-tz-over    | GET | /api/live/stats?timezone_offset=100000  | -

# ── the 500 the missing clamp makes reachable (finding 3) ───────────────────
# Not a validation error: the value coerces fine and then
# `now + timedelta(minutes=tz)` leaves `datetime`'s [year 1, year 9999] range.
# `OverflowError: date value out of range` at `services/live.py:203`, uncaught,
# so starlette's ServerErrorMiddleware answers 500 with the 21-byte plain-text
# body `Internal Server Error` and `content-type: text/plain; charset=utf-8`.
# NO `!`: the port reproduces that response byte for byte and the row is
# expected to go GREEN. It is also the only non-JSON response in the module.
#
# The asymmetry is real, not a typo. 2026 leaves ~7973 years of headroom
# forward and only ~2026 backward, so `+2147483647` is a 200 and
# `-2147483648` is a 500. Both are rows precisely because the boundary is
# counter-intuitive.
LV-stats-tz-overflow | GET | /api/live/stats?timezone_offset=-2147483648 | -
!LV-stats-tz-int32max| GET | /api/live/stats?timezone_offset=2147483647  | -

# ── the 422 legs (law 5, law 8) ─────────────────────────────────────────────
# `timezone_offset: int = 0` is coerced by FastAPI's dependency solver, so an
# uncoercible value never reaches the handler and the body is the FULL pydantic
# error LIST — NOT `json::validation_422_field_only`'s `{"detail":"<field>"}`.
# Measured, not assumed (law 6):
#   {"detail":[{"type":"int_parsing","loc":["query","timezone_offset"],
#     "msg":"Input should be a valid integer, unable to parse string as an
#     integer","input":"abc"}]}
# Expected GREEN — the port uses `json::validation_422`.
LV-stats-bad-int     | GET | /api/live/stats?timezone_offset=abc     | -
# An EMPTY value is NOT "absent": it is the empty string, and it fails to
# coerce with `"input":""`. This is the leg a port that reads "missing or
# empty -> default" would silently pass with a 200.
LV-stats-empty-int   | GET | /api/live/stats?timezone_offset=        | -
# pydantic will not narrow a float literal to an `int` field, even an integral
# one. `1.5` and `1.0` both 422.
LV-stats-float-int   | GET | /api/live/stats?timezone_offset=1.5     | -
# A bignum: Python's `int` is arbitrary-precision, so this COERCES and then
# 500s on the timedelta; `crate::qs::opt_int` parsed into `i64` and 422'd.
#
# CLOSED by the DIV-107 rewrite, and this row is the payoff for having named it
# here instead of leaving it to be rediscovered: `opt_int` saturates now rather
# than refusing, and `/api/live/stats` reaches the same overflow the reference
# does. `!` struck — the gate defends it.
LV-stats-bignum-int  | GET | /api/live/stats?timezone_offset=9999999999999999999999 | -

# ── starlette's routers (law 5) ─────────────────────────────────────────────
# A repeated scalar takes the LAST occurrence — starlette builds
# `QueryParams._dict` by comprehension. The first row must therefore be a 422
# (the junk value wins) and the second a 200 (the good value wins). A port that
# took the FIRST value would pass one row and fail the other, which is why both
# are here.
LV-stats-tz-repeat-bad  | GET | /api/live/stats?timezone_offset=-480&timezone_offset=abc  | -
!LV-stats-tz-repeat-good| GET | /api/live/stats?timezone_offset=abc&timezone_offset=-480  | -
# An unknown query parameter is IGNORED, not rejected — FastAPI does not run
# an extra-forbid model on query params.
!LV-stats-unknown-param | GET | /api/live/stats?bogus=1                | -
# The unclaimed methods. FastAPI's own handler renders
# `{"detail":"Method Not Allowed"}`; starlette's default plain text is
# replaced. Expected GREEN.
LV-stats-post        | POST   | /api/live/stats                    | -
LV-stats-put         | PUT    | /api/live/stats                    | -
LV-stats-delete      | DELETE | /api/live/stats                    | -
LV-stats-patch       | PATCH  | /api/live/stats                    | -
# A path under the prefix that no route claims: FastAPI's `{"detail":"Not
# Found"}`, not axum's empty 404. Expected GREEN.
LV-unknown-subpath   | GET | /api/live/nope                         | -

# ── the two trailing-slash rows, and why they are a PAIR ────────────────────
# starlette's `redirect_slashes` answers `307` + `location:` for a trailing
# slash on a REGISTERED path, and `404` for one that is not registered —
# measured: `/api/live/stats/` and `/api/live/stream/` both 307, while
# `/api/live/nope/` 404s. axum does neither redirect, so both 404 today.
#
# `LV-stats-slash` is therefore the pure DIV-133 control: a path mounted on
# BOTH servers, diverging only on the redirect. `LV-stream-slash` was the same
# 307-vs-404 shape, and batch E predicted that when DIV-133 landed this one
# would STAY 404 and become standing evidence that `/api/live/stream` is
# unmounted here (DIV-e-live.md finding 6).
#
# **The prediction is void, and it is void for a good reason: the premise
# expired.** DIV-320 mounted `/api/live/stream` (closing DIV-165) two batches
# after that note was written, so both rows redirect now and both are green.
# The note is kept rather than deleted because it is the useful shape of the
# mistake — a row whose MEANING depends on another row's state has to be re-read
# when that state moves, and nothing re-reads it automatically. The differ is
# what caught it: the row flipped to a candidate and the comment did not.
#
# A row on `/api/live/stream` ITSELF would still hang the differ and exit 2
# (DIV-136); the trailing slash is answered before the handler, which is exactly
# why this is the only safe form of the observation.
LV-stats-slash       | GET | /api/live/stats/                       | -
LV-stream-slash      | GET | /api/live/stream/                      | -

# ══ member `quality` ═════════════════════════════════════════════════════════════
# Batch E / quality — `routes/quality.py` (RS-5-102 / RS-5-103), closing DIV-135.
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file and strips the leading
# `P-by-dir-known` row, which already runs earlier there.
#
# ORDER IS NOT LOAD-BEARING for this group. Neither endpoint reads
# `deps.current_log_path` or any other server state; the selection row is here
# only for the batch-C convention. The rows are still listed 404s-first so that
# a failure in the cheap deterministic legs is visible before the slow ones run.
#
# ─────────────────────────────────────────────────────────────────────────────
# SAFETY — READ BEFORE ADDING ANY ROW ON A REAL SESSION ID
# ─────────────────────────────────────────────────────────────────────────────
# `grade_session` calls a local Ollama: `GET localhost:11434/api/tags` (3 s) then
# `POST /api/chat` (30 s). On THIS host :11434 is CLOSED — re-verified at probe
# time by `ss -ltn` (no listener), `curl` (exit 7, "Connection refused") and a
# direct `socket.create_connection` to 127.0.0.1 / ::1 / localhost (all ECONNREFUSED
# 111). Both sides therefore take the connect-refused path, and grading.py:205
# puts the `INSERT OR REPLACE` inside `if not is_fallback:` — so the fallback
# path WRITES NOTHING AND COMMITS NOTHING. Verified end to end: after ~20 grader
# hits per side across the probe, `SELECT COUNT(*) FROM session_quality_metrics`
# was still 0.
#
# IF :11434 IS EVER OPEN ON THE HOST RUNNING THIS MATRIX, DELETE THE
# `!QL-quality-real` ROW BEFORE RUNNING IT. With Ollama up the endpoint becomes
# (a) nondeterministic — a sampled grade — and (b) a real writer whose row the
# NEXT request serves from cache, so python-then-rust on one shared home stops
# being idempotent and the shared `.parity-state/fresh` store acquires a
# fabricated grade. Check first:  ss -ltn | grep 11434
#
# NOT IN THIS FILE, deliberately, so that "absent" never means "nobody looked":
#   * POST …/{real}/grade — `force=True`. While :11434 is closed it adds ZERO
#     coverage over the GET (both take the identical fallback path; measured,
#     byte for byte) and strictly more risk if the port ever opens. The probe
#     ran it on two real sessions and recorded the bodies in DIV-e-quality.md.
#   * The trailing-slash leg (`…/quality/`): Python answers 307, Rust 404. That
#     is DIV-133, the architect's `lib.rs` redirect_slashes item, explicitly out
#     of batch E's charter. Measured and recorded, no row.
#   * A stored-grade row. `get_stored_grade` is the ONLY fully deterministic
#     200 this module has — one indexed SELECT, no clock, no socket — and it has
#     no row because `session_quality_metrics` holds **0 rows** in
#     `.parity-state/fresh`. Only real LLM grades are ever persisted and nothing
#     has graded that snapshot. The day a row exists, add it: it is a free green.
#
# COST: the `!QL-quality-real` row is the most expensive row in the matrix.
# Measured 1.5–3.0 s (Python) and 3.9–8.0 s (Rust) per request, against 4–6 ms
# for every 404/405 row below. The cause is Python's own SQL, identical on both
# sides: the transcript joins the `messages` VIEW, a 16-partition UNION ALL that
# SQLite MATERIALIZES in full and then auto-indexes, to fetch 3 rows. Well inside
# the differ's 300 s timeout, so it terminates (DIV-136) — but it is ~10 s of
# wall clock and one line to drop if the matrix budget says no.


# ── the 404: the leg that fires BEFORE the grader, on both endpoints ─────────
# `SELECT id FROM sessions` misses -> `HTTPException(404, f"Session {sid} not
# found")`. Fully deterministic, no socket, no clock. These are the two rows the
# claim file listed as known-open; both are GREEN now.
QL-quality-missing  | GET  | /api/static-analysis/session/no-such-session-anywhere/quality | -
QL-grade-missing    | POST | /api/static-analysis/session/no-such-session-anywhere/grade   | -
# No query parameter is declared on either endpoint, so a stray one is ignored
# rather than 422'd — the one shape a `qs`-parsing port could have got wrong.
QL-quality-query    | GET  | /api/static-analysis/session/no-such-session-anywhere/quality?force=1 | -

# ── the detail string is an f-string, and the body is ensure_ascii=False ─────
# starlette percent-decodes the path param, the f-string interpolates it raw,
# and `JSONResponse` writes UTF-8 rather than `é` (law 1). 44 bytes, not 50.
QL-quality-unicode  | GET  | /api/static-analysis/session/caf%C3%A9%20session/quality | -
QL-grade-unicode    | POST | /api/static-analysis/session/caf%C3%A9%20session/grade   | -
# `+` is NOT a space in a path (only in a query string). Both sides agree.
QL-quality-plus     | GET  | /api/static-analysis/session/a+b/quality | -

# ── the unclaimed methods on both paths ─────────────────────────────────────
# FastAPI's JSON 405, not starlette's plain text. `quality` is GET-only and
# `grade` is POST-only, so each path answers 405 for the other's verb.
QL-quality-method   | POST   | /api/static-analysis/session/no-such-session-anywhere/quality | -
QL-grade-method     | GET    | /api/static-analysis/session/no-such-session-anywhere/grade   | -
QL-quality-delete   | DELETE | /api/static-analysis/session/no-such-session-anywhere/quality | -

# ── KNOWN-OPEN: two shared-router seams, measured here, owned elsewhere ──────
# NOT this module's handlers — both are pre-existing and reproduce on routes
# batches A, C and D already shipped green. Recorded as rows so the gap is in
# the file rather than missing from it; the fix is one change in `lib.rs`, which
# batch E may not make.
#
# An EMPTY non-terminal path segment. starlette compiles `{session_id}` to
# `[^/]+`, which needs at least one character, so Python never routes it and
# answers `{"detail":"Not Found"}`. axum's matchit DOES match the empty segment
# and hands the handler `""`, which the f-string renders with two spaces:
#   py {"detail":"Not Found"}      rs {"detail":"Session  not found"}
# A TERMINAL param is unaffected — `/api/static-analysis/session/` and
# `/api/context-replay/` both answer 404 identically on both sides — so this
# only surfaces where a param is followed by another segment.
!QL-quality-empty-id | GET  | /api/static-analysis/session//quality | -
!QL-grade-empty-id   | POST | /api/static-analysis/session//grade   | -
#
# A percent-encoded SLASH. uvicorn unquotes the raw path into `scope["path"]`
# BEFORE starlette routes it, so `a%2Fb` is two segments and nothing matches;
# hyper routed on the raw path and axum's `Path` decoded AFTER matching, so the
# handler got `a/b`:
#   py {"detail":"Not Found"}      rs {"detail":"Session a/b not found"}
#
# CLOSED — DIV-168, `crate::path_semantics`. This row is the NON-terminal case
# of the class (the escape is in the middle of the path, not at the end), which
# the three rows DIV-168 itself named are not; keeping it green is what stops a
# fix that only handles a trailing parameter.
QL-quality-encoded-slash | GET | /api/static-analysis/session/a%2Fb/quality | -

# ── KNOWN-OPEN: the one field that keeps a real-session 200 open ─────────────
# `graded_at` is `datetime.now(UTC)` (grading.py:200) and it is IN THE BODY, so
# two processes can never agree. Everything else does: the isolated two-server
# probe (python :8099 / rust :8098) found both bodies 368 bytes, same status,
# same `content-type`, same key order, and the FIRST DIFFERING BYTE AT OFFSET
# 331 OF 368 — inside the `graded_at` value and nowhere else. That probe, with
# its full diff, is the evidence this row cannot itself produce; it is written
# out in `parity/DIV-e-quality.md`.
#
# Read the SAFETY block at the top of this file before running this row.
# 04a86c63… is 3 messages, which is why it is this session and not a larger one.
!QL-quality-real    | GET  | /api/static-analysis/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f/quality | -

# ══ member `forks` ═════════════════════════════════════════════════════════════
# Batch E / `forks` — endpoint differ cases for `routes/forks.py` (RS-5-074).
#
# Format is `rust/parity/endpoint-cases.txt`'s; read that file's header first.
# The integrator merges these rows into the shared file and strips the leading
# `P-by-dir-known`, which already runs earlier there.
#
# `F-forks`, `F-forks-week` and `F-forks-bad-period` are the three ids the
# shared file currently carries with a leading `!` under DIV-142. They are
# reproduced here verbatim WITHOUT the `!`, because the endpoint is ported —
# flipping them is the point of this member. Every other row is new.
#
# FOR THE INTEGRATOR: if a neighbouring member's block leaves a DIFFERENT
# project selected by the time this block runs, these rows still diff cleanly —
# both servers read the same state in the same order, so they simply scope to
# whatever project is current. The only rows that assume a SPECIFIC project are
# `F-forks-log-path` (which names the slug itself) and the two `no-project`
# rows (which name a slug that exists nowhere).
#
# ── I CHECKED FOR A WALL-CLOCK STAMP FIRST, AND THERE IS NONE ────────────────
#
# The payload is `{period, scope, report, currency, warning}`. `scope` is
# `Scope.label` — a PHRASE ("today", "last 7 days", "this month (July 2026)",
# "all time"), not an instant — and there is no `generated` key anywhere. So
# unlike `/api/compare` (DIV-085, `time.time()` in the body) every 200 here CAN
# byte-match, and these rows are real rows rather than permanent known-opens.
#
# ── ORDER IS SIGNIFICANT, and here it is significant TWICE ───────────────────
#
# The usual reason applies (`deps.current_log_path` is server state). The second
# reason is this module's process-wide memo:
#
#   key   = (str(deps.store_path), scope.LABEL, tuple(sorted(project_ids)) or None)
#   token = (MAX(sessions.last_ts), SUM(sessions.message_count)) — validity only
#
# The key is the LABEL, not the period, so `?period=week` and `?period=7days`
# are ONE key: whichever runs first computes, and the second is served that
# report — rolling `now − 7d` bounds included. The rows below are ordered so
# that collision is exercised deliberately (`F-forks-week` first, then
# `F-forks-7days`) instead of by accident.
#
# Unlike `/api/optimize` (DIV-111) the memo is NOT visible in the body: there is
# no `"cache": "hit"|"miss"` field. A repeat row is therefore expected to be
# byte-identical whether it was served from the memo or recomputed — which is
# exactly what makes `F-forks-again` worth having.
#
# ── the rolling-window caveat ────────────────────────────────────────────────
#
# `7days` / `30days` bound on `now`, so the two servers compute bounds a few
# milliseconds apart and a message inside that gap is a real (if rare)
# divergence — the same property `CD-prov-week` carries. The memo caps the
# exposure at ONE request per label: every later row on that label is served
# from the entry the first one wrote.
#
# ── COST ─────────────────────────────────────────────────────────────────────
#
# DIV-142 measured ~6 s warm and 12.7 s cold on this store, and no shortcut was
# added (a faster answer that differs is a divergence). Budget for it: the
# distinct cache keys below are all-time, today, month, 7days, 30days and the
# empty scope — six cold computations per server, the rest memo-served.
#
# ── LAW 7 ────────────────────────────────────────────────────────────────────
#
# `/api/forks` writes nothing: one read-only join over messages/sessions/
# projects, one `MAX/SUM` over `sessions` for the validity token, `models.toml`
# and the `price_book` table for pricing, and an in-process memo that is
# ordinary process memory. Verified against `reports/forks.py` and
# `routes/forks.py` rather than the docstrings. Every row below is safe.


# ── validation first: these 400 before the scope is built and never cache ────
# `_PERIOD_ALIASES.get(period)` misses, and the message joins the alias table
# in DICT order — "today, week, 7days, month, 30days, all", NOT sorted.
F-forks-bad-period  | GET | /api/forks?period=nonsense        | -
# A PRESENT-but-empty `?period=` is `""`, not the `Query("all")` default, so it
# is a 400 too. Checked against DIV-086 on purpose: `/api/compare?provider=`
# reaches a FILTER and prunes everything; `?period=` here reaches a LOOKUP with
# no `""` key and answers deterministically. No analogous trap.
F-forks-empty-period | GET | /api/forks?period=               | -
# The alias table is case-SENSITIVE.
F-forks-period-case | GET | /api/forks?period=Week            | -
# pydantic does not strip a scalar `str` query param, so the space is part of
# the value and this is a 400 whose `detail` contains the space.
F-forks-period-space | GET | /api/forks?period=%20all         | -
# starlette's QueryParams is last-wins for a scalar param, so the VALID second
# value decides. First-wins would 400 here.
F-forks-period-last | GET | /api/forks?period=nonsense&period=all | -

# ── the "all time" key: the default, a repeat, and the explicit spelling ─────
# `period` defaults to `all`, so all three of these are ONE cache key. The
# repeat proves the memo is not answer-visible; the explicit row proves the
# default really is `all` and not `month` (which is what the cost routes use).
F-forks             | GET | /api/forks                        | -
F-forks-again       | GET | /api/forks                        | -
F-forks-all         | GET | /api/forks?period=all             | -
# `?log_path=` empty is FALSY and falls back to `deps.current_log_path`, so this
# is still the same key — a present-but-empty param that must not widen scope.
F-forks-empty-log   | GET | /api/forks?period=all&log_path=   | -
# The same project named explicitly: same slug, same ids, same key.
F-forks-log-path    | GET | /api/forks?period=all&log_path=/x/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -

# ── the day-aligned periods, one cold computation each ───────────────────────
F-forks-today       | GET | /api/forks?period=today           | -
F-forks-month       | GET | /api/forks?period=month           | -

# ── the rolling windows, and the label collision ─────────────────────────────
# `week` computes; `7days` maps to the SAME `parse_period` spec and therefore
# the same `scope.label`, so it is served `week`'s report. Reordering these two
# changes which one pays for the rolling-window drift.
F-forks-week        | GET | /api/forks?period=week            | -
F-forks-7days       | GET | /api/forks?period=7days           | -
# `30days` is its own label ("last 30 days") and its own key.
F-forks-30days      | GET | /api/forks?period=30days          | -

# ── the unknown project: an EMPTY scope, not a 404 ───────────────────────────
# `_project_ids_for` returns `[]` for a slug nothing matches, and `[]` means
# "a filter was requested and matched nothing" — the report is the ForkReport
# DEFAULTS (float zeros, `abandoned_cost_usd: 0.0`), served 200. That is the
# opposite of `routes/cost.py::_project_ids_for`, which 404s on the same query,
# and the difference is the whole reason this row is a 200.
F-forks-no-project  | GET | /api/forks?period=all&log_path=/x/no-such-project-anywhere | -
# `Path(path).name` is the slug, so a trailing slash resolves to the SAME slug
# on both sides — pathlib drops it, and `pyops::path_name` reproduces that.
F-forks-trailing    | GET | /api/forks?period=all&log_path=/x/no-such-project-anywhere/ | -

# ── the shapes a happy-path matrix never sees ────────────────────────────────
# An undeclared query parameter is ignored, not a 422.
F-forks-extra-param | GET | /api/forks?period=all&limit=abc&details=1 | -
# GET-only path: FastAPI's router raises HTTPException(405) and the installed
# handler renders `{"detail":"Method Not Allowed"}` as JSON. axum's native 405
# is an empty body with no content-type; `crate::json::method_not_allowed`
# restores starlette's. Body is `-` because a 405 is decided before the body is
# read, so none of these three reaches a handler or the store.
F-forks-post        | POST   | /api/forks                     | -
F-forks-put         | PUT    | /api/forks                     | -
F-forks-delete      | DELETE | /api/forks                     | -
# A neighbouring path nothing claims — FastAPI's `{"detail":"Not Found"}`, not
# starlette's plain-text default. `/api/fork` (singular) is the typo a client
# actually makes.
F-forks-singular    | GET | /api/fork                         | -
# NO ROW for `/api/forks/` — the trailing-slash 307 is DIV-133, an architect's
# `lib.rs` change, and the batch-E claim puts it out of this member's scope.
# Stated rather than silently omitted.

# ══ member `benchmark` ═════════════════════════════════════════════════════════════
# Batch E / member `benchmark` — case rows for `routes/benchmark.py`
# (RS-5-045 `GET /api/benchmark`, RS-5-046 `GET /api/benchmark/recommend`).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file **in place of the four `!BM-*`
# rows** currently sitting under the `DIV-143` comment, and strips the leading
# `P-by-dir-known` selection row, which already runs at line 68 there.
#
# The four known-open rows are reproduced verbatim below with the `!` removed:
#   BM-benchmark, BM-bad-period, BM-recommend, BM-recommend-blank.
#
# ── State this group depends on ─────────────────────────────────────────────
#
# `deps.current_log_path` is the StackUnderflow project (`P-by-dir-known` at
# line 68, re-asserted by `J-select` at line 316). Every row without an explicit
# `?log_path=` is therefore PROJECT-SCOPED, and the project-scoped report on the
# harness store is a single `explore × large` stratum with one model —
# `sessions_total: 1`. That is a small, stable, fast payload and it is what the
# happy-path rows diff.
#
# COVERAGE GAP, stated up front: the WHOLE-STORE report (1 310 sessions, 22
# strata, 117 model rows, 43 kB) is unreachable from this matrix, because there
# is no way to *de*-select a project once one is selected and every unknown
# `?log_path=` resolves to the empty report instead. It is covered by unit test
# — `services::benchmark` runs the port against the live store and against a
# CPython dump of the same 11 calls, byte for byte — and by nothing here. See
# DIV-e-benchmark.md, finding 12.
#
# ── Side effects: NONE ──────────────────────────────────────────────────────
#
# Every row is a `GET` or an unclaimed method. The handler opens the store the
# way `store/db.py::connect` does (read-write, as both sides do) and issues only
# `SELECT`s: `session_mart`, `sessions`, the `messages` VIEW, `usage_events`,
# `session_quality_metrics`, `static_analysis_findings`, `commit_session_link`,
# `pr_outcomes`, `ci_runs`, `projects`. `reports/benchmark.py` has no writer and
# imports none — `outcome_attribution.link_commits_to_sessions`, the one writer
# in its dependency graph, is not on this path (DIV-099(b)). Nothing under
# `$STACKUNDERFLOW_HOME` is written. Law 7 is satisfied.
#
# ── Runtime, so the integrator is not surprised ─────────────────────────────
#
# The project-scoped rows are milliseconds. The `?period=` rows still scan the
# whole `session_mart` join before the scope filter narrows it, so each costs
# ~2 s on the Python side and ~1 s on the port's release build. Python memoises
# per `(store, scope, ids, intent)` and the port does not, so a repeat row is
# free on one side and not the other; the differ's 300 s per-case timeout has
# three orders of magnitude of headroom either way.
#
# ── The one inherently time-sensitive pair ──────────────────────────────────
#
# `week` and `7days` resolve to a ROLLING `now - 7d` instant carrying the
# current microsecond (batch A's `_by_model_mart_eligible` measurement, and the
# `scope.rs` module docs). The two servers compute bounds milliseconds apart, so
# a session whose `first_ts` lands in that gap is a real divergence. The harness
# store's newest session starts `2026-07-30T16:32`, six days inside the window,
# so the rows are stable today. Same property as `CD-prov-week`.

# ── the state row this file needs first (integrator strips it) ───────────────

# ── GET /api/benchmark — the happy path ─────────────────────────────────────
# The default `period=all`, project-scoped. Proves the whole envelope:
# `{period, scope, report, currency, warning}`, the `report`'s nine keys in
# declaration order, the `weights` dict rendering `0.2` and not `0.20`,
# `ci_level` as `0.9`, `success_threshold` as `7.0` and not `7`, and the
# `method_notes` array with its `×`, `→`, `—` and `–` intact through
# `ensure_ascii=False`.
#
# It carries one side of the int-versus-float trap: `median_turns` is
# `statistics.median` over a list of ints, and this cell has an odd count, so it
# must render `846` and not `846.0`. The even-count spelling (`103.0`) only
# occurs in the whole-store report, which this matrix cannot reach — see the
# coverage gap above and DIV-e-benchmark.md finding 2.
BM-benchmark        | GET | /api/benchmark                    | -

# Immediately again. Python memoises the report behind `_BENCH_CACHE` and the
# port deliberately does not (DIV-e-benchmark finding 11), so this is the
# cheapest available proof that the memo cannot change the answer within one
# store revision — `PL-plan-repeat`'s trick, and the row that would catch a
# first-call-only side effect.
BM-benchmark-repeat | GET | /api/benchmark                    | -

# ── GET /api/benchmark — every leg of the period alias table ────────────────
# `_PERIOD_ALIASES` has SIX keys mapping onto FIVE `parse_period` specs, and
# `week`/`7days` are the pair that collide. All five distinct windows get a row
# so the alias table is measured and not assumed.
#
# `today` is the empty-report row: the harness store has zero sessions in the
# current calendar day, so this is `_empty_report` on the wire —
# `sessions_total: 0`, `strata: []`, the single "Not enough comparable
# evidence" caveat, and `grade_coverage: 0.0` (a float, never `0`).
BM-period-today     | GET | /api/benchmark?period=today       | -
BM-period-week      | GET | /api/benchmark?period=week        | -
BM-period-7days     | GET | /api/benchmark?period=7days       | -
BM-period-month     | GET | /api/benchmark?period=month       | -
BM-period-30days    | GET | /api/benchmark?period=30days      | -
BM-period-all       | GET | /api/benchmark?period=all         | -

# The `400`. The message joins the alias table's KEYS in INSERTION order —
# "today, week, 7days, month, 30days, all" — and not the sorted order
# `/api/optimize` prints for its own set. Two endpoints, two orders.
BM-bad-period       | GET | /api/benchmark?period=nonsense    | -

# The empty string is an unknown alias, not the default: `Query("all")` only
# applies when the key is ABSENT. The quoted `''` in the message proves it.
BM-period-blank     | GET | /api/benchmark?period=            | -

# The lookup is case-sensitive, so `ALL` is a 400 and not a 200.
BM-period-case      | GET | /api/benchmark?period=ALL         | -

# starlette's `QueryParams` keeps the LAST occurrence of a scalar, not the
# first, so this is `period=all` and a `200` — the opposite of the intuitive
# guess (`qs.rs`'s module docs; the same rule `P-list-page2` relies on).
BM-period-repeated  | GET | /api/benchmark?period=today&period=all | -

# ── GET /api/benchmark — the intent filter ──────────────────────────────────
# A real intent narrows the fact list before stratification.
BM-intent-build     | GET | /api/benchmark?intent=build       | -
BM-intent-explore   | GET | /api/benchmark?intent=explore     | -

# `if intent:` is a TRUTHINESS test, so a blank `?intent=` does NOT filter and
# this row must be byte-identical to `BM-benchmark`. A port that used
# `is not None` would answer the empty report here.
BM-intent-blank     | GET | /api/benchmark?intent=            | -

# An unknown intent matches no fact and is the empty report — a 200, never a
# 400. There is no intent allow-list on this endpoint.
BM-intent-unknown   | GET | /api/benchmark?intent=nosuchintent | -

# ── GET /api/benchmark — the project resolver ───────────────────────────────
# `_project_ids_for` is this module's OWN resolver and it does not 404: it takes
# `Path(log_path).name` as a slug, and an unknown one yields `[]`, which
# `_load_facts` reads as "no sessions". `routes/cost.py`'s resolver raises a 404
# on the same input. The asymmetry is real and this pair measures both sides.
BM-log-path-known   | GET | /api/benchmark?log_path=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
BM-log-path-unknown | GET | /api/benchmark?log_path=/nope/-no-such-project-anywhere | -

# An EMPTY `?log_path=` is falsy, so it falls back to `deps.current_log_path`
# rather than meaning "whole store" — identical bytes to `BM-benchmark`.
BM-log-path-blank   | GET | /api/benchmark?log_path=          | -

# ── GET /api/benchmark — the unclaimed methods ──────────────────────────────
# FastAPI's router raises `HTTPException(405)` and its handler renders it as
# JSON; axum's own fallback is an empty 404 with no content-type, which is what
# `json::method_not_allowed` exists to prevent.
BM-post             | POST   | /api/benchmark                | -
BM-put              | PUT    | /api/benchmark                | -
BM-delete           | DELETE | /api/benchmark                | -

# ── GET /api/benchmark/recommend — the happy path ───────────────────────────
# `{period, scope, recommendation, currency}` — note that this envelope has NO
# top-level `warning`, unlike `/api/benchmark`'s. The recommendation's eleven
# keys are in declaration order and `evidence` is either a whole model row or
# `null`.
BM-recommend        | GET | /api/benchmark/recommend?intent=build | -
BM-recommend-explore | GET | /api/benchmark/recommend?intent=explore | -

# The `size` filter is real and narrows the strata list before the "first clear
# cell" scan; `language` is echoed into the payload and read by NOTHING (the
# docstring's "(and language, when both known)" describes a filter that was
# never written — DIV-e-benchmark finding 8).
BM-recommend-size   | GET | /api/benchmark/recommend?intent=explore&size=large | -
BM-recommend-size-miss | GET | /api/benchmark/recommend?intent=explore&size=tiny | -
BM-recommend-size-blank | GET | /api/benchmark/recommend?intent=explore&size= | -
BM-recommend-lang   | GET | /api/benchmark/recommend?intent=build&language=python | -

# ── GET /api/benchmark/recommend — the validation ORDER ─────────────────────
# Three legs, and the order between them is the contract.
#
# 1. `intent: str = Query(...)` is REQUIRED, so an absent one never reaches the
#    handler: FastAPI's dependency solver answers a pydantic `missing` 422.
BM-recommend-missing | GET | /api/benchmark/recommend         | -

# 2. …and it wins even when `period` is ALSO invalid, because the solver runs
#    before the body. This row is a 422, NOT the 400 `BM-bad-period` gives.
BM-recommend-order  | GET | /api/benchmark/recommend?period=nonsense | -

# 3. Inside the body the PERIOD check runs first, so a present-but-blank intent
#    with a bad period is the period's 400.
BM-recommend-bad-period | GET | /api/benchmark/recommend?intent=&period=nonsense | -

# 4. …and only with a valid period does the blank intent produce its own 400,
#    `{"detail":"intent is required"}`. `.strip()` means whitespace counts as
#    blank, which the second row proves (`%20%20` is two spaces).
BM-recommend-blank  | GET | /api/benchmark/recommend?intent=  | -
BM-recommend-ws     | GET | /api/benchmark/recommend?intent=%20%20 | -

# An unknown intent is a 200 with an honest "insufficient evidence", never a
# 400 — there is no intent allow-list here either.
BM-recommend-unknown | GET | /api/benchmark/recommend?intent=nosuchintent | -

# The same period table, on the second endpoint. `week`'s rolling-instant
# caveat above applies to this row too.
BM-recommend-week   | GET | /api/benchmark/recommend?intent=build&period=week | -
BM-recommend-month  | GET | /api/benchmark/recommend?intent=build&period=month | -
BM-recommend-today  | GET | /api/benchmark/recommend?intent=build&period=today | -

# The same project resolver, on the second endpoint.
BM-recommend-path   | GET | /api/benchmark/recommend?intent=build&log_path=/nope/-no-such-project-anywhere | -

# ── GET /api/benchmark/recommend — the unclaimed methods ────────────────────
BM-recommend-post   | POST   | /api/benchmark/recommend       | -
BM-recommend-delete | DELETE | /api/benchmark/recommend       | -

# NO ROW for `/api/benchmark/` or `/api/benchmark/recommend/` — a trailing
# slash is starlette's `redirect_slashes` 307, which is DIV-133 and the
# architect's `lib.rs` change, explicitly out of batch E's charter.

# ══ member `patterns` ═════════════════════════════════════════════════════════════
# Batch E / member `patterns` — case rows for `routes/patterns.py` (RS-5-088/089).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file and strips the leading
# `P-by-dir-known` selection row, which already runs earlier there.
#
# THE HEADLINE, and it decides the shape of this whole file:
#
#   `GET /api/patterns` puts a WALL CLOCK in its 200 body.
#   `reports/patterns.py:653-656` computes
#       since_iso = (datetime.now(UTC) - timedelta(days=days)).isoformat()
#   and `:1055` emits it as `report.window.since`. `.isoformat()` on an aware
#   datetime renders MICROSECONDS. Two servers answering the same case a few
#   milliseconds apart emit different bytes; so does one server answering twice.
#
#   Therefore EVERY 200 on this endpoint is permanently `!`, by construction —
#   structurally DIV-085 (`/api/compare`'s `generated = time.time()`), reached
#   through a different module. `!PT-patterns` and `!PT-patterns-window` are not
#   "not yet flipped", they are NOT FLIPPABLE, and the two blocks below are split
#   on exactly that line. Both endpoints are nevertheless fully ported: the
#   arithmetic under the clock is covered by 55 unit tests, which is the only
#   place it can be covered.
#
#   `PT-bad-since` reads no clock. It flips, and every other validator leg beside
#   it flips with it (law 5).
#
# SIDE EFFECTS of the rows below: NONE. Every row is a `GET` or an unclaimed
# method on `/api/patterns`. The handler opens the store through
# `store/db.py::connect` on both sides (read-write handle, SELECT-only use,
# plus Python's idempotent `schema.apply`) and `mine_patterns` is `sqlite_master`-
# guarded SELECTs throughout. Nothing under `$STACKUNDERFLOW_HOME` is written.
#
# `POST /api/patterns/dismiss` (RS-5-089) HAS NO ROW HERE, AT ANY STATUS.
# It bumps a `dismissed` counter in `proactive_state.json`, so python-then-rust
# leaves `2` where either alone leaves `1` — non-idempotent under the differ's one
# shared home, which is law 4 / DIV-146's ruling. See `DIV-e-patterns.md` finding
# 2 for the correction to DIV-144's stated reason (the file is NOT outside
# `$STACKUNDERFLOW_HOME`; it is `deps.store_path.parent/proactive_state.json`),
# and finding 10 for the four 422 legs this leaves unprobed — a known, instructed
# law-5 gap, not an oversight.
#
# NOT CLAIMED: `GET /api/patterns/` (trailing slash). That is DIV-133, the
# architect's `lib.rs` item, and adding a row for it here would re-raise a
# dispositioned divergence under a new id.

# ── the state row this file needs first (integrator strips it) ───────────────

# ── the three inherited rows ─────────────────────────────────────────────────
# Two stay open forever; the third flips. Kept in their original order and with
# their original ids so the integrator can diff this file against the ledger.
#
# `!PT-patterns` runs AFTER the selection row, so `project` resolves through
# `deps.current_log_path` and the body opens
# `{"project":"-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnd…` — proof the
# basename-as-slug fallback fires. Everything after `report.window.since` in that
# body is deterministic; only that one string moves.
!PT-patterns         | GET | /api/patterns                     | -
!PT-patterns-window  | GET | /api/patterns?since=7d            | -
# FLIPPED. No clock on this path — a pure 400 off the query string. The body is
#   {"detail":"Invalid since 'nonsense'. Use <days>d between 1d and 365d, e.g. 7d, 30d, 90d."}
# and note that the message interpolates the parameter UNSTRIPPED, so a padded
# input echoes its own padding (PT-since-padded-bad below).
PT-bad-since         | GET | /api/patterns?since=nonsense      | -

# ── the rest of the 200 surface — same clock, same permanent `!` ─────────────
# These are here despite being unflippable because "we know" beats "nobody
# looked": each one still proves its own branch under the differ's banner, and a
# divergence in any field OTHER than `report.window.since` is a real defect the
# integrator should see.
#
# The two window boundaries. `1d` and `365d` are the inclusive ends of the
# validator's range, and `1d` also exercises the day-truncation gap recorded as
# finding 7 (the mart is bounded by `since_iso[:10]`, the messages sweep by the
# full instant, so this request sees a wider touch window than error window).
!PT-since-1d         | GET | /api/patterns?since=1d            | -
!PT-since-365d       | GET | /api/patterns?since=365d          | -
# `_SINCE_RE` is `^(\d{1,3})d$` applied to `since.strip()`: leading zeros are
# fine and surrounding whitespace is stripped BEFORE the match. Both are 7d.
!PT-since-zeropad    | GET | /api/patterns?since=007d          | -
!PT-since-padded     | GET | /api/patterns?since=%20%207d%20   | -
# An explicit `?project=` wins over `deps.current_log_path`. This one names the
# selected project itself, so it must agree with `!PT-patterns` field for field.
!PT-project-self     | GET | /api/patterns?project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -
# An unknown slug resolves to an EMPTY id list, which `mine_patterns` reads as
# "a filter matched nothing" — an empty report, NOT a 404. The feature is
# advisory and this is the row that says so.
!PT-project-unknown  | GET | /api/patterns?project=-no-such-project-anywhere | -
# `?project=` is a present-but-EMPTY `str`, which is not `None`. Python's
# `if slug is None and deps.current_log_path` is therefore skipped, `if slug` is
# false so no scope is applied, and the echo is `"project":""` — the whole-store
# view under an empty parameter, with a selected project sitting right there.
# The single most likely thing a careless port collapses into the `None` branch.
!PT-project-empty    | GET | /api/patterns?project=            | -
# A repeated scalar param: starlette's `QueryParams` builds its dict from the
# items in order, so the LAST value wins. Same rule `OPT-period-last` pins.
!PT-project-last     | GET | /api/patterns?project=aaa-a&project=zzz-b | -
# Neither FastAPI nor axum validates unknown query keys, so this must be
# field-for-field identical to `!PT-patterns`. It is here because "and then
# someone adds a parameter" is how a 422 quietly appears on a surface that never
# had one.
!PT-junk-query       | GET | /api/patterns?nonsense=1&days=7   | -

# ── the validation legs — no clock, these all diff clean (law 5) ─────────────
# Every one of these is a 400 carrying the same sentence with a different echo.
# Four distinct rejection causes are represented: below the range, above the
# range, too many digits for `\d{1,3}`, and no digits at all.
PT-since-zero        | GET | /api/patterns?since=0d            | -
PT-since-over        | GET | /api/patterns?since=366d          | -
PT-since-4digit      | GET | /api/patterns?since=1000d         | -
PT-since-empty       | GET | /api/patterns?since=              | -
PT-since-nounit      | GET | /api/patterns?since=7             | -
PT-since-unit-only   | GET | /api/patterns?since=d             | -
PT-since-inner-space | GET | /api/patterns?since=7%20d         | -
PT-since-upper       | GET | /api/patterns?since=7D            | -
PT-since-negative    | GET | /api/patterns?since=-1d           | -
PT-since-word        | GET | /api/patterns?since=7days         | -
# The echo is the RAW parameter, padding included: `Invalid since '  bad  '.`
PT-since-padded-bad  | GET | /api/patterns?since=%20%20bad%20%20 | -
# Last-value-wins again, this time landing on the 400 rather than the 200. A
# port that read the FIRST value would answer 200 here and the divergence would
# be a whole body, not a field.
PT-since-last-bad    | GET | /api/patterns?since=7d&since=nonsense | -
# ...and the mirror: a bad value FOLLOWED by a good one is a 200 (so, `!`).
!PT-since-last-good  | GET | /api/patterns?since=nonsense&since=7d | -
# Validation order: `_parse_since` runs BEFORE the project is resolved, so a bad
# window on an unknown project is still the 400, never a project error.
PT-bad-since-project | GET | /api/patterns?project=-no-such-project-anywhere&since=nope | -

# ── the router fallbacks ────────────────────────────────────────────────────
# GET-only, so FastAPI's router raises HTTPException(405) and the installed
# handler renders `{"detail":"Method Not Allowed"}` as JSON. axum's native 405 is
# an empty body with no content-type; `lib.rs`'s `method_not_allowed_fallback`
# restores starlette's. Body is `-` because a 405 is decided before the body is
# read — none of these three reaches a handler, and none can write.
PT-post              | POST   | /api/patterns                 | -
PT-put               | PUT    | /api/patterns                 | -
PT-delete            | DELETE | /api/patterns                 | -
# A path nothing claims — FastAPI's `{"detail":"Not Found"}`, not starlette's
# plain-text default. `/api/pattern` (singular) is the typo a client makes.
PT-singular          | GET | /api/pattern                      | -
PT-subpath           | GET | /api/patterns/status              | -

# ══ member `worktrees` ═════════════════════════════════════════════════════════════
# Batch E / member `worktrees` — cases for /api/worktrees (RS-5-114, RS-5-115).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. Ids are
# prefixed `W-`. ORDER IS SIGNIFICANT; do not sort this file.
#
# ── LAW 7 clearance, and the one endpoint that does NOT get it ───────────────
#
# `GET /api/worktrees` shells out to git six ways. Every invocation is
# READ-ONLY: `services/worktrees.py::_run_git` refuses anything outside the
# five-entry `_ALLOWED_GIT_PREFIXES` allow-list (`worktree list`,
# `status --porcelain`, `cherry`, `rev-parse`, `symbolic-ref`) and every call
# carries `--no-optional-locks`, so not even `status` may refresh the index. No
# file is written, no store row is written, no cache is touched. The full argv
# table and the per-command determinism verdicts are in
# `parity/DIV-e-worktrees.md`.
#
# `POST /api/worktrees/attribute` has NO ROW AND NEVER WILL. It writes
# `projects.worktree_of`. It is idempotent and additive, which is exactly what
# makes a row look safe — but python-then-rust against ONE shared home means the
# second server answers a question the first already changed. DIV-078 / LAW 4.
# The response shapes were probed against the reference on a PRIVATE COPY of the
# harness home instead: `{"updated":3}` on a store where nothing was stamped,
# `{"updated":0}` on every run after. Recorded in the ledger, not in this file.
#
# ── Why every 200 row here is `!`, permanently ───────────────────────────────
#
# The payload's last-but-one key is
#
#     "scanned_at": datetime.now(UTC).isoformat()      # routes/worktrees.py:105
#
# — microsecond resolution, stamped unconditionally. There is no query parameter
# that suppresses it and no branch that omits it. MEASURED against the reference
# on :8099: two `GET /api/worktrees` calls 11 s apart differed in EXACTLY ONE
# leaf, `/scanned_at`, and agreed on every other leaf, `worktrees[*]` included.
# So the impossibility is one field wide — precise, and still fatal to a byte
# differ. An honest `!` outranks a green tick that had to be engineered.
#
# ── What ELSE can move, on the rows that reach a real repo ───────────────────
#
# Only `W-scoped` below reaches one (see the next section). On that row three
# further fields are a function of the MACHINE rather than of the store:
#
#   * `worktrees[*].dirty_count` — the `git status --porcelain` line count.
#     MEASURED moving 23 -> 26 inside one session while sibling batch-E agents
#     wrote files into one of the scanned worktrees. This is the field most
#     likely to make a row look red for a reason that is not a port defect.
#   * `worktrees[*].head` / `.branch` / `.unique_commits` — any commit, checkout
#     or rebase in a scanned repo between the two requests.
#   * `worktrees[*].age_days`, and therefore `.verdict` — `round(days, 2)` is
#     864-second granularity, so it is stable within a call pair EXCEPT across a
#     bucket boundary, and `ACTIVE` flips at exactly 48 h.
#
# ── The whole-store scan is NOT reachable from the shared matrix ─────────────
#
# `path = log_path or deps.current_log_path`, and the whole-store branch needs
# BOTH to be falsy. The shared file selects a project early
# (`P-by-dir-known`), which sets `deps.current_log_path` to
# `/home/tmos/.claude/projects/-media-…-StackUnderflow` — a directory that is
# not a git repo. So `W-worktrees` and `W-blank` below are the SCOPED-to-a-
# non-repo case, and their body is the empty scan:
#
#   {"scope":"/home/tmos/.claude/projects/-media-…","worktrees":[],
#    "summary":{"total":0,…,"attributed_cost_usd":0.0},"scanned_at":…}
#
# (measured, and the same prefix the pre-existing `!W-worktrees` row recorded).
# That is not a loss: it is the row that proves `attributed_cost_usd` ships as
# the FLOAT `0.0` — the summary seeds the literal `0.0` and steps it with `+=`
# rather than `sum()`, so an int `0` would be a divergence in the other
# direction. `W-scoped` is what covers the git fan-out.
#
# ── The deterministic subset that DOES exist ─────────────────────────────────
#
# The method-fallback rows at the end. FastAPI's router answers them before any
# handler exists, so they touch neither git nor the store and their bodies are
# fixed strings. They are the only rows on this path that can go identical, and
# they are also LAW 5's requirement: neither endpoint has a 400/404/422 leg at
# all (`log_path` is a free-form string and there is nothing to validate), so
# the 405s are the entire error surface.
#
# NOT claimed here: `GET /api/worktrees/` (trailing slash) answers 307 on the
# reference. That is DIV-133, the architect's `lib.rs` item, and batch E's
# charter says not to fix it in passing — so no row.

# Select a project so `deps.current_log_path` is set. The integrator strips this
# row when merging — it already runs earlier in the shared file. `W-worktrees`
# and `W-blank` DEPEND on it having run.

# ── the default resolution ───────────────────────────────────────────────────
# No query parameter: falls to `deps.current_log_path`, which is the .claude
# projects directory and not a repo. Short-circuits at the `Path.is_dir()`
# stat + one `rev-parse --git-common-dir`, so this row is cheap AND it is the
# empty-scan shape.
!W-worktrees        | GET | /api/worktrees                    | -

# ── the row that actually exercises git ──────────────────────────────────────
# The maintainer's repo: one `worktree list --porcelain`, one `symbolic-ref`,
# then `cherry` + `status` per linked worktree. Three linked worktrees on this
# host at time of writing (`…/StackUnderflow-rust`, `…/.claude/worktrees/
# agent-…`, and a detached one under /tmp). ~1.4 s end to end — comfortably
# inside the differ's 300 s per-case timeout. This is where a `dirty_count`
# difference will show up if an agent is writing while the matrix runs.
!W-scoped           | GET | /api/worktrees?log_path=/media/tmos-bumblebe/dev_dev/year26/jul26/StackUnderflow | -

# A path that is not a directory short-circuits at the stat, so NO git process
# is spawned at all — and `scope` still echoes the requested path verbatim
# rather than falling back.
!W-scope-miss       | GET | /api/worktrees?log_path=/nonexistent/nope | -

# `?log_path=` is the EMPTY string, which is falsy — so it falls through to
# `deps.current_log_path` rather than scoping to `""`. `scope` must therefore be
# the selected project's log path, identical to `W-worktrees`, and NOT `"store"`.
!W-blank            | GET | /api/worktrees?log_path=          | -

# starlette resolves a repeated SCALAR to the LAST occurrence, so this scopes to
# `/nonexistent/b` and 200s — not to `a`, and not a 422. Measured.
!W-last-wins        | GET | /api/worktrees?log_path=/nonexistent/a&log_path=/nonexistent/b | -

# An unknown query parameter is ignored by FastAPI, not rejected.
!W-unknown-param    | GET | /api/worktrees?nope=1             | -

# ── the method surface: the only rows that can go green ──────────────────────
# `/api/worktrees` is GET-only, so every other verb is FastAPI's
# `{"detail":"Method Not Allowed"}` at 405 — starlette's router raises it before
# the handler exists, so nothing is scanned and nothing is written. Measured on
# the reference for all three verbs.
W-post-405          | POST   | /api/worktrees                 | -
W-put-405           | PUT    | /api/worktrees                 | -
W-delete-405        | DELETE | /api/worktrees                 | -

# The mirror image on the writer's path: it is POST-only, so a GET is the same
# 405 — and the row is safe precisely BECAUSE the handler never runs. This is
# the only way `/api/worktrees/attribute` can appear in the matrix at all.
W-attribute-get-405 | GET    | /api/worktrees/attribute       | -
W-attribute-put-405 | PUT    | /api/worktrees/attribute       | -

# ══ member `agent-teams` ═════════════════════════════════════════════════════════════
# Batch E / member `agent_teams` — case rows for `routes/agent_teams.py`
# (RS-5-042 … RS-5-044), the module DIV-082 deferred whole.
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file, strips the leading
# `P-by-dir-known` selection row (which already runs earlier there) and
# REPLACES the five `!A-*` rows in the "agent teams — the whole module is open"
# block with the un-`!`-ed copies below.
#
# ── Side effects: NONE ───────────────────────────────────────────────────────
#
# Every row is a GET or an unclaimed method. All three handlers open the store
# read-write (that is what `store/db.py::connect` does on both sides) and issue
# only SELECTs. Python additionally calls `schema.apply(conn)` per request — a
# migration ladder — which is idempotent DDL the harness store already
# satisfies; the port does not run it at all (DIV-102). Nothing under
# `$STACKUNDERFLOW_HOME` is written. Law 4 is satisfied: every row is safe, and
# every row terminates well inside the differ's 300 s socket timeout (the
# slowest, `A-list-empty-project`, is a ~6 s full-store sidechain rollup).
#
# ── None of these rows need a selected project ───────────────────────────────
#
# `agent_teams` never reads `deps.current_log_path`; `?project=` is a SLUG, not
# a log path. The selection row is carried only so this file runs standalone
# under `--only A-`.
#
# ── What DIV-042 does to these payloads ──────────────────────────────────────
#
# DIV-042 (the port's ingest-side team-metadata hook is a stub) does NOT starve
# these rows. Both servers read the SAME store, and the reference's ingest
# already materialised 50 `agent_teams` rows and 321 team-tagged sessions into
# it, so the indexed path has real content on both sides and compares like
# against like. The project the harness selects
# (`…jul26-StackUnderflow`, id 314) happens to have ZERO materialised teams and
# ZERO sidechain messages, which is why the two rows scoped to it exercise the
# *heuristic* paths instead — see `A-list-scoped` and `A-graph`. Coverage of the
# indexed path therefore comes from the unscoped and jan26-scoped rows below,
# deliberately.

# ── the state row this file needs first (integrator strips it) ───────────────

# ═══ GET /api/agent-teams — RS-5-042 ═════════════════════════════════════════
#
# The list view's THREE strategies are chosen by two gates, and each row below
# names which strategy it lands on. Getting the gate order wrong is invisible on
# an empty store and loud on this one.

# (1) INDEXED. `_indexed_teams_available` is true (321 team-tagged sessions) and
# `project_slug is None` short-circuits `_indexed_teams_match_project`, so this
# is the `agent_teams` JOIN, ordered `MAX(last_ts) DESC, team_id ASC`. **46**
# rows, not 50: the table has 50 teams and four of them have no session
# carrying their `team_id`, so the inner JOIN drops them and `LIMIT 50` never
# binds. Proves the whole indexed row shape: `team_name` = the team id,
# `description` = the config blurb, `session_id` = `lead_session_id or team_id`.
A-list              | GET | /api/agent-teams                     | -

# (3) TASK-TOOL. `?project=<the selected slug>` — project 314 has no
# `agent_teams` row, so `_indexed_teams_match_project` returns False; it has no
# sidechain message either, so `_list_team_sessions_scan` returns `[]`, which is
# FALSY and falls through. The answer is the `tools_json LIKE '%"Task"%'` rollup:
# one row, 37 invocations, with the interpolated `description` f-string.
A-list-scoped       | GET | /api/agent-teams?limit=5&project=-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -

# (1) INDEXED, FILTERED. A slug that DOES have `agent_teams` rows, so the same
# path as `A-list` but with the `WHERE p.slug = ?` arm — 14 teams. This is the
# only row that proves the filtered branch of the indexed SQL builder; without
# it a port that ignored `project` entirely would still pass `A-list`.
A-list-scoped-idx   | GET | /api/agent-teams?limit=20&project=-Users-yadkonrad-dev-dev-year26-jan26-StackUnderflow | -

# (2) SIDECHAIN SCAN. The only row that reaches it. This project has sidechain
# messages but NO `agent_teams` row, so gate 2 fails and the scan finds one
# lead candidate with two `agent-XXXX` sub-sessions. Proves the heuristic's
# `agentId` extraction (the `agent_count` is `len(set(agent ids))`, 2, not
# `len(other_subs)`), the `sum(...)` of sub-message counts (4), and that
# `team_name` stays null when no blob carries `teamName`.
A-list-scan         | GET | /api/agent-teams?limit=5&project=-Users-yadkonrad-dev-dev-year25-dec25-mycelium-ecosystem | -

# `?project=` — the EMPTY string. `_indexed_teams_match_project` tests
# `project_slug is None`, an IDENTITY test, so `""` does not short-circuit: it
# runs the JOIN with `slug = ''`, misses, and forces the fall-through. The
# fall-through paths then test `if project_slug:`, TRUTHINESS, and read the same
# `""` as "no filter". One request, two readings. `limit=1` bounds the loop to a
# single candidate so the row stays cheap.
A-list-empty-proj   | GET | /api/agent-teams?limit=1&project=    | -

# A slug no project has. All three strategies come back empty and the contract
# is a clean `{"teams":[]}` — never a 500, never a 404.
A-list-unknown-proj | GET | /api/agent-teams?project=no-such-project-slug-at-all | -

# The two INCLUSIVE bounds of `Query(50, ge=1, le=500)`. `limit=1` also proves
# the SQL `LIMIT ?` is bound and not clamped host-side.
A-list-limit-min    | GET | /api/agent-teams?limit=1             | -
A-list-limit-max    | GET | /api/agent-teams?limit=500           | -

# pydantic strips surrounding whitespace before coercing, so `%20%205%20` is 5
# and NOT a 422 — the opposite of the intuitive guess.
A-list-limit-space  | GET | /api/agent-teams?limit=%20%205%20    | -

# starlette's `QueryParams._dict` keeps the LAST occurrence of a scalar key.
A-list-limit-repeat | GET | /api/agent-teams?limit=1&limit=2     | -

# ── the four 422 legs (law 5) ────────────────────────────────────────────────
#
# `limit: int = Query(50, ge=1, le=500)` is a CONSTRAINED int, so a bound
# failure is `greater_than_equal` / `less_than_equal` with a `ctx` object — NOT
# `int_parsing`, and NOT DIV-151's `{"detail":"limit"}`. The `input` field
# echoes the RAW query string, not the coerced integer.
A-limit-zero        | GET | /api/agent-teams?limit=0             | -
A-limit-negative    | GET | /api/agent-teams?limit=-1            | -
A-limit-over        | GET | /api/agent-teams?limit=501           | -
# pydantic ints are arbitrary-precision, so this coerces fine and then fails the
# UPPER BOUND. `crate::qs::opt_int` would answer `int_parsing` here (DIV-107,
# `!CR-at-bignum`), which is exactly why this module coerces `limit` locally.
A-limit-bignum      | GET | /api/agent-teams?limit=999999999999999999999 | -
A-limit-nan         | GET | /api/agent-teams?limit=abc           | -
# A fractional STRING is not an integer to pydantic's lax str→int, so `5.5` is a
# parse failure and not a truncation.
A-limit-float       | GET | /api/agent-teams?limit=5.5           | -
# Present-but-empty: `int_parsing` with `"input":""`, not the default of 50.
A-limit-blank       | GET | /api/agent-teams?limit=              | -

# ═══ GET /api/agent-teams/{session_id} — RS-5-043 ════════════════════════════

# HEURISTIC GRAPH. The selected project's busiest session. It carries no
# `team_id`, so `_build_team_graph_indexed` returns None and the module falls
# through to `_build_team_graph_scan` — which finds the lead, finds NO sidechain
# sessions in project 314, and answers 200 with `"agents":[]`. That empty array
# is the documented "session exists but spawned nothing" contract, and the
# `lead` block it ships is the full `_agent_summary_for_session`: message count,
# the 300-CODE-POINT first user prompt, the dominant model, and `cost_usd`
# priced through the store's price book (LAW 2) and `round(…, 4)`.
A-graph             | GET | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a | -

# INDEXED GRAPH, entered by the team's own lead. One lead + three sub-agents,
# ordered `agent_role='lead' first, then first_ts ASC, session_id ASC`. Proves
# `spawned_by_session_id` reaching `parent_session_id`, the `agent_role`
# passthrough, and — the byte contract the brief flags — that the `agents` array
# order is the SQL's insertion order and not a re-sort.
A-graph-indexed     | GET | /api/agent-teams/b29f314f-3f98-46cd-aebc-f4d085f7aef4 | -

# INDEXED GRAPH, entered by a SUB-AGENT's id. `t.lead_session_id = ?` misses, so
# the second lookup resolves `sessions.team_id` and re-roots the graph at the
# team's lead: the response's `session_id` is `b29f314f…`, NOT the id in the
# URL. The only row that proves the member-resolution branch.
A-graph-member      | GET | /api/agent-teams/15f6a8d0-3e55-4867-b662-93b83a38484f | -

# Neither a lead, nor a member, nor a session — 404 with the id interpolated
# verbatim into the detail string.
A-graph-missing     | GET | /api/agent-teams/no-such-session     | -

# A percent-encoded path segment. Both routers decode before the handler sees
# it, so the 404 detail carries a literal space. Cheap proof that the port is
# not double-decoding or echoing the encoded form.
A-graph-encoded     | GET | /api/agent-teams/%20                 | -

# ═══ GET /api/agent-teams/{session_id}/agent/{agent_session_id} — RS-5-044 ═══

# The happy path: a real lead and a real sub-agent of its team, 28 messages
# (~44 KB). Proves the transcript row shape end to end — the 16 columns in
# SELECT order, `sqlite3.Row`'s STORAGE-class values with no affinity coercion,
# and `is_sidechain` coerced to a JSON bool IN PLACE at position 13 rather than
# being appended.
A-transcript-real   | GET | /api/agent-teams/42f45b99-782c-4f9e-9572-c9d2a726efc6/agent/49f7aa40-b956-44a9-b9a1-fc54419586d1 | -

# The fence is the PROJECT and nothing else — no team-membership check — so a
# session paired with ITSELF is a legitimate 200. Same rows, different
# `session_id` header field.
A-transcript-self   | GET | /api/agent-teams/49f7aa40-b956-44a9-b9a1-fc54419586d1/agent/49f7aa40-b956-44a9-b9a1-fc54419586d1 | -

# A real lead, an agent id that exists nowhere — 404.
A-transcript        | GET | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a/agent/no-such-agent | -

# A real lead and a real agent that live in DIFFERENT projects (314 vs 109). The
# self-join on `project_id` finds no pair, so this is a 404 with the SAME detail
# string as the row above — three causes, one message. Reproduced, not improved.
A-transcript-cross  | GET | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a/agent/49f7aa40-b956-44a9-b9a1-fc54419586d1 | -

# The lead is missing and the agent is real — still 404, and the detail names
# both, so the caller cannot tell which half failed.
A-transcript-nolead | GET | /api/agent-teams/no-such-session/agent/49f7aa40-b956-44a9-b9a1-fc54419586d1 | -

# ═══ the router's own answers ════════════════════════════════════════════════
#
# All four unclaimed methods 405 in starlette's router BEFORE any handler runs,
# so none of them can write anything — they are safe under law 4 by
# construction. FastAPI renders `{"detail":"Method Not Allowed"}` where axum's
# bare default would send an empty body.
A-list-post         | POST   | /api/agent-teams                  | -
A-list-delete       | DELETE | /api/agent-teams                  | -
A-graph-post        | POST   | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a | -
A-transcript-put    | PUT    | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a/agent/no-such-agent | {"x": 1}

# An empty final path segment does NOT match `{agent_session_id}` (starlette's
# default `str` convertor is `[^/]+`), and `/api/agent-teams/x/agent` is not a
# route either, so there is nothing to redirect to: a plain
# `{"detail":"Not Found"}`, not a 307. One segment too many is the same.
A-agent-no-id       | GET | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a/agent/ | -
A-agent-too-deep    | GET | /api/agent-teams/44b8f238-3609-4445-97a9-37a5a99f1c9a/agent/x/y | -

# DELIBERATELY ABSENT: `/api/agent-teams/` (the bare trailing slash). It is a
# `redirect_slashes` 307, which is DIV-133 and the ARCHITECT's — a `lib.rs`
# change this member may not make and must not measure around.

# ══ member `compare` ═════════════════════════════════════════════════════════════
# Batch E / member `compare` — case rows for `GET /api/sessions/compare`
# (RS-5-105, the endpoint DIV-070 deferred).
#
# Format and conventions: see the header of `parity/endpoint-cases.txt`. The
# integrator merges these rows into that file, replacing the single existing
# `!J-compare` row (line 329 at claim time) and stripping the leading
# `P-by-dir-known` selection row, which already runs earlier there.
#
# Side effects: NONE. Every row is a GET or an unclaimed method. The handler
# opens the store read-write (that is what `store/db.py::connect` does on both
# sides) and issues only SELECTs — `projects`, `sessions`, `messages`. Nothing
# under `$STACKUNDERFLOW_HOME` is written, so the rows are idempotent under
# python-then-rust on one shared home (law 4).
#
# ── READ THIS BEFORE BELIEVING THE `!` ROWS ──────────────────────────────────
#
# The 200 body of this endpoint is NOT byte-reproducible, and not because the
# port is incomplete. `diff.tokens` is built by iterating
# `set(sa["tokens"]) | set(sb["tokens"])` (`routes/sessions.py:405-408`), and
# CPython randomises `str` hashing per process. `endpoint-parity.sh` does not
# export `PYTHONHASHSEED` (`parity-cli.sh` does, line 113), so the reference
# server emits a different `diff.tokens` KEY ORDER on every boot. Three runs of
# the reference handler over `.parity-state/fresh` gave:
#
#   {"cache_read":…,"cache_creation":…,"input":…,"output":…}
#   {"input":…,"cache_read":…,"cache_creation":…,"output":…}
#   {"input":…,"cache_creation":…,"cache_read":…,"output":…}
#
# — with every other byte of the payload identical across all three. This is
# DIV-085's class of defect (a payload that cannot agree with itself) arriving
# through a different door, so the three 200 rows below stay `!`. Everything
# that is NOT a 200 is fully pinned, which is the whole point of the rest of
# this file: 14 green rows over 4 status codes.
#
# ── A ROW THAT CANNOT BE PLACED HERE — integrator, please read ───────────────
#
# `if not path: raise HTTPException(400, "No project selected or log_path
# provided")` is a real leg and it is verified (400, exact detail) against the
# reference. It is unreachable in the merged matrix: `P-by-dir-known` selects a
# project at line 68 and nothing ever deselects, so `deps.current_log_path` is
# truthy for every row after it. The row belongs in the pre-selection block
# beside `P-current-none`, NOT here:
#
#   J-compare-noproject | GET | /api/sessions/compare?a=x&b=y | -
#
# It is left out of the block below rather than placed where it would silently
# exercise the wrong branch. Covered meanwhile by `routes/sessions.rs`'s unit
# tests.

# ── the state row this file needs first (integrator strips it) ───────────────

# ── the happy path — permanently open, see the banner above ──────────────────
# The row the claim hands this member. `a` is a 3-message session that cost
# nothing (so `cost` renders `0.0` and `models_used` is `[]`); `b` is a
# 1276-message session across two models. Between them they exercise the float
# zero, the empty model set, the multi-model `by_model` cost fold and a
# ~95 000 s duration.
!J-compare          | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
# `a == b`. NOT a 404: the `session_id IN (?, ?)` binds the same value twice,
# SQLite collapses it to one row, and every field of `diff` is zero — which is
# the row that proves `cost`/`duration_s` still render `0.0` and
# `commands`/`errors` still render `0`. Open only because `diff.tokens` (four
# zeros) is ordered by the same randomised set.
!J-compare-same     | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
# `log_path=` overrides the selected project rather than being ignored. Points
# at the SAME project by its log dir, so the body must equal `!J-compare`'s
# modulo the token order — i.e. this row and that one diverge in exactly the
# same place or the override is broken.
!J-compare-logpath  | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a&log_path=/home/u/.claude/projects/-media-tmos-bumblebe-dev-dev-year26-jul26-StackUnderflow | -

# ── 422: the two required parameters (law 5) ─────────────────────────────────
# `a: str` and `b: str` have no defaults, so FastAPI's dependency solver 422s
# before the handler runs. Measured on the harness interpreter (fastapi 0.141.1
# / pydantic 2.13.4), not transcribed — and NOT law 8's `{"detail":"<field>"}`
# shape, which is a different bug in four other modules. The body is
# `{"detail":[{"type":"missing","loc":["query","<f>"],"msg":"Field required",
# "input":null}]}`.
J-compare-no-a      | GET | /api/sessions/compare?b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
J-compare-no-b      | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
# BOTH absent: pydantic collects every error, so the list has TWO entries — and
# they are in the order the handler DECLARES its parameters (`a`, then `b`),
# not the order of the query string. A port that returns the first error only,
# or that orders by omission, fails exactly here.
J-compare-no-ab     | GET | /api/sessions/compare | -
# Query keys are case-SENSITIVE: `?A=` does not satisfy `a`, and the unknown
# `A` is silently ignored rather than rejected.
J-compare-case-key  | GET | /api/sessions/compare?A=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -

# ── 404 (i): the session lookup, `Session(s) not found: …` ───────────────────
J-compare-unknown-a | GET | /api/sessions/compare?a=00000000-0000-0000-0000-000000000000&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
J-compare-unknown-b | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=ffffffff-ffff-ffff-ffff-ffffffffffff | -
# Both unknown: the two ids are `', '.join`ed in `(a, b)` order.
J-compare-unknown2  | GET | /api/sessions/compare?a=00000000-0000-0000-0000-000000000000&b=ffffffff-ffff-ffff-ffff-ffffffffffff | -
# The same unknown id twice. The comprehension walks the TUPLE `(a, b)` and does
# not deduplicate, so the detail really is `Session(s) not found: zzz, zzz`.
# A port that built the missing list from a set gets one `zzz` and fails here.
J-compare-dup-miss  | GET | /api/sessions/compare?a=zzz&b=zzz | -
# `?a=` is an EMPTY STRING, which is a perfectly valid `str` to pydantic — it
# passes validation, reaches the handler, and names no session. The detail has
# a trailing space and nothing after it.
J-compare-empty-a   | GET | /api/sessions/compare?a=&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
J-compare-empty-ab  | GET | /api/sessions/compare?a=&b= | -
# A repeated `?a=` keeps the LAST value (starlette's `QueryParams.get`). The
# 404 names `zzz`, not the valid first id — the cheapest available proof of
# last-wins on this endpoint.
J-compare-a-repeat  | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&a=zzz&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
# The session lookup is scoped to the resolved project's ids. Both ids are real
# sessions — of a DIFFERENT project — so a handler that forgot the
# `project_id IN (…)` predicate would answer 200 here.
J-compare-wrong-prj | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a&log_path=/home/u/.claude/projects/-Users-yadkonrad-dev-dev-year26-jun26-worldcup | -

# ── 404 (ii): the SECOND session 404, which is not the first one ─────────────
# `2c0af9c7-…` is a real `sessions` row of `-home-tmos` with zero `messages`
# rows, so it clears the id check and then produces no `session_costs` entry —
# `sa is None` at `routes/sessions.py:398`. This is the only row that reaches
# that branch; without it the second 404 is an unported leg wearing a green
# tick. `b` is a 7-message session of the same project, so the OTHER side does
# resolve and the detail names exactly one id.
J-compare-no-costs  | GET | /api/sessions/compare?a=2c0af9c7-b01c-45ea-9404-8de61fcb363c&b=02b9d902-3c4c-4553-a47a-5e090d1a8d60&log_path=/home/u/.claude/projects/-home-tmos | -

# ── 404 (iii): the project 404, which spells the slug ────────────────────────
# `f"Project '{slug}' not found in store"` — note this handler interpolates the
# slug where its neighbour `/api/jsonl-content` says only "Project not found in
# store". Two messages, two handlers, one file.
J-compare-no-proj   | GET | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a&log_path=/home/u/.claude/projects/-no-such-project-anywhere | -

# ── the unclaimed methods and the neighbouring paths ─────────────────────────
# GET-only, so FastAPI's router raises `HTTPException(405)` and the installed
# handler renders `{"detail":"Method Not Allowed"}` as JSON; axum's native 405
# is an empty body with no content-type. Body is `-` because a 405 is decided
# before the body is read.
J-compare-post      | POST   | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
J-compare-put       | PUT    | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
J-compare-delete    | DELETE | /api/sessions/compare?a=04a86c63-4cfd-4929-91a3-3edfe80e3e2f&b=44b8f238-3609-4445-97a9-37a5a99f1c9a | -
# Paths nothing claims — FastAPI's `{"detail":"Not Found"}`, not starlette's
# plain-text default. The parent collection is the URL a client guesses; the
# subpath is what a router that matched a prefix would answer.
J-compare-parent    | GET | /api/sessions | -
J-compare-subpath   | GET | /api/sessions/compare/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
# The trailing-slash row batch E deliberately left out — DIV-133 was open and
# its claim said not to extend the blast radius. DIV-133 is closed; the row is
# `SL-compare-slash`, filed with the rest of the class.

# ══ member `misc` ═════════════════════════════════════════════════════════════
# Batch E / misc — `routes/misc.py` (RS-5-079/080/084).
#
# Format and conventions: see the header of `endpoint-cases.txt`. The integrator
# folds these rows into that file and strips the leading `P-by-dir-known`, which
# already runs earlier there.
#
# ── What these rows do to the existing file ──────────────────────────────────
#
# The existing `!M-ollama` row is **REPLACED** by the unmarked `M-ollama` below.
# It is the batch's one known-open flip. Everything else here is new.
#
# ── Why the flip is legitimate, and the one condition it rests on ───────────
#
# `/ollama-api/{path}` proxies to `http://localhost:11434/api/{path}` and wraps
# the whole call in a BARE `except Exception`, whose body is a fixed
# `502 {"error": "Ollama not available"}`.
#
# Port 11434 is CLOSED on this host. Verified three ways before a line was
# written, and re-verifiable in one command each:
#
#     ss -lnt | grep 11434                       # no listener
#     bash -c 'exec 3<>/dev/tcp/127.0.0.1/11434' # Connection refused
#     bash -c 'exec 3<>/dev/tcp/::1/11434'       # Connection refused
#     getent ahosts localhost                    # 127.0.0.1 only
#     curl -m 5 http://localhost:11434/api/tags  # curl: (7) refused
#
# So the ONLY branch either side can reach is the catch-all, and its body is a
# constant. That is what makes these rows deterministic — not the port, the
# MACHINE.
#
#   ⚠ IF OLLAMA IS EVER RUNNING, EVERY `M-ollama*` ROW BELOW GOES
#     NONDETERMINISTIC. The bodies become whatever the daemon answers: `/tags`
#     lists the installed models, `/generate` streams. The rows do not
#     self-guard, and a green tick produced against a live daemon would be an
#     accident, not evidence. If they start failing, run `ss -lnt | grep 11434`
#     BEFORE looking at the Rust.
#
# ── Side effects: none, on either side ──────────────────────────────────────
#
# LAW 7 audit. A refused connection writes nothing, allocates nothing, and
# touches no file. These rows are safe anywhere in the file and in any order,
# and they leave `deps.current_log_path` alone.
#
# ── What is deliberately ABSENT, and why it is not an omission ──────────────
#
# `GET /api/pricing` and `POST /api/pricing/refresh` (RS-5-079/080) get **NO
# ROW**, here or in the merged file, ever. Both fetch the LiteLLM price book and
# `/api/pricing/refresh` writes `$STACKUNDERFLOW_HOME/cache/pricing.json` plus
# `price_book` rows — and that cache file is the INPUT to all five `PR-doctor*`
# rows. A `!` row would not help: DIV-059 established that `!` softens the
# VERDICT and still ISSUES the REQUEST, and this exact pair proved it once
# already by turning five clean `PR-doctor*` cases into five divergences from a
# case-file edit with no code change (`REFRESH-DIFFER.md` point 2). They are
# verified by the isolated procedure in `rust/PRICING-REFRESH-DIFFER.md`.
#
# A `transfer-encoding: chunked` row is also absent: law 4 bars rows for
# streams, and the branch is unreachable behind a closed port anyway.
#
# `/ollama-api` with NO trailing slash was absent for the same reason, and for
# the same reason is not any more: `SL-ollama-bare` measures the `307` now that
# DIV-133 is closed. The redirect is answered before the proxy is reached, so
# the row does not need the daemon that the rest of this block does without.


# ── the flip: replaces `!M-ollama` in endpoint-cases.txt ─────────────────────
# The GET the dashboard actually issues (the settings page's model list).
M-ollama            | GET | /ollama-api/tags                   | -

# ── the other three verbs `api_route(methods=[…])` claims ───────────────────
# Each one proves the METHOD reaches the same handler and the same catch-all.
# A router that only mounted GET would 405 here instead of 502.
M-ollama-post       | POST | /ollama-api/generate              | {"model": "llama3", "prompt": "hi"}
M-ollama-put        | PUT | /ollama-api/blobs/sha256-abc       | {"any": "payload"}
M-ollama-delete     | DELETE | /ollama-api/delete              | {"model": "llama3"}

# ── the path parameter's two edges ──────────────────────────────────────────
# `{path:path}` swallows slashes, so a nested rest is ONE parameter and not a
# 404. axum's `{*path}` had to be spelled as a wildcard to match this.
M-ollama-nested     | GET | /ollama-api/a/b/c                  | -
# `{path:path}` also matches the EMPTY rest: `/ollama-api/` is a real request
# that reaches the handler with `path == ""` and proxies to `…/api/`. axum's
# `{*path}` requires at least one segment, so this needs its own route or it
# falls through to the 404 fallback — the same shape `M-assets-bare` pins for
# `/assets/`.
M-ollama-empty      | GET | /ollama-api/                       | -

# The upstream URL is `f"http://localhost:11434/api/{path}"` — built from the
# PATH PARAMETER ONLY. The query string is silently dropped and never reaches
# Ollama. Unobservable while the port is closed, but the row pins that the query
# does not change the route match or the body.
M-ollama-query      | GET | /ollama-api/tags?stream=true&x=1   | -

# A percent-encoded separator, where the two stacks decode at DIFFERENT layers:
# uvicorn decodes the path before starlette routes it (so starlette matches
# `/ollama-api/show/name`), while hyper hands axum the raw target and only the
# `Path` extractor decodes (so matchit matches one segment, then yields
# `show/name`). Both end up interpolating the same decoded text into the
# upstream URL — this row pins that the layer difference does not surface.
M-ollama-encoded    | GET | /ollama-api/show%2Fname            | -

# ── cfg: the WRITER block. LAST in the file, and it self-cleans. ─────────────
#
# `POST` sets one alias, `DELETE` removes it. `settings._save` is
# `json.dumps(data, indent=2)` — the CLI writer — so each row writes the same
# bytes on both sides and the pair leaves `config.json` holding
# `"model_aliases": {}`, which IS the declared default. Stated exactly because
# "restores the file byte for byte" would be the convenient claim and is false:
# one key remains, holding the default, identically on both servers.
#
# Nothing below this line reads a price or a setting. Never reorder past here,
# and never leave the POST as the last row.
G-alias-set         | POST   | /api/cfg/model-aliases         | {"from": "parity/d-proxy", "to": "claude-opus-4-8"}
G-alias-set-again   | POST   | /api/cfg/model-aliases         | {"from": "parity/d-proxy", "to": "claude-opus-4-8"}
G-alias-read        | GET    | /api/cfg/model-aliases         | -
G-alias-in-cfg      | GET    | /api/cfg                       | -
# DIV-146, measured on this file's first run: `!` because the endpoint is a
# NON-IDEMPOTENT writer, and the differ runs Python then Rust against ONE shared
# home. Python's DELETE removes the alias and answers `{"aliases":{}}`; Rust
# then asks to remove an alias that is already gone and correctly answers its
# 404. Both are right. No ordering fixes it — the second server can never see
# the state the first one consumed — so a writer must be IDEMPOTENT to hold a
# green row here, exactly as a stream must be able to terminate (DIV-136).
# `G-alias-set` above IS idempotent, which is why it is green.
#
# The cleanup still happens: Python's leg does the removal, Rust's is a no-op,
# and the two rows below prove the end state agrees on both servers.
!G-alias-del        | DELETE | /api/cfg/model-aliases?from=parity/d-proxy | -
G-alias-del-again   | DELETE | /api/cfg/model-aliases?from=parity/d-proxy | -
G-alias-clean       | GET    | /api/cfg/model-aliases         | -

# ═══ CLOSING PASS (2026-08-01) — DIV-341, DIV-323, DIV-360, DIV-361 ══════════
#
# Appended at the END on purpose. Every row below is either a read or an error
# leg that returns before any mutation, but the ordering law in this file's
# header cuts both ways: a row that COULD write must have nothing after it that
# could read what it wrote. Nothing follows these.
#
#   P-set-*   DIV-341 — `POST /api/project` (RS-5-095) — PORTED 2026-08-01
#   P-global* DIV-341 — `GET /api/global-stats` (RS-5-101) — PORTED 2026-08-01
#   H-*       DIV-323 — HEAD is not GET
#   AL-*      DIV-323 / DIV-360 / DIV-361 — the `Allow` header and the mount

# ── DIV-341: the two endpoints that were invisible to a 716-row matrix ───────
#
# The decommission audit found `POST /api/project` (RS-5-095) and
# `GET /api/global-stats` (RS-5-101) with no axum registration, no ledger row,
# and — the part that matters — NO CASE ROW, not even a `!` one. The matrix
# reported `0 divergent` over 716 rows while two endpoints 404'd on the port.
# This file's own header says unported endpoints belong here, in the file, not
# missing from it. These two were the exception nobody filed.
#
# `GET /api/global-stats` is a plain read: `queries.get_global_stats(conn)`, a
# currency conversion and a config stamp. No writer, no network, no wall-clock
# in the payload (the two dates it carries come from the store). Measured on the
# reference: `200`, 38,146 bytes. It was a safe `!`; RS-5-101 landed 2026-08-01
# and it is byte-identical, so it is a plain row.
#
# ONE ROW COVERS ONE OF THE TWO STORE PATHS, and the gap is closed off-matrix.
# `queries.get_global_stats` branches on whether `daily_mart` has rows: the mart
# fast path (~10ms) or `_global_stats_raw_scan`, three full scans of the
# `messages` view that re-price every row through `compute_cost`. This home is
# backfilled — 1,418 `daily_mart` rows — so this row can only ever exercise the
# mart leg, and a fallback nobody sends a byte at is a divergence waiting for a
# fresh install. Proved separately, on a copy of this home with `daily_mart`
# emptied:
#
#   cp -a rust/.parity-state/fresh rust/.parity-state/global-rawscan
#   rm -f  rust/.parity-state/global-rawscan/store.db-{shm,wal}
#   sqlite3 rust/.parity-state/global-rawscan/store.db 'DELETE FROM daily_mart; VACUUM;'
#
#   STAX_ENDPOINT_HOME=…/.parity-state/global-rawscan \
#   STAX_ENDPOINT_DIFFS=…/endpoint-diffs-rawscan \
#   STAX_ENDPOINT_LOGS=…/endpoint-logs-rawscan \
#     rust/endpoint-parity.sh --only P-global-stats
#
# 1 identical / 0 divergent, over 384,697 messages, 3,070 ms python / 2,759 ms
# rust. No row here, because the state is not this file's.
P-global-stats      | GET  | /api/global-stats                  | -

# `POST /api/project` is a DIFFERENT animal and it gets three ERROR-LEG rows
# only, never a success one. Read `routes/projects.py:44-73` in order:
#
#     project_path = data.get("project_path")
#     if not project_path:              -> 400        (rows 1 and 2)
#     if not os.path.exists(...):       -> 400        (row 3)
#     log_path = find_claude_logs(...)
#     if not log_path ...:              -> 404
#     deps.current_project_path = project_path        <-- THE MUTATION
#     deps.current_log_path = log_path
#
# The three legs below all return at least two statements before that
# assignment, so they are provably side-effect-free — not "believed to be",
# *provably*, by the guard order.
#
# THE SUCCESS LEG HAS NO ROW, AND HERE IS WHY IT NEEDS A PROCEDURE INSTEAD.
# The mutation is not on disk, so a throwaway home does not contain it: it is
# PROCESS-GLOBAL server state, and it is the very state this file's ordering law
# exists to protect (`deps.current_log_path` is what every project-scoped GET
# reads). A `!` row for the success leg would re-point the reference's notion of
# the current project mid-run — and only the reference's, since the port has no
# handler to mutate anything — silently changing what every subsequent row
# means on one side and not the other. That is the DIV-078 hazard, and it is
# also finding 13 of `ARCHITECT-STATE.md` verbatim: a differ that talked to a
# server "which still had a project selected" reported a divergence that did not
# exist. `!` downgrades a VERDICT; it does not undo a side effect (DIV-136 made
# the same point from the other end — a `!` row can still fail the whole run).
#
# So the success leg is a PROCEDURE, on the model of the five wave-5 writer
# procedures (`ETL-BACKFILL-DIFFER.md` and its four siblings): boot each server
# on its OWN home, POST the same path to each, then diff the response AND the
# `GET /api/project` that follows it on the same server. It is written up in
# `rust/PROJECT-SET-DIFFER.md` and it is not run here.
#
# RUN 2026-08-01 as `rust/project-set-differ.sh`: six steps, two homes,
# 6/6 byte-identical, and the two homes' `store.db` still md5-equal afterwards —
# which is this argument's own premise made falsifiable. The three rows below
# are now plain: RS-5-095 landed and they are byte-identical too.
P-set-missing       | POST | /api/project                       | {}
P-set-empty         | POST | /api/project                       | {"project_path": ""}
P-set-nonexistent   | POST | /api/project                       | {"project_path": "/no/such/path/anywhere"}

# ── DIV-323: HEAD is not GET, and it never was ──────────────────────────────
#
# axum's `MethodRouter` aliases HEAD onto the GET handler; FastAPI's `APIRoute`
# does not add HEAD at all (starlette's plain `Route` does, which is why reading
# the framework docs would have got this backwards). So the reference answers
# `405` with `allow: GET` to a HEAD on EVERY one of the 93 endpoints and all
# four SPA pages, and the port answered `200` with a full `content-length`.
#
# 93 endpoints, one behaviour, and not one row in 716 sent a HEAD. These five
# are the standing guard: one API read, one flagship, one SPA page, the mount
# exception, and the 404 floor. They are NOT `!` rows — they pass, and they are
# supposed to keep passing.
H-head-health       | HEAD | /api/health                        | -
H-head-projects     | HEAD | /api/projects?limit=1              | -
H-head-spa-root     | HEAD | /                                  | -
# The exception that proves the rule is a rule about FastAPI, not about HEAD:
# `/static` is a starlette `StaticFiles` mount, which serves HEAD itself. Both
# sides answered `200` here before the fix and must still answer `200` after —
# this row is what stops the fix from being a blanket "HEAD is 405".
H-head-static       | HEAD | /static/react/index.html           | -
H-head-unknown      | HEAD | /api/definitely-not-a-route        | -
# The HEAD that used to differ, and it was DIV-341's shadow rather than
# DIV-323's. Starlette keeps the FIRST route whose path matches; `projects.py`
# declares `POST /api/project` at line 44 and `GET` at line 76, so the reference
# answers `allow: POST`, and the port — which had only the GET — answered
# `allow: GET`. A router cannot name a method it does not have.
#
# CLOSED 2026-08-01 by RS-5-095, with no edit to `method_semantics.rs`: that
# layer takes the first non-HEAD token of axum's `Allow`, axum lists methods in
# registration order, and `register()` now spells
# `post(set_project).get(get_current_project)` — `projects.py`'s own order. The
# row stays here as the standing guard on that ordering: swap the two calls and
# this is the only thing in 735 rows that notices.
H-head-project      | HEAD | /api/project                       | -

# ── DIV-323: the `Allow` header, which nothing had ever compared ────────────
#
# The differ gained an `allow` comparison for these rows (`parity/src/
# endpoints.rs`). Without it the whole class is invisible: status, content-type
# and body all AGREED on every 405 in the matrix while the header underneath
# them said `GET,HEAD,PUT,DELETE` on one side and `GET` on the other.
#
# `/api/budgets` is the discriminating case in the whole app — the only path
# carrying more than one method (GET, PUT, DELETE, declared in that order in
# `budgets.py`). Starlette answers with the first-declared route's methods and
# nothing else, so the reference says `allow: GET` to a PATCH. Any port that
# reports its registered SET instead passes every other 405 row and fails this
# one. 405 fires in the router, so no budget writer is reached.
AL-budgets-patch    | PATCH  | /api/budgets                     | {}
AL-budgets-post     | POST   | /api/budgets                     | {}
# Single-method paths, both directions: a GET-only route and a POST-only one.
AL-health-post      | POST   | /api/health                      | {}
AL-by-dir-get       | GET    | /api/project-by-dir              | -
# DIV-360 — the `/static` mount's 405 shape, found while measuring DIV-323 and
# never covered by a row. `StaticFiles.get_response` raises `HTTPException(405)`
# before it looks at the filesystem, so the reference renders FastAPI's JSON
# body with NO `Allow` header at all; tower-http's `ServeDir` answered a bare
# `405`, `allow: GET,HEAD`, no `content-type`, empty body. Both a real file and
# a missing one, because the reference's method check precedes its lookup.
AL-static-post      | POST   | /static/react/index.html         | {}
AL-static-post-miss | POST   | /static/react/nope.js            | {}
# DIV-361 — CLOSED, and by the one rule its own entry predicted. The bare mount
# ROOT is not the subtree: starlette compiles `Mount("/static")` to
# `^/static/(?P<path>.*)$`, which does not match `/static`, so the router falls
# through to `redirect_slashes` and answers a `307` to `/static/` on EVERY
# method. axum's `nest_service` registers `/static` itself, which is why this is
# the one path `path_semantics` names explicitly instead of asking the router.
# Three rows so the class cannot be closed on one method and called done — a
# fourth for the destination, which is a 404 on both sides and always was.
AL-static-root-get  | GET    | /static                          | -
AL-static-root-head | HEAD   | /static                          | -
AL-static-root-put  | PUT    | /static                          | {}
AL-static-root-post | POST   | /static                          | {}
AL-static-slash     | GET    | /static/                         | -
# DIV-461 (new, found while measuring DIV-361) — `StaticFiles` is constructed
# WITHOUT `html=True`, and `ServeDir` defaults `append_index_html_on_directories`
# ON. So a directory under the mount was a `307 location: /react/` (prefix-less,
# because `nest_service` had already stripped `/static`, so it pointed at a path
# that does not exist) and then a `200 index.html`, against `{"detail":"Not
# Found"}` on both spellings for the reference. Live since wave 5 and invisible
# because no row had ever asked the mount for a directory.
AL-static-dir       | GET    | /static/react                    | -
AL-static-dir-slash | GET    | /static/react/                   | -

# ── DIV-367 — CLOSED. The `dict`-bodied handler class, byte-for-byte ────────
#
# `set_project(data: dict[str, str])` is not "give me the parsed body". FastAPI
# hands the body to pydantic, which checks BOTH the container and every value,
# and a failure never reaches the handler — it is a `422` from the installed
# `request_validation_exception_handler`. The port used to parse with
# `serde_json`, read `.get(key).and_then(as_str)`, and let a non-string fall
# through to the handler's OWN `if not project_path` guard: a `400`, with a
# plausible message, for a request the reference never let in. A status is what
# a caller branches on, so that was strictly worse than DIV-053.
#
# THE CLASS, ENUMERATED FROM THE PYTHON TREE (ast, every `@router.*` handler
# whose body parameter mentions `dict`) — ten handlers, three annotations:
#
#   dict[str, str]        projects.py:45   POST /api/project
#   (container + VALUES)  projects.py:96   POST /api/project-by-dir
#                         tags.py:43       POST /api/tags/session/{id}
#
#   dict[str, Any]        bookmarks.py:56  POST /api/bookmarks
#   (container only)      bookmarks.py:107 PUT  /api/bookmarks/{id}
#                         bookmarks.py:146 POST /api/bookmarks/toggle
#
#   dict (bare)           cfg.py:83        POST /api/cfg/currency
#   (container only)      cfg.py:119       POST /api/cfg/model-aliases
#                         data.py:1104     POST /api/refresh          — NO ROW
#
#   dict | None = None    etl.py:86        POST /api/etl/backfill     — NO ROW
#
# The two "NO ROW" members keep their standing rulings: both are writers whose
# 422 legs are safe *only if the port rejects*, and "a port bug that failed to
# reject `[]` would turn the safe row into a live mart rebuild". They inherit
# the shared extractor without a row, and `routes/data.rs`'s unit test is what
# stands in for one. Everything else below is rowed.
#
# All ten used to have their own private check — SIX distinct spellings, four of
# which answered a `400`. There is now ONE: `crate::json::{dict_body,
# str_dict_body, optional_dict_body}`.
#
# EVERY SHAPE BELOW WAS MEASURED against the reference on `.parity-state/fresh`
# (2026-08-02, fastapi + pydantic v2), never transcribed — DIV-127's lesson is
# that an error shape no probe issued is a guess, and `routes/data.rs` had been
# carrying four guessed ones for two waves. Three findings came out of the
# probe:
#
#   1. A body of the four bytes `null` is **`missing`**, not `dict_type`.
#      FastAPI hands pydantic `None`, and `None` against a required field is
#      "no value supplied" — the container check never runs. No transcription
#      had this. `V-set-json-null` is the row.
#   2. `dict[str, str]` reports EVERY offending key, in **body order**, not the
#      first. `V-set-two-bad` sends `{"z": 1, "a": 2}` and gets `z` then `a`,
#      which is insertion order and not sorted order — the row that makes
#      `serde_json`'s `preserve_order` feature load-bearing rather than
#      incidental.
#   3. The malformed-body leg is **not pydantic's at all**. `fastapi/routing.py`
#      calls `await request.json()` and catches CPython's `JSONDecodeError`
#      itself, so `loc` carries `e.pos` and `ctx.error` carries `e.msg` — and
#      that is true for a `BaseModel` body exactly as for a `dict` one. The
#      three model-bodied handlers therefore join the class for this one leg;
#      see the `V-*-bad-json` rows at the bottom.
#
# SIDE-EFFECT-FREE BY CONSTRUCTION, and it is checked per row rather than
# assumed: every leg returns either inside FastAPI's validator (before the
# handler exists) or on the handler's FIRST guard, on BOTH implementations.
# `V-bm-put-*` are aimed at a bookmark id that does not exist, so even the leg
# that is *supposed* to reach the handler stops at its 404.

# `dict[str, str]` — the value half. `{"project_path": 123}` used to be a 400.
V-set-int           | POST | /api/project                       | {"project_path": 123}
V-set-null          | POST | /api/project                       | {"project_path": null}
V-by-dir-int        | POST | /api/project-by-dir                | {"dir_name": 123}
V-by-dir-null       | POST | /api/project-by-dir                | {"dir_name": null}
V-tag-null          | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | {"tag": null}
# Finding 2: every offending key, in body order. Sorted order would say `a, z`.
V-set-two-bad       | POST | /api/project                       | {"z": 1, "a": 2}
# A valid value beside an invalid one still fails, and only the bad key reports.
V-set-mixed         | POST | /api/project                       | {"project_path": "", "x": 3}
# `dict[str, str]` — the container half, shared with the other two annotations.
V-set-list          | POST | /api/project                       | []
V-set-num           | POST | /api/project                       | 3
V-set-str           | POST | /api/project                       | "x"
V-by-dir-list       | POST | /api/project-by-dir                | []
V-tag-list          | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | []
# Finding 1: `null` and an absent body are the SAME answer, and it is `missing`.
V-set-json-null     | POST | /api/project                       | null
V-set-nobody        | POST | /api/project                       | -
V-tag-nobody        | POST | /api/tags/session/04a86c63-4cfd-4929-91a3-3edfe80e3e2f | -
V-cur-nobody        | POST | /api/cfg/currency                  | -
# `dict[str, Any]` — the container is checked, the VALUES are not. `V-bm-put-int`
# is the row that proves the difference: `{"notes": 3}` is legal, reaches the
# handler, and stops at the 404 for a bookmark that does not exist. Without it,
# an over-eager shared extractor that string-checked everything would pass every
# other row in this block.
V-bm-list           | POST | /api/bookmarks                     | []
V-bm-toggle-list    | POST | /api/bookmarks/toggle              | []
V-bm-put-list       | PUT  | /api/bookmarks/no-such-bookmark    | []
V-bm-put-int        | PUT  | /api/bookmarks/no-such-bookmark    | {"notes": 3}
# bare `dict` — container only, which is why `K-cur-not-string` ({"code": 3})
# reaches the handler and takes its 400 two hundred rows above this one.
V-cur-list          | POST | /api/cfg/currency                  | []
V-alias-list        | POST | /api/cfg/model-aliases             | []
# Finding 3: the malformed-body leg is FastAPI's own, built before pydantic is
# reached, and CPython's decoder is underneath it. Two offsets and two of the
# nine messages, because a single `nope` at offset 0 would pass against a
# hard-coded `(0, "Expecting value")` — which is exactly what three route
# modules had.
V-set-bad-json      | POST | /api/project                       | nope
V-bm-bad-json       | POST | /api/bookmarks                     | nope
V-alias-bad-json    | POST | /api/cfg/model-aliases             | {oops
# The same leg on the three pydantic-`BaseModel` bodies (`BudgetBody`,
# `DismissRequest`, `ClaudeMdPreviewBody`). The annotation does not reach this
# code path, so the bytes are identical to the dict-bodied ones — and
# `routes/optimize.rs` had written the opposite in a doc comment.
#
# `POST /api/patterns/dismiss` has no row at any other status (it bumps a
# counter in `proactive_state.json`, non-idempotent under one shared home), and
# `DIV-e-patterns.md` finding 10 named its four 422 legs as an instructed
# law-5 gap. This one is closable because it is the ONLY leg where both
# implementations refuse in the JSON decoder: `{oops` cannot be parsed by
# CPython or by `serde_json`, so `record_dismissal` is unreachable from this row
# no matter which side has the bug. The other three legs are pydantic's and stay
# unprobed.
V-budget-bad-json   | PUT  | /api/budgets                       | {oops
V-dismiss-bad-json  | POST | /api/patterns/dismiss              | {oops
V-preview-bad-json  | POST | /api/optimize/claudemd-preview     | {oops

# ══ DIV-168 — `%2F`, and the class it turned out to be ════════════════════════
#
# uvicorn percent-decodes the path BEFORE starlette routes it:
#
#   raw_path, _, query_string = url.partition(b"?")
#   path = raw_path.decode("ascii")
#   if "%" in path:
#       path = urllib.parse.unquote(path)
#
# axum matches the raw path and lets `axum::extract::Path` decode afterwards.
# DIV-168 filed the consequence — "`%2F` routes differently on EVERY `{param}`
# endpoint … invisible only because no row anywhere sends `%2F`" — and named
# three endpoints that were GREEN at the time. Zero rows sent one until now.
#
# `crate::path_semantics` decodes as uvicorn does and re-encodes everything
# except `/`, so a decoded `%2F` becomes a structural slash the matcher sees and
# every other escape goes back on the wire for `Path` to undo exactly once.
#
# The three DIV-168 named. A `[^/]+` convertor cannot span a slash, so the
# reference never matches these at all: `{"detail":"Not Found"}`, where the port
# used to hand the handler a session id containing a slash.
PCT-sa-slash        | GET | /api/static-analysis/session/a%2Fb  | -
PCT-interaction-sl  | GET | /api/interaction/a%2Fb              | -
PCT-ctxreplay-slash | GET | /api/context-replay/a%2Fb           | -
PCT-sa-lower        | GET | /api/static-analysis/session/a%2fb  | -
# The class DIV-168 did NOT name, and the reason this is a decode rather than a
# `%2F` special-case: ANY escape of an unreserved character re-routes on the
# reference. `%61` is `a`, so this is `/api/plan` there — a 200 with the plan
# payload — and was a 404 here.
PCT-plan-unreserved | GET | /api/pl%61n                         | -
# The rows that must NOT move, which are the ones a naive decode breaks. `Path`
# decodes what the matcher captured, so a pre-decoded path would be decoded
# TWICE: `%2520` would arrive as a space instead of as `%20`.
PCT-space           | GET | /api/static-analysis/session/a%20b  | -
PCT-double-escape   | GET | /api/static-analysis/session/a%2520b | -
# `+` is a space in a QUERY string and a literal plus in a PATH — uvicorn calls
# `unquote`, `crate::qs` calls `unquote_plus`, and the asymmetry is the
# reference's.
PCT-plus            | GET | /api/static-analysis/session/a+b    | -
PCT-utf8            | GET | /api/static-analysis/session/%C3%A9 | -
# A malformed escape is left literal by `unquote`, and an undecodable byte run
# is `errors="replace"`.
PCT-bad-escape      | GET | /api/static-analysis/session/a%zzb  | -
PCT-invalid-utf8    | GET | /api/static-analysis/session/%FF    | -

# ══ DIV-050 — `CORSMiddleware` ════════════════════════════════════════════════
#
# ```python
# app.add_middleware(
#     CORSMiddleware,
#     allow_origins=[f"http://localhost:{_server_port}",
#                    f"http://127.0.0.1:{_server_port}",
#                    "http://localhost:5175", "http://127.0.0.1:5175"],
#     allow_credentials=True, allow_methods=["*"], allow_headers=["*"])
# ```
#
# DIV-050 was filed as "the parity differ is same-origin, so nothing measures
# it, and porting it blind would mean inventing a header order nobody has
# observed". The ruling was port it — so the first act was making it
# observable: rows carry request headers now, and the differ compares the five
# `access-control-*` names plus `vary`. Nothing below was written before it was
# measured.
#
# `_server_port` is `config.get("port")` — the SETTING, not the port uvicorn
# bound. The reference runs on :8097 here and its allow-list still says :8081.
# A port that used its own listening port would pass every row that names 5175
# and fail these two, which is why they are here.
CORS-simple-cfgport | GET | /api/health | - | Origin: http://localhost:8081
CORS-simple-cfg127  | GET | /api/health | - | Origin: http://127.0.0.1:8081
CORS-simple-vite    | GET | /api/health | - | Origin: http://localhost:5175
CORS-simple-vite127 | GET | /api/health | - | Origin: http://127.0.0.1:5175
# The measured surprise, and the reason `tower_http::cors::CorsLayer` is not
# what this is: `simple_headers` is applied UNCONDITIONALLY, so a refused origin
# still gets `access-control-allow-credentials: true` and gets neither the echo
# nor the `Vary`. `CorsLayer` emits nothing at all for a refused origin.
CORS-simple-refused | GET | /api/health | - | Origin: http://evil.example
# Membership is an exact string compare — no scheme coercion, no case folding,
# no trailing-slash tolerance.
CORS-origin-case    | GET | /api/health | - | Origin: http://LOCALHOST:5175
CORS-origin-slash   | GET | /api/health | - | Origin: http://localhost:5175/
CORS-origin-https   | GET | /api/health | - | Origin: https://localhost:5175
# `allow_all_origins` is False, so the `has_cookie` branch is dead: a cookie on
# a refused origin changes nothing. The row exists because the branch is in the
# reference's source and "unreachable" is a claim, not an observation.
CORS-simple-cookie  | GET | /api/health | - | Origin: http://evil.example;;Cookie: a=b
# The headers ride on whatever the inner stack produced — a 404, a 405, and (a
# consequence of this layer being OUTSIDE `path_semantics`) a 307.
CORS-on-404         | GET | /api/nope | - | Origin: http://localhost:5175
CORS-on-405         | POST | /api/health | - | Origin: http://localhost:5175
CORS-on-307         | GET | /api/health/ | - | Origin: http://localhost:5175
CORS-on-static      | GET | /static/react/index.html | - | Origin: http://localhost:5175
# ── preflight: `OPTIONS` **and** an `access-control-request-method` ──────────
# It never reaches the router, which is the one place a middleware outranks
# routing. `PlainTextResponse("OK")` — `text/plain; charset=utf-8`, two bytes.
CORS-pre-ok         | OPTIONS | /api/health | - | Origin: http://localhost:5175;;Access-Control-Request-Method: GET
# `allow_all_headers` MIRRORS the requested headers back verbatim — spacing and
# casing preserved — rather than emitting the sorted safelisted union.
CORS-pre-headers    | OPTIONS | /api/health | - | Origin: http://localhost:5175;;Access-Control-Request-Method: POST;;Access-Control-Request-Headers: X-Foo, Content-Type
# The four failure legs, and the asymmetry between them: the origin echo is set
# BEFORE the method check, so a `method` failure keeps the echo and an `origin`
# failure does not. The joined text is ordered origin-then-method.
CORS-pre-bad-origin | OPTIONS | /api/health | - | Origin: http://evil.example;;Access-Control-Request-Method: GET
CORS-pre-bad-method | OPTIONS | /api/health | - | Origin: http://localhost:5175;;Access-Control-Request-Method: BREW
CORS-pre-bad-both   | OPTIONS | /api/health | - | Origin: http://evil.example;;Access-Control-Request-Method: BREW
CORS-pre-lower-verb | OPTIONS | /api/health | - | Origin: http://localhost:5175;;Access-Control-Request-Method: get
# A preflight for a path that does not exist, and for one that would have
# redirected. Both are `200 OK`: the router is never consulted.
CORS-pre-unknown    | OPTIONS | /api/nope-nothing | - | Origin: http://localhost:5175;;Access-Control-Request-Method: GET
CORS-pre-slash-path | OPTIONS | /api/health/ | - | Origin: http://localhost:5175;;Access-Control-Request-Method: GET
CORS-pre-static     | OPTIONS | /static/react/index.html | - | Origin: http://localhost:5175;;Access-Control-Request-Method: GET
# An `OPTIONS` WITHOUT `access-control-request-method` is not a preflight: it
# falls through to the router and comes back a `405 allow: GET` wearing the
# simple-request headers. The discriminator is the header, not the verb.
CORS-options-plain  | OPTIONS | /api/health | - | Origin: http://localhost:5175
# …and with no `Origin` at all the middleware returns before it does anything,
# which is what keeps the other 760-odd rows in this file unmoved.
CORS-options-bare   | OPTIONS | /api/health                       | -
